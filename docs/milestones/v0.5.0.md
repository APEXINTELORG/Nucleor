# v0.5.0 Milestone Tracker

**Target:** 2027-Q1
**Theme:** Robotics + DbC + URDF + atomics
**Status (2026-04-23, post-v0.2.204):** **Robotics roadmap CLOSED.**
The compute path is complete end-to-end: URDF input → FK chain
→ IK solve (3D + 6D + joint limits + singularity detection) →
collision (sphere/capsule/AABB/OBB pairs + GJK + EPA + CCD) →
motion planning (RRT family + PRM Dijkstra + BVH + A*) →
trajectory parameterization (quintic / trapezoid / S-curve / DMP
/ TOPP). Remaining v0.5 items are framing (RFC-0003 generic
frame-typed Pose/Vec3, deferred to v0.6) + DbC + atomics +
package-manager registry.

This is the **canonical sequencing doc** for the v0.5.0 release. It
picks up after v0.4 lands generics + frame-typed values + the v0.2
robotics primitives are wrapped in the type-system overlay.

## RFCs targeted at v0.5.0

| RFC | Title | Status |
|---|---|---|
| [0006](../rfcs/RFC-0006-design-by-contract.md) | DbC — `#[require], #[ensure], #[invariant]` | Draft |
| [0007](../rfcs/RFC-0007-atomic.md) | `#[atomic]` + lock-free queues (SPSC, MPMC) | Draft |
| [0013](../rfcs/RFC-0013-urdf-static-frames.md) | URDF-aware compile-time frame chain verification | Draft |
| [0014](../rfcs/RFC-0014-max-depth.md) | `#[max_depth = N]` — bounded recursion attribute | Draft |
| [0019 cont.](../rfcs/RFC-0019-package-manager.md) | Registry + PubGrub + git deps | Implemented (partial) v0.2 |
| [0022 cont.](../rfcs/RFC-0022-cross-platform.md) | Sysroots + cross-compile | Implemented (partial) v0.3 |

## Robotics-stack v0.5 follow-on

The compute path shipped in the v0.2.174-179 ship arc. v0.5 adds
the framing + safety overlay:

### From v0.2.174 (kinematics) — RFC-0003

- [ ] Generic `Pose<F: Frame>`, `Vector3<F: Frame>` types
- [ ] Compile-time frame-mismatch detection on `pose_apply`,
      `vec3_*` ops between mixed-frame values
- [ ] `#[frame]` attribute for declaring user frames
- [ ] Migration path from v0.2 untagged Pose / Vec3 to the
      generic-tagged form

### From v0.2.175 (fk_chain) + v0.2.176 (ik_dls) — RFC-0013

- [x] **URDF parser (`urdf_parse(handle, src_ptr) -> n_joints`) — v0.2.204**
- [ ] Compile-time chain validation: every joint's parent frame
      must be the previous link's child frame *(deferred to v0.6
      alongside RFC-0003 frame-typed values)*
- [x] **DH-parameter constructor (alternative to URDF) — v0.2.182**
- [x] **Joint-limit enforcement in IK solver — v0.2.193**
- [x] **Singularity-detection metric in IK — v0.2.199**
- [x] **Orientation IK (6×n Jacobian) — v0.2.194**

### From v0.2.177 (trajectory)

- [x] **Trapezoidal velocity profile (caps on max velocity + acceleration) — v0.2.181**
- [x] **S-curve / bounded-jerk profile — v0.2.191**
- [x] **Dynamic Movement Primitives (DMPs) for learnable trajectories — v0.2.192**
- [x] **TOPP — time-optimal path parameterization for piecewise-
      linear paths (forward+backward pass with corner detection
      and within-segment peak integration) — v0.2.203**
      *(full TOPP-RA convex-optimization formulation deferred to
      v0.6 if needed)*

### From v0.2.178 (collision)

- [x] **GJK for general convex shapes — v0.2.183**
- [x] **GJK EPA penetration depth + contact normal — v0.2.202**
- [x] **Continuous collision detection (CCD)** — sphere-sphere
      (v0.2.196), capsule-capsule + sphere-AABB (v0.2.201)
- [x] **BVH builder + traversal — v0.2.184**
- [x] **Sphere-AABB + capsule-AABB cross-pairs — v0.2.195**
- [x] **Sphere-OBB cross-pair — v0.2.197**
- [ ] Mesh-mesh collision via convex decomposition *(deferred to
      v0.6; GJK + EPA already cover convex-mesh-on-convex-mesh
      via support functions)*

### From v0.2.179 (rrt)

- [x] **RRT-Connect — bidirectional tree — v0.2.188**
- [x] **RRT* — asymptotically optimal — v0.2.190**
- [x] **PRM (Probabilistic Roadmap) — v0.2.185**
- [x] **PRM Dijkstra query (multi-query against precomputed roadmap) — v0.2.200**
- [x] **A* shortest-path search (foundation) — v0.2.186**
- [x] **Path shortcutting + smoothing — v0.2.182**
- [x] **Goal-region sampling (vs goal-point) — v0.2.198**

## Design by Contract (RFC-0006)

`#[require(predicate)]` on fn entry, `#[ensure(predicate)]` on fn
exit, `#[invariant(predicate)]` on struct types. Predicates are
arbitrary boolean expressions; checked at runtime by default,
suppressible with `--release` or `--profile=cert` (where the
proof-checker bridge produces a static guarantee).

| Phase | Item |
|---|---|
| 1 | `#[require]` parsing + IR codegen |
| 2 | `#[ensure]` with access to result via `result` keyword |
| 3 | `#[invariant]` on struct types — checked at every public mut |
| 4 | `nuc explain DBC-001..010` for contract violations |
| 5 | Build-profile gate: cert mode rejects unproven contracts |

Builds on RFC-0004 `#[assume(...)]` infrastructure (v0.4) for the
predicate parser.

## Atomics + lock-free queues (RFC-0007)

`#[atomic]` attribute on shared values; SPSC + MPMC lock-free
queue rods backed by C11 `_Atomic` primitives in the runtime.
Foundation for real-time messaging in robotics control loops.

| Phase | Item |
|---|---|
| 1 | C11 atomic primitives in runtime |
| 2 | `#[atomic]` attribute + memory-order parameters |
| 3 | SPSC queue rod (Lamport's algorithm) |
| 4 | MPMC queue rod (Vyukov's algorithm) |
| 5 | Compatibility audit: `#[atomic] + #[no_alloc]` (RT path) |

## `#[max_depth = N]` (RFC-0014)

Bounded recursion attribute. The compiler verifies the call graph
of any fn marked `#[max_depth = 8]` reaches at most 8 deep on any
path; otherwise compile error. Foundation for stack-bounded
real-time systems.

| Phase | Item |
|---|---|
| 1 | Parse + store the bound on the fn signature |
| 2 | Build call graph in the type pass |
| 3 | Cycle detection — `#[max_depth]` on a cyclic fn group must
      have a wrapping argument that proves termination |
| 4 | Diagnostic `RT-008` for depth-bound violation |

## Package manager v0.5 follow-on (RFC-0019)

| Phase | Item |
|---|---|
| 6 | Registry server + client |
| 7 | PubGrub-style version resolution |
| 8 | Git-dep support |
| 9 | `nuc publish` to public registry |
| 10 | `nuc yank` for vulnerability response |

## Cross-platform v0.5 follow-on (RFC-0022)

| Phase | Item |
|---|---|
| 5 | Sysroot management — `nuc target add aarch64-unknown-linux-gnu` |
| 6 | Cross-compile end-to-end test suite |
| 7 | Pre-built sysroots for top-5 embedded targets |

## External roadmap input (2026-04-28)

Three items picked up from the parallel ML Suite agent's
[Top 15 roadmap](EXTERNAL_ROADMAP_INPUT_2026-04-28.md):

### Developer tooling MVP (Pick A)

| Phase | Item |
|---|---|
| 1 | `nuc fmt` — AST→text serializer respecting existing rod conventions |
| 2 | LSP server (VS Code first): hover, goto-def, completion, diagnostics |
| 3 | Editor extension on the marketplace |

Why: language quality is judged through the editor before users read
the compiler architecture. v0.5 fits cleanly because v0.4 is already
loaded with RFC-0023..0029.

### RFC-0031 Capsule signing native to compiler (Pick C)

Move today's Python-side hashing/signing/verification into Nucleor.
Support signed `.ncap`, provenance, agent trace, source hashes,
compiler hash, rod graph hash, model/data hashes, hardware profile.

| Phase | Item |
|---|---|
| 1 | Native SHA-256 / Ed25519 in compiler runtime |
| 2 | `nuc capsule sign` / `nuc capsule verify` first-class commands |
| 3 | NCAP envelope spec wired (port from `Nucleor_ML_Suite_ParallelAgent_Mainline/docs/ecosystem_blueprints/NCAP_SIGNING_TRUST_SPEC.md`) |
| 4 | Reproducible-rerun verifier: source + data + model + metrics + SBOM |

### `nuc port` Python migration MVP (Pick D)

| Phase | Item |
|---|---|
| 1 | Python AST analyzer → migration plan emitter |
| 2 | Convert low-risk modules: type-annotated functions, dataclasses, plain numerics |
| 3 | Generate parity harness for converted modules |
| 4 | Python extension module generation (C ABI bridge for kept-Python sibs) |

Note: the parallel agent has scaffolding in
`Nucleor_ML_Suite_ParallelAgent_Mainline/examples/python_parity/python_port_smoke/port_plan.md`
that's the starting design.

### Cross-project crossover note (added 2026-04-28)

Picks A / C / D have substantial overlap with the active
**Nucleor_Translate** project at `C:\Users\JoeWe\Desktop\Nucleor_Translate`
(spec phase as of 2026-04-26):

- **Pick D (`nuc port` Python)** is essentially Nucleor_Translate's
  Phase A Python front-end. Nucleor_Translate ingests Python (one
  of 10 source languages) and emits Nucleor `.nr` source / rods /
  exes via tier labels (`@tier(native | wrapped | redesigned)`) and
  verification labels (`@verified(tested | differential | strict |
  unverified)`). The MVP described under Pick D is the Phase A
  deliverable shape from a different angle. **Coordinate with the
  Translate spec before starting Pick D — likely the right move is
  to make `nuc port` the CLI surface over the Translate engine
  rather than building a parallel implementation.**

- **Pick C (RFC-0031 native capsule signing)** overlaps with
  Translate's "Coordination item #1" — a `.nucleor_provenance`
  PE/ELF section emitted by `nuc build` for watermark + provenance.
  RFC-0031 capsule signing is the bigger story; the provenance
  section is one specific channel within it. Translate's scope says
  this needs Nucleor-team coordination BEFORE Phase A launch.

- **Pick A (`nuc fmt` + LSP)** is more tangential to Translate but
  still relevant: a translator that emits Nucleor source needs
  canonical formatting. Building `nuc fmt` first means Translate's
  emitted code can `nuc fmt`-clean itself rather than each Translate
  rod re-implementing layout rules.

Before starting Picks A / C / D in the v0.5 cycle, **read the
Translate spec** (`Nucleor_Translate\docs\superpowers\specs\
2026-04-26-nucleor-translate-design.md`) to confirm the dependency
graph and avoid duplicate implementation paths.

## Success criteria

v0.5.0 ships when:

- [ ] All v0.5-targeted RFCs implemented to definition-of-done
- [ ] Robotics rod set has the v0.5 follow-on items above
- [ ] Verify gate green on Windows + Linux + macOS for the
      robotics test suite
- [ ] DbC contracts have ≥80% adoption in `stdlib/rods/`
- [ ] Migration story for v0.4→v0.5 in CHANGELOG
- [ ] `nuc fmt` + LSP MVP shipped (external roadmap Pick A)
- [ ] RFC-0031 native capsule signing shipped (external roadmap Pick C)
- [ ] `nuc port` Python migration MVP shipped (external roadmap Pick D)
