# v0.6.0 Milestone Tracker

**Target:** 2027-Q2
**Theme:** Units, ISRs, embedded targets
**Status (2026-04-24):** Planning

## RFCs targeted at v0.6.0

| RFC | Title | Status |
|---|---|---|
| [0005](../rfcs/RFC-0005-units.md) | `unit<T, dim>` — typed dimensional units | Draft |
| [0008](../rfcs/RFC-0008-isr.md) | `#[isr]` — interrupt service routine attribute | Draft |

## Typed dimensional units (RFC-0005)

`unit<T, dim>` where `dim` is a compile-time-checked dimension
expression (e.g. `length`, `length/time`, `mass·length²/time²`
for energy). The compiler enforces dimensional correctness on
arithmetic — adding meters to seconds is a compile error;
multiplying meters by seconds yields `unit<f64, length·time>`.

This is the type-system overlay for the existing 17 CODATA 2018
constants + SI conversion functions (already shipped in v0.1
via `physics.nr`). v0.6 makes them dimensionally enforced.

Mars Climate Orbiter: `nuc mco` already explains why this
matters; v0.6 is the prevention.

| Phase | Item |
|---|---|
| 1 | Dimension expression syntax + parser |
| 2 | Type-system propagation through `+`, `-`, `*`, `/`, `^` |
| 3 | Diagnostic `UNIT-001..006` family |
| 4 | Migrate `physics.nr` constants to dimensioned form |
| 5 | Migrate `linalg.nr` operations to preserve dimension |

## `#[isr]` interrupt service routine (RFC-0008)

`#[isr]` attribute marks a fn as eligible for use as a hardware
interrupt service routine. Composes with v0.3 `#[no_alloc]` +
v0.5 `#[max_depth]` + the runtime tier system. Fn must:

- Have `void(void)` signature (no args, no return)
- Be `#[no_alloc]`
- Be `#[max_depth = N]` for some N
- Optionally `#[deadline = T]` (RFC-0001 v0.3)
- Compile to a sequence with no `call` to non-`#[isr]`-eligible fns

| Phase | Item |
|---|---|
| 1 | `#[isr]` attribute parsing + signature check |
| 2 | Composition checker: `#[isr]` implies the above set |
| 3 | Linker-section directive (`.text.isr`) for the embedded
      flash layout |
| 4 | Vector-table glue: `#[isr_vector(IRQ_NUM)]` registers the fn |

## Embedded-target sysroots

Concrete embedded targets supported as build targets:

- ARM Cortex-M (M0+, M3, M4F, M7) via `arm-none-eabi`
- RISC-V 32-bit (rv32imac) via `riscv32-none-elf`
- ESP32 (Xtensa) via `xtensa-esp32-elf`

Each gets a sysroot with stripped libc (newlib-nano) and a
linker script template.

## External roadmap input (2026-04-28)

Two items picked up from the parallel ML Suite agent's
[Top 15 roadmap](EXTERNAL_ROADMAP_INPUT_2026-04-28.md):

### `nuc serve model.ncap` MVP (Pick E)

| Phase | Item |
|---|---|
| 1 | Real GGUF / safetensors / ONNX weight loader (mmap + lazy decode) |
| 2 | Tokenizer integration end-to-end with real vocab + merges |
| 3 | `nuc serve` HTTP/streaming endpoint with batching + KV cache |
| 4 | Backend selection (CPU SIMD baseline + later CUDA/MLIR adapter) |
| 5 | Telemetry hooks for capsule reproducibility evidence |

Why: the wedge for AI adoption. ML Suite already has GGUF/safetensors/
ONNX manifests; making `nuc serve` execute them end-to-end closes the
demo loop. Adoption story: "from hf model → nuc serve → curl" in
under five minutes.

### Flagship apps (Pick F) — first 3 of 5 land in v0.6

The parallel agent specified 5 concrete flagship apps. Three land in
v0.6 (the AI/data-flow ones); the robotics + regulated artifact apps
land in v0.7.

| # | Flagship app |
|---|---|
| 1 | Local LLM server from `.ncap` with GGUF/safetensors fixture |
| 4 | Dataframe → model pipeline replacing a small pandas/sklearn workflow |
| 3 | Differentiable physics/control example with gradients + safety constraints |

(Apps 2 and 5 — robot perception/control + reproducible regulated
artifact — land in v0.7. See [`v0.7.0.md`](v0.7.0.md).)

## Success criteria

v0.6.0 ships when:

- [ ] RFC-0005 + RFC-0008 implemented to definition-of-done
- [ ] At least 3 embedded targets build with `nuc build --target`
- [ ] Verify gate runs the embedded test suite under qemu-system
- [ ] Migration story for v0.5→v0.6 in CHANGELOG
- [ ] `nuc serve model.ncap` MVP shipped (external roadmap Pick E)
- [ ] 3 of 5 flagship apps shipped (external roadmap Pick F)
