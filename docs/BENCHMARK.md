# Verify Timing Summary

The generated timing-dashboard path is retained for tooling compatibility, but
the public benchmark reference is now [benchmarks.md](benchmarks.md).

`tools/verify_timings*.csv` files are local rolling artifacts and are ignored by
git. Regenerate this page only from a curated release timing run.
