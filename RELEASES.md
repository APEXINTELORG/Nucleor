# Releases

Tag-only navigable index of every Nucleor release. Generated by `tools/gen_releases_index.py` from `CHANGELOG.md`.

For full per-release notes (changes, gate status, fixed point), see [`CHANGELOG.md`](CHANGELOG.md). For RFC-by-RFC status see [`docs/rfcs/README.md`](docs/rfcs/README.md). For the v0.2 shipped/deferred rollup see [`docs/status/v0.2-shipped-and-deferred.md`](docs/status/v0.2-shipped-and-deferred.md).

**Total releases:** 1175

GitHub tag URLs follow the pattern `https://github.com/APEXINTELORG/Nucleor/releases/tag/vX.Y.Z`.

## v0.8.x

| Version | Date | Summary |
|---|---|---|
| **v0.8.13** | 2026-05-04 | Phase B step-3 — multi-key range capture for `@thermal` |
| **v0.8.12** | 2026-05-04 | Fix: str method dispatch for untyped let bindings (wrong-class TYP-005) |
| **v0.8.11** | 2026-05-04 | Phase B step-2 expansion — numeric value extraction from
`@energy(max=2mJ)`, `@thermal(max_temp=85C)`, `@within(200ns)` |
| **v0.8.10** | 2026-05-04 | Phase B step-2 expansion — `@policy` policy-name + `@enclave`
engine-name extraction |
| **v0.8.9** | 2026-05-04 | Phase B step-2 first ship — `@authored` author-list extraction |
| **v0.8.8** | 2026-05-04 | Phase B step-1 — core Nucleor attribute audit (`@hot`,
`@const_fn`, `#[max_depth]`, `#[deadline]`) |
| **v0.8.7** | 2026-05-04 | Phase B step-1 — extended attribute audit (7 attribute
families across RFC-0052 / 0053 / 0057 / 0060 / 0054) |
| **v0.8.6** | 2026-05-04 | Phase B step-1 — RFC-0050 `@energy` / `@thermal` build-time
audit count |
| **v0.8.5** | 2026-05-04 | First Phase B compiler-side ship — RFC-0045 Phase B step-1:
`@differentiable` build-time audit count |
| **v0.8.4** | 2026-05-04 | Punchlist forward — RFC-0061 Tier 5 V1.17c CLOSED: graph-
capabilities documentation consolidation |
| **v0.8.3** | 2026-05-04 | Punchlist forward — RFC-0061 Tier 3 Phase A: quantum
entanglement union-find + gate-influence DAG (rod + runtime) |
| **v0.8.2** | 2026-05-04 | Punchlist forward — RFC-0058 Phase A: post-quantum crypto
surface (rod + STUB runtime). Closes V2 frontier Tier-B lane |
| **v0.8.1** | 2026-05-04 | Punchlist forward — RFC-0057 Phase A: enclave types + info-flow
label surface (rod + runtime) |
| **v0.8.0** | 2026-05-04 | Punchlist forward — RFC-0056 Phase A: deterministic-replay
event-log surface (rod + runtime) |

## v0.7.x

| Version | Date | Summary |
|---|---|---|
| **v0.7.99** | 2026-05-04 | Punchlist forward — RFC-0055 Phase A: distributed collectives
+ sharding-spec surface (rod-only, single-rank pass-through) |
| **v0.7.98** | 2026-05-04 | Bootstrap fix — compile_error! false-positive on Linux self-compilation
(src_path exclusion guard) |
| **v0.7.97** | 2026-05-04 | Punchlist forward — RFC-0054 Phase A: logical-qubit +
pulse-level type surface (rod-only) |
| **v0.7.96** | 2026-05-04 | Punchlist forward — RFC-0053 Phase A: neuromorphic /
event-driven type surface (rod + LIF runtime) |
| **v0.7.95** | 2026-05-04 | Combined ship — RFC-0052 Phase A (photonic-compute type
surface) + Rust `Self { ... }` constructor halt |
| **v0.7.94** | 2026-05-04 | Punchlist forward — RFC-0061 Tier 2 Phase A: graph-rendering
format helpers (rod-only) |
| **v0.7.93** | 2026-05-04 | Punchlist forward — RFC-0045 Phase A: fn-level
differentiability metadata registry (rod-only). Closes
drift-restoration V1.12-V1.14 lane |
| **v0.7.92** | 2026-05-04 | Punchlist forward — RFC-0044 Phase A: per-BinOp overflow-mode
helper surface (rod-only). Drift restoration V1.13 |
| **v0.7.91** | 2026-05-04 | Punchlist forward — RFC-0043 Phase A: Q-format fixed-point
arithmetic helpers (rod-only). Drift restoration V1.12 lane
opens |
| **v0.7.90** | 2026-05-04 | Punchlist forward — RFC-0051 Phase A: foundation-model
provenance-tracking surface (rod-only). Closes V2 frontier
Tier-A easy-win lane |
| **v0.7.89** | 2026-05-04 | Punchlist forward — RFC-0050 Phase A: per-fn energy + thermal
budget metadata registry (rod-only) |
| **v0.7.88** | 2026-05-04 | Combined ship — RFC-0048 Phase A (hardware capability queries) +
Rust `compile_error!(...)` halt |
| **v0.7.87** | 2026-05-04 | Combined ship — RFC-0049 Phase A (memory-space type tags) +
Rust prelude `drop(value)` halt |
| **v0.7.86** | 2026-05-04 | Punchlist forward — RFC-0061 Tier 1 final item: `nuc graph` and
`nuc impact` CLI verb docs (V1.17a closeout) |
| **v0.7.85** | 2026-05-04 | Real feature acceptance — path-qualified trait name in `impl` block
(`impl fmt::Display for T`, `impl std::fmt::Display for T`) |
| **v0.7.84** | 2026-05-04 | Punchlist forward — RFC-0047 Phase A: 7-vector SI dimensional
surface (rod-only, no compiler change) |
| **v0.7.83** | 2026-05-04 | Punchlist forward — RFC-0046 Phase A: coordinate-frame marker
types (no compiler enforcement yet) |
| **v0.7.82** | 2026-05-04 | Defensive halt — `?` operator inside a closure body (wrong-class silent miscompute) |
| **v0.7.81** | 2026-05-04 | Combined ship — RFC-0061 Tier 1 adjacency-matrix view +
array-destructure-in-let halt |
| **v0.7.80** | 2026-05-04 | Combined ship — RFC-0061 Tier 1 negative-cycle detection +
wildcard-`_` if-let halt |
| **v0.7.79** | 2026-05-04 | Combined ship — RFC-0060 Phase 2b (governance PolicyDecl +
check_policies) + Rust `#[test]` attribute halt |
| **v0.7.78** | 2026-05-04 | Combined ship — RFC-0061 Tier 1 partial (graph remediation
V1.17a) + Rust block-comment halt |
| **v0.7.77** | 2026-05-04 | Defensive halts (consolidated) — `yield <expr>;` (Rust nightly
coroutine syntax) and `(a, b) = (1, 2);` (Rust 1.59+ tuple-LHS
destructuring assignment) |
| **v0.7.76** | 2026-05-04 | Defensive halt — Rust 1.79+ `const { ... }` inline-const block
expression (closes wrong-class NR020 at parse_primary) |
| **v0.7.75** | 2026-05-04 | Defensive halts (consolidated) — Rust syntax-fidelity audit:
`fn f(mut x: T)` and raw-identifier prefix `r#name` |
| **v0.7.74** | 2026-05-04 | Defensive halt — Rust experimental `try { ... }` block expression
(closes wrong-class TYP-005 from `try`-as-fn-call lowering) |
| **v0.7.73** | 2026-05-04 | Defensive halts (consolidated) — module-scope silent-strip family:
`extern crate <name>;` and `macro_rules! name { ... }` now both halt
cleanly at module-item dispatch |
| **v0.7.72** | 2026-05-04 | Defensive halt — Rust 2024 if-let / while-let chain
(`if let PATTERN = EXPR && cond { ... }`) closes wrong-class
TYP-005 from a pattern-binding shadow in the desugared scrutinee |
| **v0.7.71** | 2026-05-04 | Defensive halt — Rust `async fn` (closes wrong-class
silent-acceptance at module-item dispatch) |
| **v0.7.70** | 2026-05-04 | Perf — integrate helper agent's strtab-intern hash slice
(`origin/perf/strtab-intern-hashmap` @ `6c884044`) |
| **v0.7.69** | 2026-05-04 | Defensive halt — Rust opt-out trait bound `?Sized` / `?Trait`
(closes wrong-class silent-acceptance in `parse_generic_params`) |
| **v0.7.68** | 2026-05-04 | Defensive halt — Rust C-string literal `c"..."` (closes wrong-class
TYP-005 family). Promoted from probe finding 2026-05-03 (raw/byte/
C-string family) |
| **v0.7.67** | 2026-05-04 | Feature — `#[derive(Clone)]` on named and tuple structs now auto-generates
a field-by-field `<Type>__clone(a) -> <Type>` helper; calling `.clone()` on
a struct without `#[derive(Clone)]` now produces a clean halt instead of
synthesising a wrong-class `vec_clone(...)` call that fails at clang link
(TYP-005) |
| **v0.7.66** | 2026-05-04 | Test consolidation — collapse 3 RFC 2795 format-arg fixtures
(inline / positional / named) into 1 combined fixture. Fixture
count drops 50 → 48 |
| **v0.7.65** | 2026-05-04 | Test consolidation — collapse 4 local-decl fixtures (type-alias /
const / static / fn inside fn body) into 1 combined fixture.
Fixture count drops 53 → 50 |
| **v0.7.64** | 2026-05-04 | Test consolidation — collapse 6 idiom-method fixtures into 1
combined fixture. Fixture count drops 58 → 53; underlying halt
path (v0.6.82 idiom-list) is unchanged |
| **v0.7.63** | 2026-05-04 | Perf — integrate helper's P2 hot-path allocation hunt
(origin/perf/p2-hotpaths-v722). Cold drops 3.85s → 3.26s median
(-590ms / -15%); peak 301MB |
| **v0.7.62** | 2026-05-04 | Defensive halt — qualified-path supertrait `trait Sub:
super::Iterator { ... }` (or `trait Sub: std::fmt::Display`) now
produces a clean halt with a drop-the-namespace workaround
pointer |
| **v0.7.61** | 2026-05-04 | Defensive halt — `unsafe trait T { ... }` and `unsafe impl T
for S { ... }` now produce clean halts. Sister to v0.6.53 `unsafe
fn` halt — closes the silent-strip class for trait/impl decls |
| **v0.7.60** | 2026-05-04 | Defensive halt — paren-wrapped pattern in `if let` / `while let`
(`if let (Some(x)) = opt { ... }`) now produces a clean halt with
a drop-the-parens workaround pointer. Sister to v0.7.54
paren-wrapped variant-payload halt |
| **v0.7.59** | 2026-05-04 | Defensive halt — local fn declaration `fn inner(...)` inside a
fn body / match-arm block now produces a clean halt with hoist /
closure workaround pointers. Sister to v0.7.47/55/56 (local-type-
alias / -const / -static halts) |
| **v0.7.58** | 2026-05-04 | Feature acceptance — `where`-clause on enum declaration now
builds cleanly. Promotes the v0.7.57 halt to acceptance, sister
to v0.6.46 struct/fn where-clause acceptance |
| **v0.7.57** | 2026-05-04 | Defensive halt — `where`-clause on enum declaration (`enum E<T>
where T: Copy { ... }`) now produces a clean halt with a hoist-
to-inline-bound workaround pointer |
| **v0.7.56** | 2026-05-04 | Defensive halt — local `static N: T = ...;` inside a fn body
now produces a clean halt with a `let`-binding workaround. Sister
to v0.7.55 local-const and v0.6.21 module-scope static halts |
| **v0.7.55** | 2026-05-04 | Defensive halt — local `const N: T = ...;` inside a fn body
now produces a clean halt with hoist / let-binding workaround
pointers. Sister to v0.7.47 local-type-alias halt |
| **v0.7.54** | 2026-05-04 | Defensive halt — paren-wrapped payload inside a variant pattern
(`Some((1))`, `Ok((x, y))`) now produces a clean halt with a
drop-the-parens / anonymous-tuple workaround pointer |
| **v0.7.53** | 2026-05-04 | 🛑 Compiler SEGFAULT fix — bare float-literal payload inside a
variant pattern (`Some(1.5)`, `Ok(2.0)`) now produces a clean halt
instead of crashing the compiler. Sister to v0.7.51 int-payload
crash fix |
| **v0.7.52** | 2026-05-04 | Defensive halt — module-scope forward fn declaration `fn h() ->
i64;` (no body, semicolon-terminated) now produces a clean halt
with `extern fn` / `todo!()` workaround pointers |
| **v0.7.51** | 2026-05-04 | 🛑 Compiler SEGFAULT fix — bare integer-literal payload inside
a variant pattern (`Some(1)`, `Ok(0)`, `Err(42)`) now produces a
clean halt instead of crashing the compiler |
| **v0.7.50** | 2026-05-04 | Defensive halt — or-patterns in `if let` / `while let` (e.g.
`if let Some(1) \| Some(2) = opt { ... }`) now produce a clean halt
with a match-form workaround pointer |
| **v0.7.49** | 2026-05-04 | Defensive halt — typeless default-value fn parameter
`fn h(x: i64, y = 5)` (no explicit `:type`) now produces a clean
halt. Sister to v0.7.38 typed default-arg halt — completes the
default-arg family |
| **v0.7.48** | 2026-05-04 | Defensive halt — Rust C-FFI variadic param `extern "C" fn
printf(fmt: str, ...);` now produces a clean halt with a
fixed-arity wrapper-fn workaround pointer |
| **v0.7.47** | 2026-05-04 | Defensive halt — local type alias `type Foo = Bar;` inside a fn
body now produces a clean halt with a hoist-to-module-scope
workaround pointer |
| **v0.7.46** | 2026-05-04 | Defensive halt — `else if` (or `else if let`) chained after an
`if let` (`if let Some(x) = a { ... } else if let Some(y) = b
{ ... }`) now produces a clean halt with a nested-else workaround
pointer |
| **v0.7.45** | 2026-05-04 | Defensive halt — tuple-struct pattern in match arm
(`match p { P(a, b) => ... }`) now produces a clean halt with a
field-access workaround pointer |
| **v0.7.44** | 2026-05-04 | Defensive halt — impl-target type with lifetime in its generic
args (`impl<'a> R<'a> { ... }`) now produces a clean halt with a
drop-the-lifetime workaround pointer |
| **v0.7.43** | 2026-05-04 | Defensive halt — Rust named format-arg `format!("{x}", x = 5)`
now produces a clean halt with explicit-positional workaround
pointer |
| **v0.7.42** | 2026-05-04 | Defensive halt — Rust positional format-arg `{0}` / `{1}` /
etc. (referencing args by index, allowing reuse like
`{0} {1} {0}`) now produces a clean halt with linear-args
workaround pointer. Also fixes a v0.7.41 regression that wrongly
classified positional refs as inline args |
| **v0.7.41** | 2026-05-04 | Defensive halt — Rust 1.58+ inline format-arg `{name}` (where
`<name>` resolves to a binding in the calling scope) now produces
a clean halt with explicit-arg workaround pointer |
| **v0.7.40** | 2026-05-04 | Defensive halt — Rust ownership/conversion-trait methods
(`.into_iter`, `.try_into`, `.try_from`, `.from`, `.borrow`,
`.borrow_mut`, `.into_inner`, `.leak`, `.as_str`, `.as_slice`,
`.as_ptr`) now route to the v0.6.82 idiom-list halt with drop-call
or explicit-conversion workarounds |
| **v0.7.39** | 2026-05-04 | Defensive halt — pattern-destructure in fn parameter
(`fn h((a, b): (i64, i64))`, `fn h(P { x, y }: P)`) now produces a
clean halt with a single-name + body-destructure workaround
pointer |
| **v0.7.38** | 2026-05-04 | Defensive halt — default-value fn parameter `fn f(n: i64 = 1)`
(C++ / Python idiom — Rust does not support this either) now
produces a clean halt with overload / Option<T> workaround
pointers |
| **v0.7.37** | 2026-05-04 | Defensive halt — Rust path type in type-annotation position
(`std::collections::HashMap<K,V>`, `core::option::Option<T>`,
`crate::MyType`) now produces a clean halt with an unqualified-name
workaround pointer instead of wrong-class
`error[NR020]: parse_primary cannot start an expression at token kind 46` |
| **v0.7.36** | 2026-05-04 | Feature acceptance — `todo!()` and `unimplemented!()` macros now
expand to canonical Rust-stdlib panic messages ("not yet
implemented" / "not implemented") matching `std::macros::todo` /
`std::macros::unimplemented`. Sister to v0.6.56 `unreachable!()` |
| **v0.7.35** | 2026-05-04 | Defensive halt — top-level `ref` / `ref mut` binding mode in
a match arm pattern (`match x { ref n => ... }`) now produces a
clean halt with a drop-the-keyword workaround pointer |
| **v0.7.34** | 2026-05-04 | Defensive halt — Rust iterator/Option/Result conversion idioms
(`.copied`, `.cloned`, `.as_ref`, `.as_mut`, `.and_then`,
`.or_else`, `.ok_or`, `.ok_or_else`, `.transpose`, `.expect_err`)
now route to the v0.6.82 idiom-list halt with drop-the-call or
explicit-match workarounds |
| **v0.7.33** | 2026-05-04 | Defensive halt — Rust Vec mutating + sort + extend methods
(`.dedup`, `.retain`, `.truncate`, `.resize`, `.split_at`,
`.sort_by`, `.sort_by_key`, `.sort_unstable`, `.sort_unstable_by`,
`.max_by_key`, `.min_by_key`, `.extend_from_slice`) now route to
the v0.6.82 idiom-list halt with manual-loop workarounds |
| **v0.7.32** | 2026-05-04 | Defensive halt — Rust string-iterator and conversion methods
(`.chars`, `.lines`, `.bytes`, `.as_bytes`, `.repeat`,
`.split_whitespace`, `.split_terminator`, `.splitn`, `.rsplit`,
`.char_indices`, `.parse`) now route to the v0.6.82 idiom-list
halt with index-based-loop workarounds |
| **v0.7.31** | 2026-05-04 | Defensive halt — Rust char-classification methods (`.is_alphabetic`,
`.is_numeric`, `.is_alphanumeric`, `.is_ascii`, `.is_whitespace`,
`.is_lowercase`, `.is_uppercase`, `.is_digit`, `.to_ascii_uppercase`,
`.to_ascii_lowercase`, `.to_lowercase`, `.to_uppercase`, `.to_digit`)
now route to the v0.6.82 idiom-list halt with codepoint-range
workarounds |
| **v0.7.30** | 2026-05-04 | Defensive halt — Rust integer-intrinsic methods (`.abs()`, `.pow()`,
`.signum()`, `.count_ones()`, `.count_zeros()`, `.leading_zeros()`,
`.trailing_zeros()`, `.rotate_left()`, `.rotate_right()`,
`.swap_bytes()`, `.checked_add/sub/mul`, `.wrapping_add/sub/mul`,
`.saturating_add/sub/mul`) now route to the v0.6.82 idiom-list halt
with per-method workarounds |
| **v0.7.29** | 2026-05-04 | Defensive halt — Rust Unicode-escape char literal `'\u{NNNN}'`
(e.g. `'\u{0041}'`, `'\u{1F600}'`) now produces a clean halt with
a hex-codepoint workaround pointer |
| **v0.7.28** | 2026-05-04 | Defensive halt — Rust byte literal `b'A'` (u8 ASCII-value form)
now produces a clean halt with a char-cast workaround pointer |
| **v0.7.27** | 2026-05-04 | Defensive halt — Rust ref-pattern in `for`-loop binding position
(`for &x in &v { ... }`, `for &mut x in &mut v { ... }`) now
produces a clean halt with a drop-the-ampersand workaround
pointer |
| **v0.7.26** | 2026-05-04 | Defensive halt — Rust FFI extern block form `extern "C" { fn
cfn(x: i64) -> i64; ... }` now produces a clean halt with a
single-line workaround pointer |
| **v0.7.25** | 2026-05-04 | Perf — tighten the v0.7.20 built-in-macro halt from 36 LOC down
to 8 LOC; cold median 3.8s → 3.45s (-350ms / -9%) |
| **v0.7.24** | 2026-05-04 | Defensive halt — bare negative-literal payload inside a variant
pattern (`Some(-5)`, `Ok(-1)`, `Err(-9)`) now produces a clean
halt with a guard workaround pointer |
| **v0.7.23** | 2026-05-04 | Defensive halt — `ref` / `ref mut` binding mode inside a variant
pattern (`Some(ref v)`, `Ok(ref mut s)`, etc.) now produces a clean
halt with a drop-the-keyword workaround pointer |
| **v0.7.22** | 2026-05-04 | Defensive halt — bare negative-literal pattern in match arm
(`match x { -5 => ... }`) now produces a clean halt with a guard
workaround pointer (MATCH-015) |
| **v0.7.21** | 2026-05-04 | Defensive halt — parenthesized range expression `(start..end)`,
`(start..=end)` (e.g. `(0..10).collect()`, `for i in (0..3) {}`,
`let r = (0..N);`) now produces a clean halt with
form-specific workaround pointers |
| **v0.7.20** | 2026-05-04 | Defensive halt — Rust built-in macros that Nucleor doesn't yet
support (`file!`, `line!`, `column!`, `module_path!`, `stringify!`,
`concat!`, `env!`, `option_env!`, `include_str!`, `include_bytes!`)
now produce clean halts with per-macro workaround pointers |
| **v0.7.19** | 2026-05-04 | Defensive halt — `@` binding inside a variant pattern
(`Some(n @ 1..=10)`, `Ok(s @ "a")`, `Err(e @ MyErr::X)`) now produces
a clean halt with hoist-or-guard workaround pointers |
| **v0.7.18** | 2026-05-03 | Governance rod Phase 2a — RFC-0060 scaffolding lands as an
optional rod. Programs that don't import it pay zero cost |
| **v0.7.17** | 2026-05-04 | Defensive halt — `if let` guard in match arm (Rust 1.77 let-chains)
now produces a clean halt with a hoist-above-match workaround pointer |
| **v0.7.16** | 2026-05-03 | Fix — `nuc test` / `nuc lock` / `nuc doc` failed with
`'"nucleor_tools.exe"' is not recognized` when the helper binary
was sandbox-local. Now resolved |
| **v0.7.15** | 2026-05-04 | Feature acceptance — `dyn Trait + Bound` type expressions
(e.g., `&dyn Write + Send`, `Box<dyn Foo + Sync + 'static>`) now
parse cleanly; additional bounds are accepted and silently stripped |
| **v0.7.14** | 2026-05-03 | Defensive halt — Rust 2021 raw-reference syntax `&raw const T` /
`&raw mut T` now produces a clean halt with a `&` / `&mut`
workaround pointer |
| **v0.7.13** | 2026-05-03 | Defensive halt — canonical Rust trait alias `trait Pretty =
Show;` (RFC 1733, nightly-only in Rust) now produces a clean
halt with a supertrait-list workaround pointer |
| **v0.7.12** | 2026-05-03 | Defensive halt — Rust stdlib smart-pointer types (`Cell`,
`RefCell`, `Rc`, `Arc`, `Mutex`, `RwLock`, `Weak`) now produce
clean halts with "use bare type" workaround pointers |
| **v0.7.11** | 2026-05-03 | 🛑 Compiler-crash fix — canonical Rust never type `!`
(diverging-fn return) was a SIGSEGV (rc=139) pre-fix; now a
clean halt with workaround pointer |
| **v0.7.10** | 2026-05-03 | Defensive halt — canonical Rust const-generic params
`<const N: usize>` now produce a clean halt with a regular-fn-arg
workaround pointer |
| **v0.7.9** | 2026-05-03 | Defensive halt — canonical Rust `async { ... }` block
expression now produces a clean halt with a "drop the `async`
keyword" workaround pointer |
| **v0.7.8** | 2026-05-03 | Defensive halt — canonical Rust complex-receiver `.await`
(`fetch().await`, `arr[0].await`, `expr.method().await`) now
produces a clean halt with a let-extract workaround pointer |
| **v0.7.7** | 2026-05-03 | Defensive halt — C/Java-idiom `x++` / `x--` post-increment/
decrement operators now produce clean halts with explicit-assignment
workaround pointers |
| **v0.7.6** | 2026-05-03 | Defensive halt — canonical Rust `T::default()` (Default trait)
now produces a clean halt with per-type literal-default
workaround pointers |
| **v0.7.5** | 2026-05-03 | Defensive halt — canonical Rust path-rooted fn calls
`crate::foo(...)`, `super::foo(...)`, `self::foo(...)` now
produce a clean halt with a "drop the path prefix" workaround
pointer |
| **v0.7.4** | 2026-05-03 | Defensive halt — canonical Rust `extern "C" fn name() { body }`
(FFI-exposed fn with body) now produces a clean halt with a
"drop the `extern \"C\"`" workaround pointer |
| **v0.7.3** | 2026-05-03 | `&T` / `&mut T` |
| **v0.7.2** | 2026-05-03 | Defensive halt — canonical Rust loop labels (`'outer: for ...
{ break 'outer; }`, `'lbl: loop { continue 'lbl; }`) now produce
a clean halt with a boolean-flag workaround pointer |
| **v0.7.1** | 2026-05-03 | Defensive halt extension — canonical Rust iterator adapter
idioms (`.rev()`, `.enumerate()`, `.zip()`, `.windows()`,
`.chunks()`, `.flat_map()`, `.flatten()`, `.peekable()`,
`.fuse()`) now produce clean halts with per-idiom index-based-
loop workaround pointers |
| **v0.7.0** | 2026-05-03 | Major version boundary — Rust marker traits (`Sized`, `Send`,
`Sync`, `Unpin`, `RefUnwindSafe`, `UnwindSafe`, `Copy`, `Clone`)
now accepted as no-ops in trait bounds. Adopters porting Rust
generic-fn code with these standard bounds get a clean build |

## v0.6.x

| Version | Date | Summary |
|---|---|---|
| **v0.6.99** | 2026-05-03 | Defensive halt — canonical Rust associated types in traits
(`type Item;`, `type Item = i64;`) now produce a clean halt with
a generic-param workaround pointer |
| **v0.6.98** | 2026-05-03 | RFC V1.7-ext — turbofish syntax `.method::<TypeArgs>(args)`
(e.g. `.collect::<Vec<i64>>()`) now parses + lowers |
| **v0.6.97** | 2026-05-03 | Defensive halt — canonical Rust struct-update syntax
(`Point { x: 10, ..rest }`, functional record update) now produces
a clean halt with a per-field workaround pointer |
| **v0.6.96** | 2026-05-03 | Defensive halt — canonical Rust stdlib namespace path
`std::mem::swap`, `std::cmp::min`, `std::collections::HashMap::new`
etc. (3+ `::`-separated path with `std`/`core`/`alloc` root) now
produces a clean halt with per-helper workaround pointers |
| **v0.6.95** | 2026-05-03 | Defensive halt — canonical Rust let-else
(`let Some(v) = opt else { return; };`, Rust 1.65+) and ctor-
pattern-in-let (`let Some(v) = opt;`) now produce clean halts
with `if let` / `match` workaround pointers |
| **v0.6.94** | 2026-05-03 | Defensive halt — canonical Rust range expression at expression
position (`let r = 0..10;`, `(0..10).step_by(2)`) now produces
a clean halt with a while-loop workaround pointer |
| **v0.6.93** | 2026-05-03 | Correctness fix — explicit `self: &Self` / `self: &mut Self`
param shape in impl-block methods now substitutes `Self` to the
concrete type. Closes a long-standing canonical-Rust idiom hazard |
| **v0.6.92** | 2026-05-03 | Defensive halt — canonical Rust `impl Trait` type position
(`fn make_iter() -> impl Iterator<Item = i64>`) now produces a
clean halt with workaround pointer |
| **v0.6.91** | 2026-05-03 | Defensive halt — canonical Rust `Box<dyn Trait>` polymorphism
method dispatch (`let d: Box<dyn Animal> = ...; d.speak();`) now
produces a clean halt with workaround pointer |
| **v0.6.90** | 2026-05-03 | Defensive halt — canonical Rust nested pattern in `if let` /
`while let` / `match` (e.g. `if let Some(Some(v)) = opt`) now
produces a clean halt with a 2-step-destructure workaround pointer |
| **v0.6.89** | 2026-05-03 | RFC V1.4 ext — derive(PartialEq) now works for GENERIC structs.
Canonical Rust `#[derive(PartialEq)] struct Pair<T> { x: T, y: T }`
auto-generates a generic helper `fn Pair__derived_eq<T>(a: Pair<T>,
b: Pair<T>) -> i64` |
| **v0.6.88** | 2026-05-03 | `assert_eq!(a, b)` / `assert_ne!(a, b)` (no format args) now
route through the `==` / `!=` binop auto-dispatch, closing the
silent-miscompute hazard for str / String / derived-PartialEq
struct values |
| **v0.6.87** | 2026-05-03 | `str == str` / `String == String` now auto-dispatch to the
runtime helpers `str_eq` / `string_eq`. Closes the v0.4.52 +
v0.4.151 silent-miscompute hazard that adopters had to manually
work around with `str_eq(a, b) == 1` |
| **v0.6.86** | 2026-05-03 | RFC V1.4 (correctness fix) — `!=` auto-dispatch was using `add`
opcode instead of `sub`. Recursive derive(PartialEq) now correct |
| **v0.6.85** | 2026-05-03 | RFC V1.4 — auto-dispatch `==` / `!=` to the derived-eq helper.
V1.4 fully shipped (paired with v0.6.84 helper-fn generation) |
| **v0.6.84** | 2026-05-03 | RFC V1.4 (partial) — `#[derive(PartialEq)]` now auto-generates
the helper fn `<Type>__derived_eq(a, b) -> i64`. Adopters call it
manually; auto-dispatch of `==` / `!=` to the helper is deferred
to a follow-on V1.4 ship |
| **v0.6.83** | 2026-05-03 | Defensive halt — Option/Result methods not yet in the runtime
surface (`unwrap_or_default`, `take`, `replace`, `flatten`,
`as_ref`, `as_mut`, `iter`, `ok_or`, `ok_or_else`, `cloned`,
`copied`, `map_err`, `expect_err`) now halt cleanly with per-
method workaround pointers |
| **v0.6.82** | 2026-05-03 | Defensive halt — canonical Rust conversion-idiom methods
(`.into()` / `.to_string()` / `.to_owned()` / `.to_vec()` /
`.iter_mut()`) now produce clean halts with per-idiom workaround
pointers instead of the wrong-class `vec_<idiom>` post-link diag |
| **v0.6.81** | 2026-05-03 | RFC V1.7 — UFCS dispatch `<Type as Trait>::method(args)`. Canonical
Rust universal function call syntax now parses + lowers |
| **v0.6.80** | 2026-05-03 | TYP-006 String hint — adopters passing a `String` to `print` /
`str_eq` / `str_concat` / etc. now see a hint pointing at the safe
coercion `string_as_str(s)` |
| **v0.6.79** | 2026-05-03 | RFC V1.11 (defensive halt) — anonymous-tuple field access
`let p: (i64, i64) = (5, 7); p.0` now produces a clean halt
with a named-tuple-struct workaround pointer |
| **v0.6.78** | 2026-05-03 | RFC V1.11 — `&[T]` slice type as fn-parameter. Canonical Rust
`fn first(s: &[i64]) -> i64` now parses + lowers |
| **v0.6.77** | 2026-05-03 | RFC V1.11 — `[VAL; N]` array-literal repeat-init. Canonical Rust
`let buf: Vec<i64> = [0; 4];` now parses + lowers |
| **v0.6.76** | 2026-05-03 | RFC V1.9 — `assert!` / `assert_eq!` / `assert_ne!` format-args
expansion. Canonical Rust `assert!(cond, "fmt {}", arg)` now
expands and produces formatted panic message on failure |
| **v0.6.75** | 2026-05-03 | RFC V1.2 — generic-T inference for `Box::new(literal)`. Canonical
Rust `let b: Box<i64> = Box::new(5);` now type-checks |
| **v0.6.74** | 2026-05-03 | RFC V1.1 steps 2 + 3 — tuple-struct constructor `Pair(5, 10)`
+ positional access `p.0`/`p.1`. V1.1 fully shipped |
| **v0.6.73** | 2026-05-03 | RFC V1.1 step 1 — tuple-struct decl `struct P(T1, T2);` parses
with synthesized `__0`/`__1` field names. Construction +
`.0`/`.1` access ship in subsequent steps |
| **v0.6.72** | 2026-05-03 | Restore strtab_intern to v0.6.70 floor — backward-scan via locality
of reference. Closes the +0.4s drift introduced post-v0.6.70 |
| **v0.6.71** | 2026-05-03 | Regression fix: v0.6.68 lex-time halt for `9223372036854775808`
broke v0.6.43 + v0.6.49 fixtures (canonical Rust `-9223372036854775808`
i64::MIN form) |
| **v0.6.70** | 2026-05-03 | String constant pool dedup — closes B4 of the parallel-agent
punchlist. .ll output -3.1% / 4572 fewer `@.str.` references on
the self-host |
| **v0.6.69** | 2026-05-03 | Nested struct pattern in match arms — clean parse halt. Closes
sub-case 1 of the nested-struct-pattern-and-tuple-struct-decl
finding |
| **v0.6.68** | 2026-05-03 | Match-arm literal overflow caught at lex time — closes the
match-arm-literal-exceeds-i64-silently-dead probe finding |
| **v0.6.67** | 2026-05-03 | Type alias resolver — `type Name = T;` decls now resolve at use
sites. Closes the type-alias sub-gap of the module-scope-decl
silent-noop finding |
| **v0.6.66** | 2026-05-03 | `let c: char = 'a';` accepted — char-literal type matching closes
the char-literal row of the rust-syntax-translation-fidelity audit |
| **v0.6.65** | 2026-05-03 | RFC-0034 gap 3 — struct CT-params parse halt. All 3 RFC-0034
gaps now closed |
| **v0.6.64** | 2026-05-03 | NUM-021 gap 1 — u64 const overflow now caught at compile time |
| **v0.6.63** | 2026-05-03 | RFC-0034 gap 1 — explicit CT-arg call SEGFAULT → clean parse halt |
| **v0.6.62** | 2026-05-03 | `move` closure parse halt — closes another sub-row of the
keyword-silent-strip audit |
| **v0.6.61** | 2026-05-03 | `let Point { x, y } = p;` parse halt — closes another rust-syntax-
translation-fidelity audit row |
| **v0.6.60** | 2026-05-03 | UFCS `<Type as Trait>::method(args)` parse halt — closes another
audit row |
| **v0.6.59** | 2026-05-03 | `for (k, v) in &m` parse halt — closes another rust-syntax-
translation-fidelity audit row |
| **v0.6.58** | 2026-05-03 | `r"..."` raw string + `b"..."` byte string parse halts — closes
two more rust-syntax-translation-fidelity audit rows |
| **v0.6.57** | 2026-05-03 | `break <value>` parse halt — closes the break-with-value row of
the rust-syntax-translation-fidelity audit |
| **v0.6.56** | 2026-05-03 | Two batched closures — `unreachable!()` macro + RFC-0034 gap 2 |
| **v0.6.55** | 2026-05-03 | `const fn` parse halt — closes the const-fn row of the rust-
syntax-translation-fidelity audit |
| **v0.6.54** | 2026-05-03 | Perf-slice integration from `Nucleor_OSS_v631_perf_slice` —
huge cold + memory wins |
| **v0.6.53** | 2026-05-03 | Three batched closures: NUM-021 gap 3 + tuple-struct halt +
unsafe-fn halt |
| **v0.6.52** | 2026-05-03 | IEEE 754 negative-zero sign bit preserved through unary minus —
two probe-finding closures, sidesteps the v0.6.48-attempt-1
bootstrap-cycle hole |
| **v0.6.51** | 2026-05-03 | HashMap<i64, V> CRITICAL crash → clean compile-time halt — probe
finding closure |
| **v0.6.50** | 2026-05-03 | Batch closure ship — 4 probe findings landed in one cycle |
| **v0.6.49** | 2026-05-03 | Canonical `i64::MIN` literal `-9223372036854775808` accepted at
compile time (was NUM-021 false-fire) — probe finding closure
`2026-05-02-num-021-coverage-gaps-u64-imin-shift-divzero` gap 2 |
| **v0.6.48** | 2026-05-03 | str runtime helpers accept `&s` (parity with bare `s`) — probe
finding closure / translation-fidelity |
| **v0.6.47** | 2026-05-03 | `print_f64` switches to scientific notation at extremes (probe
finding closure — diagnostic-quality / silent value loss) |
| **v0.6.46** | 2026-05-03 | Where-clauses on struct decls + impl blocks + impl generic-param
lists `impl<T>` parse cleanly (probe finding closure — Rust
canonical shapes that previously NR020-panicked) |
| **v0.6.45** | 2026-05-03 | NUM-019 — const-folded negative binop now caught for unsigned
bindings (probe finding closure — silent miscompute coverage gap) |
| **v0.6.44** | 2026-05-03 | `vec_insert_at` PANICs on OOB instead of silently clamping
(probe finding closure — silent miscompute, asymmetric Vec OOB
coverage) |
| **v0.6.43** | 2026-05-03 | **Unary `-` overflow check at runtime — `neg(i64::MIN)` now panics |
| **v0.6.42** | 2026-05-03 | `let x = vec_pop(v)` now fires TYP-021 instead of silently
binding 0 (probe finding closure — silent miscompute on void-
returning helper) |
| **v0.6.41** | 2026-05-03 | `vec![X; N]` Rust repeat syntax now produces a length-N Vec
instead of silently dropping N (probe finding closure — silent
miscompute on canonical Rust idiom) |
| **v0.6.40** | 2026-05-03 | Diag improvement — fn-form `vec_<method>(…)` now suggests method
form `vec.<method>(…)` instead of misleading "no method" wording
(probe finding closure) |
| **v0.6.39** | 2026-05-03 | `panic!` macro routes format args through the same expansion
path as `println!` (probe finding partial closure — gap 1 of
panic-family format-arg drop) |
| **v0.6.38** | 2026-05-03 | Diagnostic dedup at the emit site — TYP-006 triple-emit
collapses to single error block (probe finding closure, sister
to v0.5.18 MATCH-001/TYP-001 dual-emit fix) |
| **v0.6.37** | 2026-05-03 | NUM-008 — width-aware shift-amount bound (probe finding closure
— silent miscompute on narrow widths) |
| **v0.6.36** | 2026-05-03 | NR036 — self-recursive struct without Box / & indirection halts
at parse-time instead of accept-then-runtime-stack-overflow
(probe finding closure — accept-then-crash class fix) |
| **v0.6.35** | 2026-05-03 | NR035 — explicit enum discriminants halt cleanly at parse-time
instead of segfaulting the compiler (probe finding closure —
CRITICAL crash-class fix) |
| **v0.6.34** | 2026-05-03 | `Result::unwrap_err()` implemented (probe finding fully closed
— gap 3) |
| **v0.6.33** | 2026-05-03 | `Option::unwrap()` + `Result::unwrap()` discriminant checks
(probe finding partial closure — gaps 1 + 2, including a CRITICAL
silent-miscompute fix) |
| **v0.6.32** | 2026-05-03 | ISR-007 — `#[isr(prio = N)]` validation (probe finding closure)
+ corrective rename of v0.6.31's placement check from ISR-004 to
ISR-008 (slot reserved for "Vector name not recognized") |
| **v0.6.31** | 2026-05-03 | `#[isr]` placement check at the attribute's own line (probe
finding partial closure) |
| **v0.6.30** | 2026-05-03 | Vec/array OOB diag reworded — Rust-canonical phrasing, no
implementation leak (probe finding partial closure) |
| **v0.6.29** | 2026-05-03 | Compiler perf fast-path + verify infra polish (parallel-1
consultant integration) |
| **v0.6.28** | 2026-05-02 | `(expr)(args)` direct-call form — three more inner-expression
shapes halt cleanly instead of silent miscompute (probe finding
closure, sister to v0.6.12) |
| **v0.6.27** | 2026-05-02 | Duplicate-decl halts at parse / collect time — TYP-039 dup field
in struct decl, TYP-040 dup enum variant, TYP-041 dup top-level
struct or enum (probe finding closures, sister halts) |
| **v0.6.26** | 2026-05-02 | `#[derive(...)]` — early `warning[DERIVE-001]` instead of silent
drop (probe finding closure) |
| **v0.6.25** | 2026-05-02 | `#[cfg(...)]` and `#[cfg_attr(...)]` attributes — clean parse-time
halt instead of silent-drop + duplicate-fn PANIC (probe finding
closure) |
| **v0.6.24** | 2026-05-02 | RFC-0035 Sendable + actor substrate (consultant lane G) — integrated
with main-agent perf fix |
| **v0.6.23** | 2026-05-02 | Method-ambiguity diagnostic — clean "ambiguous method dispatch"
text replaces misleading "duplicate pub fn name across modules"
(probe finding closure) |
| **v0.6.22** | 2026-05-02 | TYP-011 `str < str` workaround text now points at a workaround that
exists (probe finding closure) |
| **v0.6.21** | 2026-05-02 | `static` items at module scope — clean parse-time halt instead of
silent-drop + accept-then-clang-link-fail (probe finding closure) |
| **v0.6.20** | 2026-05-02 | TYP-011 — `assert_eq!` / `assert_ne!` on str arguments does pointer
comparison (CRITICAL probe-finding closure, main-agent compiler-only
ship) |
| **v0.6.19** | 2026-05-02 | RFC-0034 compile-time `[]` parameter syntax — first-pass parser
acceptance (consultant lane F; final round-3/4 spike integration) |
| **v0.6.18** | 2026-05-02 | NUM-021 — compile-time integer overflow diagnostic for module-level
const expressions (parallel-1 lane E; closes probe finding
`2026-05-01-const-overflow-not-caught-at-compile-time`) |
| **v0.6.15** | 2026-05-02 | `str_substring_strict` migration discoverability — docs + fixture
(parallel-1 lane D; closes spine §8.4 deferred item #8) |
| **v0.6.14** | 2026-05-02 | MATCH-014 — clean diagnostic for negative literal bounds in range
patterns (probe finding `2026-05-01-match-range-pattern-negative-
bound-parse-fail` closure, parallel-1 spike) |
| **v0.6.13** | 2026-05-02 | NVec inline-data audit (consultant lane B) — heap-corruption finding
closure beyond v0.5.32 |
| **v0.6.12** | 2026-05-02 | `(expr)(args)` direct-call form — clean halt instead of silent
miscompute (probe finding closure) |
| **v0.6.11** | 2026-05-02 | Generic-T trait-bound dispatch — `where T: Trait` form closure
(probe finding sister to v0.5.31 Track Y) |
| **v0.6.9** | 2026-05-02 | Track X round-3 lane A — RFC-0008 `#[isr]` interrupt service routine
attribute (consultant deliverable) |
| **v0.6.1** | 2026-05-02 | typ-026 closure — `expr;` at end of non-void fn body now diagnosed |
| **v0.6.0** | 2026-05-02 | v0.6.0 cut — RFC-0014 max_depth extensions + RFC-0033 effects-types
substrate, both consultant tracks integrated |

## v0.5.x

| Version | Date | Summary |
|---|---|---|
| **v0.5.33** | 2026-05-02 | Drift fix — `RELEASES.md` regen for v0.5.32 |
| **v0.5.32** | 2026-05-02 | Memory tighten — NVec small-buffer optimization + per-fn IR free during
emit + vec_insert_at inline-buffer fix |
| **v0.5.31** | 2026-05-02 | 🚀 Tools-only — perf fix: `cli_explain_full_smoke` and
`err_tests_have_expect_smoke` rewritten to eliminate per-iteration
fork explosion. Track Y merged |
| **v0.5.30** | 2026-05-02 | 🔧 Tools-only — wrapper robustness fix: summary row now writes on
every exit path; transient Get-Process errors no longer abort the
sampler |
| **v0.5.29** | 2026-05-02 | 🔧 Tools-only — full bisect-narrow protocol with run-to-run delta detection
and 1 GB hard e-stop |
| **v0.5.28** | 2026-05-01 | Canonical Rust unit-struct syntax `struct U;` now parses |
| **v0.5.27** | 2026-05-01 | 🐛 CRITICAL silent-miscompute fix: `(a < b) as i32` for f64
operands silently returned 0 |
| **v0.5.26** | 2026-05-01 | 🔧 Bisect-narrow protocol modes — `--rerun-failed` + `--only` |
| **v0.5.25** | 2026-05-01 | 🔧 async_await crash-class fix — handle validation registry |
| **v0.5.24** | 2026-05-01 | 🔧 Lexer: `1e20` (no fractional dot) now lexes as f64 |
| **v0.5.23** | 2026-05-01 | 🔧 Hex literal u64::MAX no longer wrongly-NUM-002 |
| **v0.5.22** | 2026-05-01 | 🔧 `eprintln!` + `eprint!` macros recognized |
| **v0.5.21** | 2026-05-01 | 🔬 Memory drift per-ship attribution |
| **v0.5.20** | 2026-05-01 | 🔧 String-literal escape: `\0` and `\r` now emit actual byte
values |
| **v0.5.19** | 2026-05-01 | 🔧 ASYNC-001 + per-agent CSV gitignore |
| **v0.5.18** | 2026-05-01 | 🔧 MATCH-001 dedup — non-exhaustive match no longer dual-emits
TYP-001 + MATCH-001 |
| **v0.5.17** | 2026-05-01 | 🔧 CSV agent-namespacing — 3 concurrent agents, no race |
| **v0.5.16** | 2026-05-01 | 🔬 Memory drift profile (Lane B1 from v0.5.14 deferred work) |
| **v0.5.15** | 2026-05-01 | 📝 `findings/_heartbeat.md` refreshed for v0.5.7 → v0.5.14
arc |
| **v0.5.14** | 2026-05-01 | 🔧 Memory-budget tighten — drift caught at v0.5.13 |
| **v0.5.13** | 2026-05-01 | 🔧 Generic-T propagation closure — call-site rtype inference now
substitutes gparam bindings |
| **v0.5.12** | 2026-05-01 | 🔧 `str_to_int_strict` opt-in strict variant |
| **v0.5.11** | 2026-05-01 | 🔧 FMT-003 — `format!()` extra-args silent-drop closure |
| **v0.5.10** | 2026-05-01 | 🔧 Numeric edge-case closure: `iN::MIN / -1` now panics
cleanly at narrow widths |
| **v0.5.9** | 2026-05-01 | 🔧 RFC-0014 closure (continued): `#[max_depth = N]` on assoc-fn
form now works |
| **v0.5.8** | 2026-05-01 | 📝 Probe-agent Q3 follow-through: `## Promoted` footer
backfilled across all older findings |
| **v0.5.7** | 2026-05-01 | 🔧 RFC-0014 closure: `#[max_depth = N]` on impl methods now
works |
| **v0.5.6** | 2026-05-01 | 🔧 Drift fix: rod_manifest.toml regen |
| **v0.5.5** | 2026-05-01 | 📝 `findings/_heartbeat.md` refreshed for v0.4.282 → v0.5.4
arc |
| **v0.5.4** | 2026-05-01 | 🔧 RFC-0007 closure: `atomic_swap_bool` + typed `atomic_swap`
ship |
| **v0.5.3** | 2026-05-01 | 📚 ML expansion crosswalk: v0.7.0 + v0.8.0 milestone trackers
enriched |
| **v0.5.2** | 2026-05-01 | 🔧 Drift fix: v0.5.1 retroactive CHANGELOG entry + helper
manifest regeneration |
| **v0.5.1** | 2026-05-01 | 📚 `docs/UPGRADE_v0.5.0.md` fully populated |
| **v0.5.0** | 2026-05-01 | 🎯 v0.5.0 — Production Robotics + RFC-0006 DbC + RFC-0007 atomics +
RFC-0014 max_depth + content-addressed cache |

## v0.4.x

| Version | Date | Summary |
|---|---|---|
| **v0.4.287** | 2026-05-01 | 📝 v0.6.0 milestone tracker — ML expansion Lanes E/F/G/I/J
substrate enrichment |
| **v0.4.286** | 2026-05-01 | 📝 v0.5.0 milestone tracker — ML expansion Lanes A/B/C/D/K
substrate enrichment |
| **v0.4.285** | 2026-05-01 | 📝 docs/UPGRADE_v0.5.0.md outline placeholder (retroactive
CHANGELOG entry, 2026-05-01) |
| **v0.4.284** | 2026-05-01 | 📝 RFC-0033 + RFC-0034 design drafts merged into
`docs/rfcs/` |
| **v0.4.283** | 2026-05-01 | 🛡️ RFC-0006 — undefined identifier in contract predicate
reject (CONTRACT-011) |
| **v0.4.282** | 2026-05-01 | 📋 Sync ship — sequencing doc + heartbeat refresh
(v0.4.279-281) |
| **v0.4.281** | 2026-05-01 | 🔧 RFC-0007 AtomicBool ordered ops shipped |
| **v0.4.280** | 2026-05-01 | 🚨 ATOMIC-006 — closure+atomic compiler-meltdown halt
(temporary) |
| **v0.4.279** | 2026-05-01 | 🛡️ `str_char_at_strict` opt-in bounds-check |
| **v0.4.278** | 2026-05-01 | 📋 Sync ship — sequencing doc + heartbeat refresh |
| **v0.4.277** | 2026-05-01 | 🛡️ RFC-0006 — `old(...)` in `#[require]` reject (CONTRACT-010) |
| **v0.4.276** | 2026-05-01 | 🧹 MATCH-012 panic-stutter fix |
| **v0.4.275** | 2026-05-01 | 🛡️ RFC-0006 — `NUCLEOR_DBC_MODE` value validation
(CONTRACT-009) |
| **v0.4.274** | 2026-05-01 | 🎯 Track H integration — RFC-0007 lock-free queues LIVE |
| **v0.4.273** | 2026-05-01 | 🎯 Track G integration — RFC-0007 atomics LIVE |
| **v0.4.272** | 2026-05-01 | 🛡️ RFC-0006 — `result` in void-fn `#[ensure]` reject
(CONTRACT-008) |
| **v0.4.271** | 2026-05-01 | 🛡️ RFC-0006 — `old(<heap-typed-expr>)` compile-time reject
(CONTRACT-006) |
| **v0.4.270** | 2026-05-01 | 📚 Doc audit — strict-mode default note refresh |
| **v0.4.269** | 2026-05-01 | Closes the trajectory rod's bits-ABI
surface (DMP / TOPP / Catmull-Rom / Bezier) that was
intentionally deferred from v0.4.264's motion-profile ship.
With this, the entire trajectory rod has f64-typed surface
parity for every fn whose i64 carries an f64 bit pattern.

### What lands

12 new wrappers at `stdlib/rods/trajectory.nr:262+`: |
| **v0.4.268** | 2026-05-01 | 📋 findings/ inbox audit + heartbeat refresh |
| **v0.4.267** | 2026-05-01 | Ninth and final natural-fit rod after
units → kinematics → linalg → csv_table → kdt → trajectory →
rrt → fk_chain. Adopters writing differentiable quantum-
simulation code now get f64-typed gate angles + gradient
loss + selective-backward threshold.

### What lands

3 new wrappers at `stdlib/rods/diff_sim.nr:78+`:
- `diff_gate_f64(h, gate_type, q0, q1, angle: f64)` (gate angle)
- `diff_backward_f64(h, grad_loss: f64)` (loss-gradient backprop)
- `diff_backward_selective_f64(h, grad_loss: f64, threshold: f64)`
  (selective backprop with importance threshold)

Bits-ABI fns above remain — purely additive.

### Scoping

- `mode_bits` in `diff_sim_init` is intentionally i64 — it's a
  packed bitfield (bit 0 = noisy, bit 1 = per-feature gradient,
  bit 2 = state-vector caching), not an f64. No wrapper.
- Pointer args (`vec`, `prior_vec`, `grad_input_vec`) and
  handle-typed bits-i64 returns (state-vector / probabilities /
  logits / gate-importance / export-prior) stay i64 — they're
  pointers to caller-allocated `double[]` buffers or vec
  handles, not f64 scalars.

### Fixture

`tests/features/diff_sim_f64.nr`: 2-qubit clean-mode simulator,
applies two RZ gates via `diff_gate_f64` (angles 0.5 + 1.0 rad),
runs `diff_backward_f64(1.0)`. Asserts `n_gates == 0` initially
then `== 2` after the gate calls.

### Validation

- Self-build clean, two-stage fixed-point at SHA
  `eb5c4d061f45cf04bedf1dfa42ef4627bf7669fd6fe013863d932a83bfcd2c7e`
  (= v0.4.258-266; 8th ship in a row at this SHA — compiler
  doesn't import diff_sim.nr).
- Drift gate clean — `rod_manifest.toml` regenerated to 2189
  stdlib fns (+3 from v0.4.266's 2186).
- Fixture exit 0, "OK diff_sim_f64".

### F64-arc CLOSE |
| **v0.4.266** | 2026-05-01 | **🛠️ stdlib/rods/fk_chain — `*_f64` ergonomic wrappers.** |
| **v0.4.265** | 2026-05-01 | **🛠️ stdlib/rods/rrt — `*_f64` ergonomic wrappers.** Seventh |
| **v0.4.264** | 2026-05-01 | Sixth rod in the v0.4.260→263 arc.
Adopters writing motion-planning code now get f64-typed
constructors and evaluators for the three named motion
profiles: quintic polynomial, trapezoidal velocity, and
S-curve (bounded-jerk).

### What lands

15 new wrappers at `stdlib/rods/trajectory.nr:97+`: |
| **v0.4.263** | 2026-05-01 | Fourth + fifth rods in the v0.4.260→262 arc.
Each module had a small enough f64-bits surface that bundling
them as one ship matches the unit (one feature per cycle)
without hitting trivial-ship cadence on the 2-wrapper csv_table.

### What lands |
| **v0.4.262** | 2026-05-01 | **🛠️ stdlib/rods/linalg — `*_f64` ergonomic wrappers.** |
| **v0.4.261** | 2026-05-01 | **🛠️ stdlib/rods/kinematics — `*_f64` ergonomic wrappers.** |
| **v0.4.260** | 2026-05-01 | 🛠️ stdlib/rods/units — `unit_convert_f64` ergonomic wrapper |
| **v0.4.259** | 2026-05-01 | v0.5 sequencing doc — RFC-0006 closeout v0.4.256→258 |
| **v0.4.258** | 2026-05-01 | 🎯 RFC-0006 `#[no_check]` per-fn opt-out LIVE |
| **v0.4.257** | 2026-05-01 | 🎯 RFC-0006 Liskov compile-time check LIVE — CONTRACT-004 +
CONTRACT-005 |
| **v0.4.256** | 2026-05-01 | v0.5 sequencing doc closure update |
| **v0.4.255** | 2026-05-01 | Adopter UPGRADE doc for the RFC-0006 DbC arc |
| **v0.4.254** | 2026-05-01 | RFC-0006 DbC — mid-body return multi-ensure support |
| **v0.4.253** | 2026-05-01 | 🎯 RFC-0006 DbC — `#[invariant(EXPR)]` constructor exit-emit
LIVE |
| **v0.4.252** | 2026-05-01 | 🎯 RFC-0006 DbC — `NUCLEOR_DBC_MODE` build-mode strip-out
LIVE |
| **v0.4.251** | 2026-05-01 | 🎯 RFC-0006 DbC — `old(expr)` snapshot in #[ensure]
postconditions LIVE |
| **v0.4.250** | 2026-05-01 | RFC-0006 DbC — multiple `#[require]` / `#[ensure]` attributes
per fn |
| **v0.4.249** | 2026-05-01 | RFC-0006 invariant — exit-emit on every `self` method |
| **v0.4.248** | 2026-05-01 | 🎯 RFC-0006 DbC — `#[invariant(EXPR)]` LIVE END-TO-END
(CONTRACT-003) |
| **v0.4.247** | 2026-05-01 | RFC-0006 ensure — mid-body explicit `return X;` support |
| **v0.4.246** | 2026-05-01 | 🎯 RFC-0006 DbC — `#[ensure(EXPR)]` LIVE END-TO-END |
| **v0.4.245** | 2026-04-30 | 🎯 RFC-0006 Design by Contract — `#[require(EXPR)]` LIVE
END-TO-END |
| **v0.4.244** | 2026-04-30 | RFC-0006 Design by Contract — substrate ship |
| **v0.4.243** | 2026-04-30 | v0.5 ship 7 closeout — OWN-001 use-after-move caret precision |
| **v0.4.242** | 2026-04-30 | Robustness ratchet — v0.4.239 wrap-block regression-lock + UPGRADE
note bump |
| **v0.4.241** | 2026-04-30 | v0.5 ship 7 substantive completion — call/cast/vec-read diag
spans |
| **v0.4.240** | 2026-04-30 | 🔧 LIVE BUG FIX — unsigned-overflow intrinsic dispatch |
| **v0.4.239** | 2026-04-30 | 🔧 LIVE BUG FIX — `wrapping { ... }` blocks no longer trap
under v0.4.238's strict-default-on |
| **v0.4.238** | 2026-04-30 | v0.5 ship 8 of 8 — phase 3e.3 strict-mode default FLIPPED |
| **v0.4.237** | 2026-04-30 | v0.5 ship 7 progress — struct-init diag site span migration |
| **v0.4.235** | 2026-04-30 | v0.5 Track E — narrow-intrinsic correctness validation +
explicit-mode precedence fix |
| **v0.4.234** | 2026-04-30 | v0.5 ship 4 of 8 — phase 3e.1 LLVM overflow-intrinsic substrate
(opt-in via `NUCLEOR_INT_STRICT_INTRIN=1`) |
| **v0.4.233** | 2026-04-30 | v0.5 atomic integration: 5-of-8 punchlist ships landed in one
verified ship |
| **v0.4.227** | 2026-04-30 | v0.5 residual sequencing doc |
| **v0.4.226** | 2026-04-30 | Doc-only ship — v0.4 residuals nested into v0.5 milestone with
explicit v0.4 rationale + v0.5 implementation path for each item |
| **v0.4.225** | 2026-04-30 | 🎯 Crash-class close — `?` operator on `Option<T>` now works
correctly |
| **v0.4.224** | 2026-04-30 | Phase 3c REVERTED + perf baseline updated for v0.4 closeout |
| **v0.4.223** | 2026-04-30 | Inbox closeout — both Option-`?` findings promoted with root-
cause analysis and v0.5 deferral note |
| **v0.4.222** | 2026-04-30 | 🎯 RFC-0015 phase 3 v0.4 closeout — comprehensive width-correctness
fixture lands; v0.4 milestone reflects reality |
| **v0.4.221** | 2026-04-30 | 🎯 RFC-0015 phase 3c LANDS — fn params with narrow declared types
now use typed allocas, body arith composes phase 3b narrow path |
| **v0.4.220** | 2026-04-30 | Silent-miscompute close — scientific notation literals (`1.0e30`,
`2.5e-3`) now lex correctly |
| **v0.4.219** | 2026-04-30 | Silent-miscompute close (partial) — `fn f() -> T { let _z = 5; }`
no longer silently returns 0; halts cleanly with TYP-026 |
| **v0.4.218** | 2026-04-30 | Wrong-error close — `match x { }` (empty match body) no longer
panics at parse time |
| **v0.4.217** | 2026-04-30 | Wrong-error close — method-style call on `&T` receiver no longer
reports `Vec<T>` as the receiver type |
| **v0.4.216** | 2026-04-30 | Wrong-error close — NUM-003 ("`as` cast loses precision") no
longer emits twice for the same cast in fn-call argument position |
| **v0.4.215** | 2026-04-30 | Wrong-error close — MATCH-002 ("unreachable arm after wildcard")
no longer false-fires on guarded var-binder arms |
| **v0.4.214** | 2026-04-30 | Wrong-error close — TYP-005 false-fire on closure-binding called
from inside another closure body now fixed |
| **v0.4.213** | 2026-04-30 | 🎯 Phase 3b COMPLETE for arithmetic — nested narrow expression
chains now emit width-correct iN ops end-to-end |
| **v0.4.212** | 2026-04-30 | RFC-0015 phase 3b extended — narrow-arith now covers `var + literal`
and `literal + var` patterns too |
| **v0.4.211** | 2026-04-30 | RFC-0015 phase 3b LANDS — narrow `add iN` instead of `add i64` +
narrow_via_as helper |
| **v0.4.210** | 2026-04-30 | RFC-0015 phase 3a-step-3 expanded — i32/u32 now use typed memory
ops too |
| **v0.4.209** | 2026-04-30 | 🎯 KEYSTONE — RFC-0015 phase 3a-step-3 SHIPS |
| **v0.4.208** | 2026-04-30 | Wrong-error CLEANUP — NR020 parse-error messages now use human-
readable token names instead of raw IDs |
| **v0.4.207** | 2026-04-30 | Silent-miscompute close — `fn add(a: i32, a: i32)` is now rejected
at parse time with NAM-001 instead of silently shadowing the first
parameter |
| **v0.4.206** | 2026-04-30 | COMPILER-MELTDOWN CLOSE — `match <f64> { 1.5 => ... }` no longer
ACCESS_VIOLATIONs the compiler |
| **v0.4.205** | 2026-04-30 | MEMORY-BLOW-UP CLOSE — `for x in v { v.push(...) }` no longer grows
unboundedly |
| **v0.4.204** | 2026-04-30 | Silent-miscompute close — `\|y\| { y + 7 }` (braced-body closure)
now returns 17 instead of 0 |
| **v0.4.203** | 2026-04-30 | TWO MORE silent-miscompute closes — `__tuple`/`__slice` and enum-
variant-with-payload pattern arms also had missing guard fall-through.
Plus regression fixture `tests/features/rfc0023_struct_pat_guard_fallthrough.nr`
locks in all 3 fixes |
| **v0.4.202** | 2026-04-30 | SILENT-MISCOMPUTE CLOSE: struct-destructure-with-guard now
correctly falls through. Found + fixed same-day |
| **v0.4.201** | 2026-04-30 | `examples/25_patterns_tour.nr` — single runnable program
exercising every RFC-0023 pattern feature. Plus: silent-miscompute
discovered in struct-destructure-with-guard, filed for probe agent |
| **v0.4.200** | 2026-04-30 | 🎉 v0.4.200 milestone — phase 3 plan doc updated with the v0.4.199
postmortem + revised step-3 design (Option D: AST-node width
attachment) |
| **v0.4.199** | 2026-04-30 | RFC-0015 phase 3a-step-3 wiring attempt — REVERTED for perf
budget violation. Postmortem + revised design captured here |
| **v0.4.198** | 2026-04-30 | RFC-0015 phase 3a regression-guard fixture
`tests/features/rfc0015_phase3a_widths.nr`. Verify 218 → 219 PASS |
| **v0.4.197** | 2026-04-30 | RFC-0015 phase 3a-step-2 — sext/trunc IR ops (substrate, unused).
Plus: i16/u8 silent-miscompute observation withdrawn (didn't repro) |
| **v0.4.196** | 2026-04-30 | RFC-0015 phase 3a foundation — typed IR ops + emit-time width
dispatch. Back-compat at byte-identical IR for current call sites |
| **v0.4.195** | 2026-04-30 | `docs/RFC-0015_PHASE_3_PLAN_2026-04-30.md` — keystone roadmap
for the 2 remaining v0.4 success-criteria boxes |
| **v0.4.194** | 2026-04-30 | `docs/v0.4_FEATURE_AUDIT_2026-04-30.md` — consolidated audit
reference for v0.4 shipped surface |
| **v0.4.193** | 2026-04-30 | RFC-0006 (Design by Contract) status — parser-passthrough today;
runtime enforcement is the v0.5.0 ship |
| **v0.4.192** | 2026-04-30 | `docs/language-tour.md` — added 5 new sections covering v0.4-era
features confirmed shipping during this session's status audits |
| **v0.4.191** | 2026-04-30 | RFC-0007 (atomics) status — partial. `AtomicI64` surface ships
in stdlib; `#[atomic]` attribute + ordering variants deferred |
| **v0.4.190** | 2026-04-30 | RFC-0014 (`#[max_depth = N]`) status updated — attribute parses,
opts out of RT-008 |
| **v0.4.189** | 2026-04-30 | RFC-0005 (units) + RFC-0011 (nuc-cxx) status updates — both
partial. Plus: f64 unit-conversion silent miscompute filed for
probe agent |
| **v0.4.188** | 2026-04-30 | RFC-0002 (allocator types) status — partial: basic `Box<T>`
ships, parameterized allocators genuinely deferred |
| **v0.4.187** | 2026-04-30 | RFC-0001 (real-time function attributes) status updated —
all four attributes shipped with diagnostic enforcement |
| **v0.4.186** | 2026-04-30 | RFC-0016 phase 4 (`From`/`Into` auto-conv in `?`) audited as
DONE. v0.4 DEFERRED-from-v0.2 row state updated |
| **v0.4.185** | 2026-04-30 | v0.4.0 success criteria — "All RFC-0023..0029 implemented to
definition-of-done" → DONE |
| **v0.4.184** | 2026-04-30 | RFC-0026 status updated — basic traits + impls work; `dyn Trait`
genuinely deferred |
| **v0.4.183** | 2026-04-30 | RFC-0025 status updated — closures with capture work end-to-end
(audit ship). Same pattern as v0.4.171 (RFC-0023) and v0.4.182
(RFC-0024) |
| **v0.4.182** | 2026-04-30 | RFC-0024 status doc updated to match shipped reality. Generic
functions, structs, and enums all work end-to-end |
| **v0.4.181** | 2026-04-30 | Operating-model revision: probe agent moves to a propose-via-PR
flow. Main agent owns integration + version + tag |
| **v0.4.180** | 2026-04-30 | `nuc help` doc-command help text + `compiler_version_label`
catch-up |
| **v0.4.179** | 2026-04-30 | TYP-011 ext: `for <var> in <struct>` rejected. Closes 3 silent-
miscompute findings (struct iteration was bit-walking storage) |
| **v0.4.178** | 2026-04-30 | Doc-only: bring verify-baseline references in
`PARALLEL_AGENT_PROBE_MANDATE.md` and `SELF_HANDOFF.md` up to date
(217 PASS / 1 baseline-FAIL) |
| **v0.4.177** | 2026-04-30 | `nuc explain MATCH-011` + `nuc explain MATCH-012` — close
explain-registry gap for codes shipped this session |
| **v0.4.176** | 2026-04-30 | RFC-0029 — `nuc doc` HTML mode now recognizes `#`-headings inside
`///` doc blocks. Pairs with v0.4.175's Markdown-side demotion |
| **v0.4.175** | 2026-04-30 | RFC-0029 — `nuc doc` demotes `#`-headings inside `///` doc blocks
by two levels in Markdown output |
| **v0.4.174** | 2026-04-30 | RFC-0029 — `nuc doc` cross-links param + return types to in-file
struct / enum anchors. Adds an anchored Types section |
| **v0.4.173** | 2026-04-30 | `nuc doc --test-list <file>` enumerates `nucleor` code blocks
from `///` comments — extraction half of doc tests (RFC-0029) |
| **v0.4.172** | 2026-04-30 | v0.4.0 milestone tracker — success-criteria audit + 1 box flipped |
| **v0.4.171** | 2026-04-30 | RFC-0023 + RFC-0029 status lines updated to match shipped reality |
| **v0.4.170** | 2026-04-30 | **`verify_parallel.sh` now skips `*_aux.nr` helper files. Verify |
| **v0.4.169** | 2026-04-30 | RFC-0023 regression-guard fixtures: pattern guards, @-bindings,
slice patterns, struct destructure + same-name enum or-patterns
locked into the verify suite. Verify count: 213 → 217 PASS |
| **v0.4.168** | 2026-04-30 | Migration story v0.2 → v0.4 — closes a v0.4 success-criteria
checkbox |
| **v0.4.167** | 2026-04-30 | RFC-0029 — `nuc doc` adds Struct index + Enum index |
| **v0.4.166** | 2026-04-30 | RFC-0029 — `nuc doc` recognizes module-level doc comments (`//!`) |
| **v0.4.165** | 2026-04-30 | RFC-0029 — `nuc doc` per-parameter + per-return rendering. Closes
the param-rendering deferral from the v0.2 doc-generator skeleton ship |
| **v0.4.164** | 2026-04-30 | TYP-014 over-tightening + TYP-016 misfire on passthrough blocks
closed; verify gate restored to 213 PASS / 2 baseline-FAIL |
| **v0.4.163** | 2026-04-30 | MATCH-012 (URGENT — compiler ACCESS VIOLATION close): struct
pattern with literal field test (`Point { x: 0, y: 0 } => ...`)
crashed the compiler with 0xC0000005 ACCESS VIOLATION mid-codegen.
Now: parser emits MATCH-012 with a remediation hint pointing at
guards |
| **v0.4.162** | 2026-04-29 | TYP-025: generic trait bounds enforced at call sites.
`add_values<T: Addable>(a: i64, b: i64)` halts when no
`impl Addable for i64` exists. Closes residual #1 |
| **v0.4.161** | 2026-04-29 | NUM-008 ext: shift amount via let-bound const now caught at
compile time. `let n: i64 = 100; let r = x << n;` halts.
Closes residual #7 |
| **v0.4.160** | 2026-04-29 | `tools/verify.sh` now folds the parallel fixture phase
internally via `xargs -P` worker pool. New `-j` / `--jobs` flag
(also `NUC_VERIFY_JOBS` env). All 385 expected fixture result
files produced; no missing-result-file gap. Closes residual #6 |
| **v0.4.159** | 2026-04-29 | Self-host perf: peak RSS 519 MB → 420 MB, wall 6.3s → 3.15s,
str_len calls 791M → 9.8M (~80× reduction). Verify wall 44s →
37s. Integration of `spike/v04-memory-peak-v2` (commits c49e9f4
"reduce compiler native-link peak RSS" + 015ff20 "balance
self-host memory and compile time") from the parallel-agent
workstream |
| **v0.4.158** | 2026-04-29 | TYP-008 cascade ext: kind-8 type_expr now returns the
receiver Vec type for clone/sort/reverse/map/filter/collect/
take/skip/chain/step_by. Pre-fix `let r: i64 =
v.clone().push(N);` silently bound r=0 because the outer push
saw recv_t="" (clone's catch-all return) and the v0.4.139
void-mutator route required Vec recv_t to fire |
| **v0.4.157** | 2026-04-29 | TYP-011 ext: `?` on a kind-3 (var-ref) receiver of non-Result/
Option type silently SIGSEGV'd. v0.4.54 fixed the kind-7 (call)
case; the var-ref case escaped. Now halts |
| **v0.4.156** | 2026-04-29 | TYP-021 ext: closure call returning void silently bound the
let target to 0. `let r = void_closure(args)` now halts |
| **v0.4.155** | 2026-04-29 | TYP-008 ext: `let mut x: T;` (mutable, no initializer) now
rejected. Closes the deferred-from-v0.4-session "uninit-mut
binding" item |
| **v0.4.154** | 2026-04-29 | OWN-001: String use-after-move on binding transfer.
`let s = String::new(); let t = s; s.len();` now warns OWN-001
(use of moved variable). Closes the deferred-from-v0.4-session
"use-after-move on String/Vec" item |
| **v0.4.153** | 2026-04-29 | ```nucleor
let max: i64 = 9223372036854775807;
let r: i64 = saturating { (max + 1) - 5 };
// pre-fix: r == wrap(max+1)-5 (wrong)
// now:     r == max - 5         (correct — sat clamp on add)
```

Integration of `spike/v04-saturating-per-op-v2` (commit
`eea4f2f`) from the parallel agent's residual-#3 workstream.

The v0.4.105 design lowered the entire saturating block as
plain i64 arithmetic and clamped only the block's final value
through `__nucleor_sat_i32`. For multi-step expressions where
inner operations could overflow i64 — or where sat-then-unsat
semantics were intended (e.g. `saturating { (max + 1) - 5 }`
should produce `max - 5`, not `min - 4`) — the inner wrap broke
the contract. The spike re-routes the dispatch:

- |
| **v0.4.152** | 2026-04-29 | TYP-011 ext: `for c in <str/String/scalar/bool>` silently
produced 0 iterations. The loop body never ran and the build
emitted nothing. Now halts |
| **v0.4.151** | 2026-04-29 | TYP-011 ext: `String == String` / `String != String` silently
did pointer comparison. Two equal-bytes Strings at different
heap addresses returned FALSE. Now halts |
| **v0.4.150** | 2026-04-29 | TYP-011 ext: `String + <scalar>` (or any String arith) silently
did i64_add on the String heap pointer, producing a wild
pointer. Now halts. Plus kind-12 type_expr now propagates
String/Vec/HashMap/etc. types from constructor calls |
| **v0.4.149** | 2026-04-29 | TYP-006 ext: `print(<non_str>)` / `println(<non_str>)` /
`eprint(<non_str>)` / `eprintln(<non_str>)` silently passed any
i64 to the C printf-style helper, printing garbage bytes
(or SIGSEGVing on bad pointers). Now halts |
| **v0.4.148** | 2026-04-29 | TYP-022 ext: struct name called like a fn (`P(42)`) silently
emitted `call @P` and failed late at clang link. Now halts at
type-check with the right hint |
| **v0.4.147** | 2026-04-29 | TYP-011 ext: `struct == struct` / `!= ` / `<` etc. silently
did pointer comparison. Two equal-field structs at different
addresses returned FALSE. Now halts |
| **v0.4.146** | 2026-04-29 | TYP-005 ext: closure called with wrong argc silently lowered
to an indirect call with garbage params. `let f = \|x: i64, y:
i64\| x + y; f(5);` silently passed and ran with the second
param undefined. Now halts |
| **v0.4.145** | 2026-04-29 | TYP-024: if-expression branches with incompatible types
silently picked the then-branch type and bit-cast the else
value into that slot. Now halts |
| **v0.4.144** | 2026-04-29 | TYP-005 ext: nested fn call inside a runtime-helper arg
escaped every diagnostic. `print_int(add(5))` for `fn add(a:
i64, b: i64)` silently lowered with garbage. Now halts |
| **v0.4.143** | 2026-04-29 | NUM-023 (URGENT — runtime SIGSEGV close): float / bool
as-cast to `str` silently produced an invalid str pointer.
`let s: str = x as str` for `x: f64` SIGSEGV'd on print.
Now halts |
| **v0.4.142** | 2026-04-29 | TYP-010 ext: `return <expr>;` in void / implicit-void fn
silently discarded the value at codegen.
`fn f() { return 42; }` — value vanished, fn appeared to
return cleanly. Now halts |
| **v0.4.141** | 2026-04-29 | NUM-022 ext: int vs float in arithmetic binop produced
meaningless i64 garbage. `5 + 5.0` returned
4617315517961601024. Now halts cleanly |
| **v0.4.140** | 2026-04-29 | MATCH-011 (URGENT — runtime SIGSEGV close): heterogeneous
match arm literal types silently lowered to a runtime crash.
`match n: i64 { 1 => ..., "a" => ..., _ => ... }` for n=2
SIGSEGV'd. Now halts at type-check |
| **v0.4.139** | 2026-04-29 | TYP-023: chained method on void-returning receiver silently
returned 0 / wrong bool / garbage. `v.push(30).len()` returned
0; `let x = v.push(20)` bound x=0. Now halts cleanly |
| **v0.4.138** | 2026-04-29 | TYP-022: bare struct name in value position lowered to a
nonsense `ptrtoint @<Name>` and failed late at clang link with
`undefined value '@<Name>'`. Now halts cleanly with the right
hint |
| **v0.4.137** | 2026-04-29 | NUM-022: int vs float in comparison binop silently
bit-pattern-compared. `5 == 5.0` returned FALSE; `5 < 5.0`
returned TRUE. Now halts cleanly |
| **v0.4.136** | 2026-04-29 | TYP-021: bare-let RHS evaluating to `void` silently bound as 0
— sister to v0.4.132 (binop void). Now halts cleanly |
| **v0.4.135** | 2026-04-29 | **str-literal method dispatch: `"abc".len()` silently returned 0 |
| **v0.4.134** | 2026-04-29 | MATCH-001 ext: `match b { true => 1 }` on a bool was a wildcard-
binding silent miscompute (matched ANY value, returned 1 for
b=false too) |
| **v0.4.133** | 2026-04-29 | TYP-008 ext: int literal in bool struct-field init silently stored
truthy garbage — now halts cleanly |
| **v0.4.132** | 2026-04-29 | TYP-020: void-typed expression in arithmetic/comparison binop
silently coerced to 0 — now halts cleanly |
| **v0.4.131** | 2026-04-29 | TYP-018 sister fix: assignment to unknown struct field
(`p.y = 99`) halts at type-check |
| **v0.4.130** | 2026-04-29 | TYP-019: trait-bound NAME verification — typo'd or undefined
trait names in `<T: Trait>` bounds halt cleanly (closes v0.4
residual #1 / audit doc-#1 §8 partial) |
| **v0.4.129** | 2026-04-29 | TYP-018: unknown field access (`p.y` where struct has no `y`)
halts at type-check instead of runtime "vec_get OOB index -1" |
| **v0.4.128** | 2026-04-29 | TYP-017: duplicate field name in struct init silently took the
first occurrence — now halts cleanly |
| **v0.4.127** | 2026-04-29 | TYP-005 lift now detects synthetic `vec_<method>` names from
the method-dispatch catch-all and shows the source-side method
name + supported Vec method list |
| **v0.4.126** | 2026-04-29 | TYP-016: `if cond { val }` with no else branch assigned to a
non-void binding now halts with clean diagnostic instead of
silently lowering to alloca-zero-init |
| **v0.4.125** | 2026-04-29 | Extended Vec/iterator surface — 5 new methods (closes v0.4
residual #5) |
| **v0.4.124** | 2026-04-29 | Extended `String` heap-string surface — 10 new methods (closes
v0.4 residual #4) |
| **v0.4.123** | 2026-04-29 | TYP-015: struct-field-as-method-call (`p.x(42)` where `x` is
a field) halts at codegen with the right hint instead of
surfacing a confusing `vec_x()` TYP-005 |
| **v0.4.122** | 2026-04-29 | TYP-014: calling a non-callable typed binding (`s(args)` for
`s: str`) silently SIGSEGV'd; now halts at codegen with clear
diagnostic |
| **v0.4.121** | 2026-04-29 | Two silent miscompute closes in one tick: bare `print()` no
longer SIGSEGVs at runtime; unknown string escapes (`"a\zb"`)
no longer silently drop the backslash |
| **v0.4.120** | 2026-04-29 | Rich match patterns: enum or-patterns with bindings, `@` range
bindings, struct destructure, and Vec-backed tuple/slice patterns
(parallel-agent spike integrated on 2nd pass; closes audit
doc-#1 §5) |
| **v0.4.119** | 2026-04-29 | NUM-021: decimal literal beyond u64::MAX silently wrapped to
garbage — now panics at lex time |
| **v0.4.118** | 2026-04-29 | `Box<dyn Trait>` binding/call coercion now type-checks when the
concrete type has a matching trait impl (parallel-agent spike
integrated on 2nd pass; closes audit doc-#1 §9) |
| **v0.4.117** | 2026-04-29 | TYP-011 str-indexing guard moved to codegen side — catches
`print_int(s[0])`, `s[0] as i64`, and every other context where
`s[i]` was previously slipping past the type-check guard |
| **v0.4.116** | 2026-04-29 | FnMut-style direct closure capture writeback (parallel-agent
spike integrated; closes audit doc-#1 §7) |
| **v0.4.115** | 2026-04-29 | RFC-0016 §3.7: `?` operator now invokes `From::from` for error
conversion; generic `impl Trait<Arg> for Type` headers parse +
dispatch (parallel-agent spike integrated; closes audit doc-#1
§3 / RFC-0024 Phase 5 / v0.2 deferred row #5) |
| **v0.4.114** | 2026-04-29 | `String` heap-string type lands; `string_basic.nr` restored to
active gate (closes the last `_unimplemented/` fixture) |
| **v0.4.113** | 2026-04-29 | NUM-020: integer-typed expression in float binding silent
miscompute closed |
| **v0.4.112** | 2026-04-29 | Three control-flow silent fall-throughs closed: `break`/`continue`
outside loop, bare `return;` in non-void fn |
| **v0.4.111** | 2026-04-29 | Chained assignment `a = b = c` rejected at parse time (silent
miscompute close) |
| **v0.4.110** | 2026-04-29 | No-main-fn link error lifted to clean NR022 diagnostic |
| **v0.4.109** | 2026-04-29 | Int/str match without `_` arm now halts as MATCH-001 (closes
silent-fall-through-to-0 sister hazard) |
| **v0.4.108** | 2026-04-29 | Tail-expression return-type check closes a SIGSEGV-class silent
miscompute |
| **v0.4.107** | 2026-04-29 | **Primitive-receiver method guard: `5.no_such_method()` halts with |
| **v0.4.106** | 2026-04-29 | Undefined fn-call link error lifted to clean TYP-005 diagnostic
(closes deferral doc-#1 §_undef-fn-call, ML_SUITE queue
deferred row #1) |
| **v0.4.105** | 2026-04-29 | `saturating { ... }` block lands; overflow_saturating.nr +
overflow_comprehensive.nr restored to active gate (audit doc-#1
§3a closes) |
| **v0.4.104** | 2026-04-29 | Concurrency-safe compiler cache: per-output `clang_link.log`;
parallel verify harness lands at 9.13× speedup |
| **v0.4.103** | 2026-04-29 | Trait-bound clauses `<T: Addable>` parse-skip; `trait_bounds.nr`
restored to active gate (audit doc-#1 §8 parse-side) |
| **v0.4.102** | 2026-04-29 | `wrapping { ... }` block restored as passthrough; `saturating { ... }`
halts cleanly with NR021 (audit doc-#1 §3a partial) |
| **v0.4.101** | 2026-04-29 | `Vec.chain(other)` runtime helper added; `iter_combinators.nr`
restored to active gate (audit doc-#1 §6 partial) |
| **v0.4.100** | 2026-04-29 | 13 `_unimplemented/` feature fixtures restored to active gate |
| **v0.4.99** | 2026-04-29 | parse_primary fall-through narrowed (audit doc-#1 §_p — closes
the long-deferred v0.4.72 / v0.4.81 attempts) |
| **v0.4.98** | 2026-04-29 | Vec debug element-type dispatch — `Vec<str>` and `Vec<Option<i64>>`
print correctly (extends v0.4.97 §10b) |
| **v0.4.97** | 2026-04-29 | Recursive Debug for `Vec<i64>` / `Option<i64>` / `Result<i64,i64>`
(audit doc-#1 §10b — extends v0.4.96 §10) |
| **v0.4.96** | 2026-04-29 | Display / Debug for structs (audit doc-#1 §10) — `println!("{}", p)`
on a struct with `impl Display` actually prints the struct now,
instead of emitting a pointer integer |
| **v0.4.95** | 2026-04-29 | Variable-divisor div/mod by zero now panics with clean message
(was silent SIGFPE / exit 127) |
| **v0.4.94** | 2026-04-29 | TYP-011 close — `s[i]` on `str` halts (was silent vec_get on
str pointer → OOB panic / garbage) |
| **v0.4.93** | 2026-04-29 | `Result.or_else(f)` recovery method |
| **v0.4.92** | 2026-04-29 | Option/Result fn-arg methods: `.map(f)`, `.and_then(f)`,
`.unwrap_or_else(f)` for both Option and Result |
| **v0.4.91** | 2026-04-29 | Mirror v0.4.90 Option/Result dispatch into tools_suite |
| **v0.4.90** | 2026-04-29 | CRITICAL: Option<T> / Result<T,E> method dispatch restored.
Plus verify-count display fix |
| **v0.4.89** | 2026-04-29 | Extends v0.4.88's str dispatch to 7 more methods:
`to_lower`, `to_upper`, `trim`, `trim_start`, `trim_end`,
`substring`, `char_at` |
| **v0.4.88** | 2026-04-29 | **str method dispatch fix — `s.len/contains/replace/split/ |
| **v0.4.87** | 2026-04-29 | Vec method dispatch-name drift fix — `v.insert(idx, val)` and
`v.remove(idx)` now route to the correct runtime helpers |
| **v0.4.86** | 2026-04-29 | TYP-008 ext — Vec<T>.set(idx, literal) and .insert(idx, literal)
wrong-type halts (extends v0.4.85 to elem-arg-at-position-1
methods) |
| **v0.4.85** | 2026-04-29 | TYP-008 ext — Vec<T>.push(literal) wrong-type halts (was silent
str-ptr-as-i64-cell miscompute) |
| **v0.4.84** | 2026-04-29 | TYP-008 ext — struct-field type mismatch on literal RHS halts
(was silent ptr-as-i64 / bit-cast miscompute) |
| **v0.4.83** | 2026-04-29 | TYP-008 ext — immutable `let` binding without initializer halts
(was silent zero-read miscompute) |
| **v0.4.82** | 2026-04-29 | `nucleor_tools_suite.nr` `expect_tok` NR020 mirror — same close
as s1 v0.4.78. Last big silent-fall-through site in the sister
compiler's parser path |
| **v0.4.81** | 2026-04-29 | 4 silent fall-throughs in `nucleor_tools_suite.nr` mirrored from
the s1 v0.4.34/v0.4.36 closes (sister-compiler drift cleanup) |
| **v0.4.80** | 2026-04-29 | Two `print("WARNING: …")` runtime-SIGSEGV sites promoted to panic.
Continues the v0.4.78/79 silent-fall-through closure at the call-
expression type-check pass |
| **v0.4.79** | 2026-04-29 | Four parser silent fall-throughs closed (batch). Same class as
v0.4.78 NR020 but at specific recovery sites that printed an
"ERROR:" diagnostic and silently continued past the bad input |
| **v0.4.78** | 2026-04-29 | NR020 — parse errors halt the build (was print-and-recover →
silently broken binary) |
| **v0.4.77** | 2026-04-29 | NUM-019 — negative literal in unsigned binding halts at compile
time (audit doc-#1 §1 numeric-soundness extension, batched series
with v0.4.74/75/76) |
| **v0.4.76** | 2026-04-29 | Two silent-coerce miscomputes closed: NUM-018 (float literal in
integer binding) and TYP-002 extension (unary `!` on non-bool).
Batch ship — both root-cause from over-permissive types_compatible |
| **v0.4.75** | 2026-04-29 | NUM-008 — shift amount out of range for i64 halts at compile time
(audit doc-#1 §1 numeric-soundness extension, batch with v0.4.74) |
| **v0.4.74** | 2026-04-29 | NUM-009 — division/remainder by literal zero halts at compile time
(audit doc-#1 §1 numeric-soundness extension) |
| **v0.4.73** | 2026-04-29 | Generic Option<T> / Result<T,E> / Vec<T> type propagation
(audit S2 keystone close) |
| **v0.4.72** | 2026-04-28 | `str_from_int(i32)` signature was lying — added `str_from_i64(i64)`
as truthful internal entry, kept `str_from_int` as wrapper |
| **v0.4.71** | 2026-04-28 | bool with bitwise/shift ops (`a & x`, `a \| x`, `a ^ x`, `a << x`,
`a >> x`) — TYP-002 close |
| **v0.4.70** | 2026-04-28 | Audit-derived batch close: NUM-002 promoted to error + format
placeholder/arg-count mismatch halts at preprocess |
| **v0.4.69** | 2026-04-28 | `=` vs `==` typo guard in `while` / `if` conditions |
| **v0.4.68** | 2026-04-28 | Vec<T> ordering ops `<`, `<=`, `>`, `>=` — TYP-011 ptr-compare close |
| **v0.4.67** | 2026-04-28 | str ordering ops `<`, `<=`, `>`, `>=` — TYP-011 pointer-compare close |
| **v0.4.66** | 2026-04-28 | Mixed str/int arithmetic — TYP-011 close (also covers `+=` desugar) |
| **v0.4.65** | 2026-04-28 | Struct field assignment type mismatch — TYP-009 close |
| **v0.4.64** | 2026-04-28 | Indexed assignment type mismatch — TYP-009 close |
| **v0.4.63** | 2026-04-28 | Struct init extra-field silent-drop — TYP-013 hard error |
| **v0.4.62** | 2026-04-28 | Struct init missing-field silent-default-zero — TYP-012 hard error |
| **v0.4.61** | 2026-04-28 | Vec<T> arithmetic + ==/!= silent-miscompute close (closes deferred
item #1 from the queue — final deferred item resolved this cycle) |
| **v0.4.60** | 2026-04-28 | Undefined-fn-call surfaces at type-check time as warning-tier
TYP-005 (closes deferred item #2 from the queue) |
| **v0.4.59** | 2026-04-28 | MATCH-001 now fires for match-as-expression (closes the v0.4.56
follow-on; deferred item #306 from the queue resolved) |
| **v0.4.58** | 2026-04-28 | **`str - str` / `str * str` / `str / str` / `str % str` no longer |
| **v0.4.56** | 2026-04-28 | Non-exhaustive match (statement form) on enum now halts at compile
time with MATCH-001 instead of silently returning 0 |
| **v0.4.55** | 2026-04-28 | Slice expressions `expr[lo..hi]` halt at compile time with clear
diagnostic instead of silently SIGSEGVing at runtime |
| **v0.4.54** | 2026-04-28 | `?` operator on a non-Option/Result receiver no longer silently
SIGSEGVs at runtime — TYP-011 hard error at compile time |
| **v0.4.53** | 2026-04-28 | `x.unwrap()` (and 11 other Option/Result methods) on a non-Option
receiver no longer fails late at clang link with `undefined value
'@vec_unwrap'` — compiler now halts at dispatch with a clear hint |
| **v0.4.52** | 2026-04-28 | `str == str` and `str != str` no longer silently miscompute —
TYP-011 hard error pointing at `str_eq()` |
| **v0.4.51** | 2026-04-28 | `str + str` no longer silently SIGSEGVs at runtime — TYP-011
hard error at compile time pointing at `str_concat()` |
| **v0.4.50** | 2026-04-28 | `if let Some(...) = v.first()` (and .last/.pop, vec_first/last/pop)
no longer silently SEGFAULTs at runtime — parse-time guard halts
the build with a clear workaround hint |
| **v0.4.49** | 2026-04-28 | RFC-0023 partial: int-literal + wildcard or-patterns
(`A \| B \| C => body`) — silent miscompute closed |
| **v0.4.48** | 2026-04-28 | NUC-FEEDBACK-002 silent-miscompute guard extended to the fn-call
form: `vec_get(v, i) as f32` (and `vec_first` / `vec_last` /
`vec_pop`) on a `Vec<NarrowFloat>` now also emits NUM-006 |
| **v0.4.47** | 2026-04-28 | NUC-FEEDBACK-002 silent-miscompute guard: `v[i] as <NarrowFloat>`
on a `Vec<NarrowFloat>` is now a hard error, not a quiet wrong
result |
| **v0.4.46** | 2026-04-28 | RFC-0024 Phase 2 follow-up: type-compat bridge tolerates the new
`Option<T>`/`Result<T>` shapes |
| **v0.4.45** | 2026-04-28 | RFC-0024 Phase 2: constructor type propagation for `Some(x)` and `Ok(x)` |
| **v0.4.44** | 2026-04-28 | RFC-0024 Phase 2 foundation: `type_first_arg(t)` helper |
| **v0.4.43** | 2026-04-28 | Span migration: 4 line-only diagnostic call sites → line+col spans |
| **v0.4.41** | 2026-04-28 | `{:?}` Debug formatter — Rust-style string quoting |
| **v0.4.40** | 2026-04-28 | Custom fill char pinned — closes RFC-0028 phase 5 for the
adopter-everyday set |
| **v0.4.39** | 2026-04-28 | Verify gate: cli_explain step parallelized via `xargs -P 2` |
| **v0.4.38** | 2026-04-28 | RFC-0028 phase 5: `{:e}` / `{:E}` scientific notation for floats |
| **v0.4.37** | 2026-04-28 | RFC-0028 phase 5 cosmetic close: `{:X}` now produces upper-case
hex digits |
| **v0.4.36** | 2026-04-28 | `Foo { x: 1 }` where `Foo` is undeclared now panics at compiler
level instead of producing cryptic `%r.-1` LLVM IR |
| **v0.4.35** | 2026-04-28 | Silent-miscompute close: bare `print(a, b)` multi-arg dropped extras silently |
| **v0.4.34** | 2026-04-28 | Two compiler-internal safety nets promoted to hard panic |
| **v0.4.33** | 2026-04-28 | Three more silent-fall-through closes — parser-level "not yet
supported" sites |
| **v0.4.32** | 2026-04-28 | Two more silent-miscompute closes |
| **v0.4.31** | 2026-04-28 | Silent-miscompute close: unsupported `Type::method(...)` calls
panic instead of lowering to literal 0 |
| **v0.4.30** | 2026-04-28 | RFC-0028 phase 5: `{:+}` force-sign on integers |
| **v0.4.29** | 2026-04-28 | RFC-0028 phase 5: radix specs (`{:x}` / `{:X}` / `{:o}` / `{:b}`) +
alternate form (`{:#x}` / `{:#o}` / `{:#b}`) + combination with width
and align |
| **v0.4.28** | 2026-04-28 | RFC-0028 phase 5: width / align / zero-pad / combination spec dispatch |
| **v0.4.27** | 2026-04-28 | RFC-0028 phase 5: `{:.N}` precision spec for floats actually rounds |
| **v0.4.26** | 2026-04-28 | Memory budget rebaselined to current reality |
| **v0.4.25** | 2026-04-28 | 4th silent miscompute closed: `&mut <scalar>` parameter no longer compiles a no-op binary |
| **v0.4.24** | 2026-04-28 | Three silent miscomputes closed (NUC-FEEDBACK-002 + audit follow-ups) |
| **v0.4.23** | 2026-04-28 | RFC-NRT-005: dead-code elimination at the rod-import boundary |
| **v0.4.22** | 2026-04-27 | SUB-5s COLD COMPILE — `str_char_at` inlined as GEP+load |
| **v0.4.21** | 2026-04-27 | Phase B cache-wiring attempt: reverted (net-zero perf gain) |
| **v0.4.20** | 2026-04-27 | Phase A: generic str-keyed source-cache helpers (sub-5s perf
prep, dormant) |
| **v0.4.19** | 2026-04-27 | Cold compile time: bounded source-text scans (modest 5% gain) |
| **v0.4.18** | 2026-04-27 | Format-macro dispatch for user-defined enum variant bindings +
shadow-aware fallback when let-scan returns a struct/enum |
| **v0.4.17** | 2026-04-27 | Hazard-sweep regression pinning — 20 adopter patterns locked into
the verify gate |
| **v0.4.16** | 2026-04-27 | Two more hazard-sweep finds: `mut` struct field qualifier +
nested `format!()` type-src threading |
| **v0.4.15** | 2026-04-27 | Format-macro dispatch for builtin container getters
(`vec_get`/`hashmap_get`/`btreemap_get`/etc) |
| **v0.4.14** | 2026-04-27 | Scope-local scrutinee inference + multi-occurrence conflict
detection (closes 3/4 of a hazard-sweep probe) |
| **v0.4.13** | 2026-04-27 | Format-macro field-access dispatch on pattern bindings — closes
the println side of v0.4.11/4.12 |
| **v0.4.12** | 2026-04-27 | Field-access scrutinee for `match s.field { Some(g) => ... }`
(closes Translate's exact PipelineSuccess.gap_report shape) |
| **v0.4.11** | 2026-04-27 | Phase B — generic Option<T> / Result<T,E> propagation via the
compile-src side table (closes Translate's last actively-shaping
defect for bare-ident scrutinees) |
| **v0.4.10** | 2026-04-27 | SLSA-Build-Level-3 unblocker — `-Wl,/Brepro` for byte-reproducible
PE/COFF + Phase A compile-source side table + verify-reproducible
EXE comparison |
| **v0.4.9** | 2026-04-27 | Format-macro pattern-binding inference for `Some(s)` / `Ok(s)` /
`Err(e)` (closes Translate SPEC-1.5/1.6 macro-path wishlist) |
| **v0.4.8** | 2026-04-27 | RFC-NRT-002 v1 — `nuc tools install / uninstall / list / home`
subcommand (direct-URL install, no central registry yet) |
| **v0.4.7** | 2026-04-27 | RFC-NRT-003 — `nuc verify-reproducible` subcommand (Translate
SPEC-2 SLSA-Build-Level-3 unblocker, partial) |
| **v0.4.6** | 2026-04-27 | **Diagnostic: `extern fn __nucleor_*` declarations that conflict with |
| **v0.4.5** | 2026-04-27 | RFC-NRT-001 — `.nucprov` PE/ELF section (Nucleor_Translate SPEC-2
unblocker) |
| **v0.4.4** | 2026-04-27 | Option<str> Some-payload binding type propagation (SPEC-1.5
wishlist, partial — non-macro path) |
| **v0.4.3** | 2026-04-27 | Hash-backed sym_get — warm-cache redesign (closes the 0.3 carryover) |
| **v0.4.2** | 2026-04-27 | RFC-NRT-004 §G — last open Nucleor_Translate ask closed |
| **v0.4.1** | 2026-04-27 | RFC-NRT-004 §F + §D + §H + §E + investigation of §B/§C/§I/§D-followon/§G
(Nucleor_Translate consolidated working doc) |
| **v0.4.0** | 2026-04-27 | 0.4.0 cut — 0.3 closeout milestone marker |

## v0.3.x

| Version | Date | Summary |
|---|---|---|
| **v0.3.237** | 2026-04-27 | Hash-backed sym_get refactor — Phase B prototyped + reverted (honest
result; Phase A infrastructure stays dormant for redesigned 0.4.x) |
| **v0.3.236** | 2026-04-27 | Hash-backed sym_get refactor — Phase A (dormant infrastructure) |
| **v0.3.235** | 2026-04-27 | RFC-NRT-004 §B + §C (Nucleor_Translate) regression-pin: multi-payload
recursive enum binding USE + multi-multi-payload variant dispatch |
| **v0.3.234** | 2026-04-27 | 0.3 closeout series — runtime safety hardening (all hazards I had
flagged as remaining for 0.3 are now closed) |
| **v0.3.232** | 2026-04-27 | NUC-FEEDBACK-011 (ML_Suite): split out special-function
wrappers so importing `math_typed.nr` doesn't require a
v0.3.230+ compiler |
| **v0.3.231** | 2026-04-27 | RFC-NRT-004 (Nucleor_Translate) regression-pin: recursive
enum payload extraction in `match` arms |
| **v0.3.230** | 2026-04-27 | NUC-IMPROVE-007: typed special functions for exact SciPy
stats p-values |
| **v0.3.229** | 2026-04-27 | `hashmap_with_capacity` + `hashmap_grow` overflow PANICs |
| **v0.3.228** | 2026-04-26 | `nuc_t3_new_nd` safety: NULL/empty shape PANICs + dim
overflow check |
| **v0.3.227** | 2026-04-26 | `vec_percentile_f64` NaN-input UB fix |
| **v0.3.226** | 2026-04-26 | Tensor reduction safety: empty-input + null-handle PANICs +
matmul shape validation |
| **v0.3.225** | 2026-04-26 | Two more pattern-extension fixes from earlier hazard family
audits |
| **v0.3.224** | 2026-04-26 | Third instance of the v0.3.218 ftell-CVE pattern fixed in
`__nucleor_toml_parse_file` |
| **v0.3.223** | 2026-04-26 | Three CVE-class fixes + perf/memory monitor wired into every
verify |
| **v0.3.222** | 2026-04-26 | `sym_get` most-recent-entry fast path; hash-table refactor
attempted and reverted as too invasive |
| **v0.3.221** | 2026-04-26 | Perf regression monitor + own_put early-exit + perf-pattern memo |
| **v0.3.220** | 2026-04-26 | 13x compile-time perf recovery: cold self-build 81.5s → 6.3s |
| **v0.3.218** | 2026-04-26 | File I/O safety: buffer-overflow CVE fix +
strict-variant + write-error PANIC |
| **v0.3.217** | 2026-04-26 | `str_to_int` / `str_to_i64` on overflowing input now PANICs
instead of silently returning a wrapped or clamped value |
| **v0.3.216** | 2026-04-26 | `f32::NAN as i64` (and friends) now returns 0 (Rust `as`
semantic) instead of being undefined behavior |
| **v0.3.215** | 2026-04-26 | OOM PANIC instead of silent NULL → segfault |
| **v0.3.214** | 2026-04-26 | Shift-overflow now PANICs |
| **v0.3.213** | 2026-04-26 | Unary minus `-x` for i64 now panics on overflow |
| **v0.3.212** | 2026-04-26 | `NUCLEOR_PROFILE=1` runtime helper hot-spot detector |
| **v0.3.211** | 2026-04-26 | `f64 as i64` / `f64 as u64` / `f32 as i64` / `f32 as u64`
now numeric-truncate (Rust `as` semantics) |
| **v0.3.210** | 2026-04-26 | `str_char_at` negative-index PANIC + documented residual |
| **v0.3.209** | 2026-04-26 | f8e4m3 / f8e5m2 pack: round-to-nearest-even + NaN/inf
preservation |
| **v0.3.208** | 2026-04-26 | **Default `+`/`-`/`*`/`/`/`%` is now panic-on-overflow.** This |
| **v0.3.207** | 2026-04-26 | Ergonomic strict-arithmetic surface: `panic_add` / `panic_sub`
/ `panic_mul` / `panic_div` / `panic_rem` / `panic_neg` |
| **v0.3.206** | 2026-04-26 | NUC-FEEDBACK-002 fully closed |
| **v0.3.205** | 2026-04-26 | `str_substring` bounds-check + PANIC on OOB |
| **v0.3.204** | 2026-04-26 | `Vec<f32>` works end-to-end + missing `f32_to_str` helper |
| **v0.3.203** | 2026-04-26 | Tensor 2D and 3D accessors now bounds-check + PANIC on OOB |
| **v0.3.202** | 2026-04-26 | HashMap/BTreeMap missing-key access now PANICs by default |
| **v0.3.201** | 2026-04-26 | Vec OOB panic class extended |
| **v0.3.200** | 2026-04-26 | Vec OOB now PANICs by default with index/len in the
message |
| **v0.3.199** | 2026-04-26 | Locked-output retry + path-specific cleanup hint |
| **v0.3.198** | 2026-04-26 | Pre-IR halt for unresolved field access |
| **v0.3.197** | 2026-04-26 | `let k: f64 = 0.7978845608028654;` no longer silently
truncates to `0.797884` |
| **v0.3.196** | 2026-04-26 | `print(match x { ... })` and `print({ ... block ... })` no
longer SIGSEGV |
| **v0.3.195** | 2026-04-26 | `println!("{}", if cond { f } else { 0.0 })` for f64 if-expr
now dispatches to f64_to_str instead of int_to_str |
| **v0.3.194** | 2026-04-26 | `print(h.get(k))` for `HashMap<K, V>` now dispatches by V
instead of SIGSEGVing |
| **v0.3.193** | 2026-04-26 | Improved unresolved field-access error message
(NUC-FEEDBACK-010 partial close) |
| **v0.3.192** | 2026-04-26 | `print(if cond { x } else { y })` no longer SIGSEGVs |
| **v0.3.191** | 2026-04-26 | Two follow-ons bundled: |
| **v0.3.190** | 2026-04-26 | Three fixes bundled: |
| **v0.3.188** | 2026-04-26 | `for s in stats.iter() { println!("{}", s.field) }` now infers
s's type from the for-loop expression and dispatches struct
fields correctly |
| **v0.3.187** | 2026-04-26 | `for n in v.iter() { print(n); }` now works instead of
SIGSEGVing |
| **v0.3.186** | 2026-04-26 | Multi-payload enum variants like `Variant(str, i64)` now
dispatch print() per-binding instead of all-bindings-share-
one-type |
| **v0.3.185** | 2026-04-26 | `match Msg::Text(s) => print(s)` for user-defined `enum Msg
{ Text(str), ... }` now prints the string instead of pointer |
| **v0.3.184** | 2026-04-26 | `match Err(s) => print(s)` for `Result<T, str>` now prints the
string instead of the pointer |
| **v0.3.183** | 2026-04-26 | `println!("{}", str_concat(a, b))` and friends now dispatch
correctly via runtime-helper return-type fallback in the
println! heuristic |
| **v0.3.182** | 2026-04-26 | `print(hashmap_get(h, k))` for `HashMap<K, str>` now prints
the string instead of the pointer |
| **v0.3.181** | 2026-04-26 | `print(vec_get(vec_of_str, i))` now prints the string instead
of the pointer address |
| **v0.3.180** | 2026-04-26 | `Vec<f32>` (and `Vec<f16>`/`Vec<bf16>`/`Vec<f8e4m3>`/
`Vec<f8e5m2>`) is now a HARD compile error (TYP-009) |
| **v0.3.179** | 2026-04-26 | Better link-failure diagnostic — always print the failing
clang command and flag empty-log spawn failures |
| **v0.3.178** | 2026-04-26 | `print(hashmap_get(h, k))` and `print(h.get(k))` now work
instead of SIGSEGVing |
| **v0.3.177** | 2026-04-26 | `print(p)` inside a closure body now works for closure
parameters |
| **v0.3.176** | 2026-04-26 | `match Some(v) => print(v)` and friends now work instead of
SIGSEGVing |
| **v0.3.175** | 2026-04-26 | `for i in 0..n { print(i); }` now works instead of SIGSEGVing |
| **v0.3.174** | 2026-04-26 | Extended literal-only WARNING to remaining adopter-facing
handle-taking helper families |
| **v0.3.173** | 2026-04-26 | **Extended literal-only WARNING to tokenizer helpers |
| **v0.3.172** | 2026-04-26 | `println!("{}", s.method())` for method calls returning typed
scalars now dispatches correctly |
| **v0.3.171** | 2026-04-26 | **Extended literal-only WARNING to all container helper families |
| **v0.3.170** | 2026-04-26 | **Added literal-only WARNING for `vec_*(int_literal, ...)`.** |
| **v0.3.169** | 2026-04-26 | Two more `println!("{}", X)` silent-miscompute shapes fixed:
typed float binop and unary not |
| **v0.3.168** | 2026-04-26 | Five more `print()` dispatch shapes fixed: cast, unary minus,
unary not, user method calls, and builtin methods (Vec::len etc.) |
| **v0.3.167** | 2026-04-26 | `println!("{}", a == b)` and friends now print `"true"`/`"false"`
instead of `"0"`/`"1"` |
| **v0.3.166** | 2026-04-26 | `println!("{}", X)` now correctly dispatches for struct fields,
vec indexing, and casts |
| **v0.3.165** | 2026-04-26 | `print(a + b)` for binop results now dispatches by op/type
instead of SIGSEGVing |
| **v0.3.164** | 2026-04-26 | `print(v[i])` for typed vec elements now dispatches by element
type instead of SIGSEGVing |
| **v0.3.163** | 2026-04-26 | `print(p.field)` for struct fields now dispatches by field type
instead of SIGSEGVing |
| **v0.3.162** | 2026-04-26 | Added literal-only WARNING for `f64_to_str(int_literal)` /
`f32_to_str(int_literal)` |
| **v0.3.161** | 2026-04-26 | Rolled back v0.3.159/160 f64-helper arg-type-check |
| **v0.3.160** | 2026-04-26 | Extended f64-arg-0 type-check to all f64/f32 conversion helpers |
| **v0.3.159** | 2026-04-26 | `f64_to_str(int)` now emits TYP-006 instead of returning a
subnormal-looking f64 garbage |
| **v0.3.158** | 2026-04-26 | Bare `{}` format spec now infers types for FN-CALL return values
too, not just `let X: TYPE` declarations |
| **v0.3.157** | 2026-04-26 | Added `getenv` / `print_raw` / `panic` to the str-arg-0
type-check list |
| **v0.3.156** | 2026-04-26 | Extended runtime-helper arg-type-check to all common
str-taking helpers |
| **v0.3.155** | 2026-04-26 | Extended runtime-helper arg-type-check (v0.3.154 follow-up) to
`str_eq`, `str_concat`, `str_substring`, `str_char_at` |
| **v0.3.154** | 2026-04-26 | Runtime helpers `str_len` / `str_to_int` / `str_to_f64` /
`str_to_int_with_base` now type-check arg 0 |
| **v0.3.153** | 2026-04-26 | Hardened the v0.3.152 source-scan heuristic to detect type
conflicts |
| **v0.3.152** | 2026-04-26 | Bare `{}` format spec now infers types from `let X: TYPE`
declarations in source |
| **v0.3.151** | 2026-04-26 | `print(a, b, c)` no longer silently drops extra args without a
diagnostic |
| **v0.3.150** | 2026-04-26 | Removed reserved-but-unused `wrapping` and `saturating` keywords —
user fns named `wrapping` or `saturating` no longer fail to link with
a confusing `__nucleor_wrap_i32` undefined-reference error |
| **v0.3.149** | 2026-04-26 | `unsafe` is now a contextual keyword — user fns named `unsafe`
no longer silently miscompile to `0` after a noisy parse-error
cascade |
| **v0.3.148** | 2026-04-26 | `&&` and `\|\|` now properly short-circuit |
| **v0.3.147** | 2026-04-26 | `opt_fold_block` no longer silently miscompiles bitwise `&` `\|` `^`
on negative operands |
| **v0.3.146** | 2026-04-26 | `str_from_int` no longer returns just `"-"` for `i64::MIN`. Const-
folded expressions producing `i64::MIN` (e.g. `i64::MAX + 1`) no
longer emit invalid LLVM IR like `add i64 -, 0` |
| **v0.3.145** | 2026-04-26 | `opt_fold_block` no longer silently miscompiles `x / 0` and `x % 0`
to constant `0` AND no longer skips the next IR instruction in the
block |
| **v0.3.144** | 2026-04-26 | `stdlib/rods/tokenizer.nr` now exposes token-vector accessors and
the existing `nuc_tok_decode` runtime helper |
| **v0.3.143** | 2026-04-26 | `print(arg)` now dispatches by arg's static type — `print(int)`
no longer SIGSEGVs when the integer happens to look like a small
pointer |
| **v0.3.142** | 2026-04-26 | `as` is now a contextual keyword — user fns named `as` no longer
silently miscompile to `0`. The `x as T` postfix cast operator
still works |
| **v0.3.141** | 2026-04-26 | `loop` is now a contextual keyword — user fns named `loop` no
longer silently miscompile to `0`. The canonical `loop { BODY }`
stmt-form still works |
| **v0.3.140** | 2026-04-26 | Removed reserved-but-unused `restricts` keyword — user fns named
`restricts` no longer silently miscompile to `0` |
| **v0.3.139** | 2026-04-26 | `requires` is now a contextual keyword — user fns named
`requires` no longer silently miscompile to `0` |
| **v0.3.138** | 2026-04-26 | Removed reserved-but-unused `scope` and `spawn` keywords — user
fns named `scope` or `spawn` no longer silently miscompile to `0` |
| **v0.3.137** | 2026-04-26 | `pure` is now a contextual keyword — user fns named `pure` no
longer silently miscompile to `0` |
| **v0.3.136** | 2026-04-25 | `str_to_int` as a user-callable runtime helper |
| **v0.3.135** | 2026-04-25 | NUC-FEEDBACK-007 closed — cache key now includes compiler
version so binary upgrades invalidate cached IR |
| **v0.3.134** | 2026-04-25 | `--version` string bumped from `0.2.0-v2` to current tag |
| **v0.3.133** | 2026-04-25 | NUC-FEEDBACK-006 closed — sub-micro f64 literals via runtime
str_to_f64 |
| **v0.3.132** | 2026-04-25 | Format heuristic extension — `_f64(` / `_f32(` typed-wrapper
suffix detection |
| **v0.3.131** | 2026-04-25 | Bare `{}` format heuristic — closes a HIGH-blast silent
miscompute on the most common debug pattern |
| **v0.3.130** | 2026-04-25 | NUC-FEEDBACK-005 closed — long-precision f64 literals silently
collapsed to 0 |
| **v0.3.129** | 2026-04-25 | Struct-field access on captured variables inside closures |
| **v0.3.128** | 2026-04-25 | Enum-variant equality (`t == Tag::A`) — closes a HIGH-blast
silent miscompute |
| **v0.3.127** | 2026-04-25 | NUC-IMPROVE-005 closed — `stdlib/rods/math_typed.nr` typed-f64
elementary math wrappers |
| **v0.3.126** | 2026-04-25 | User-defined fns shadow runtime helpers of the same name —
closes a HIGH-blast silent miscompute hidden in name collisions |
| **v0.3.125** | 2026-04-25 | NUC-IMPROVE-004 closed — explicit `f64_from_bits` /
`f64_to_bits` (and f32 siblings) reinterpret helpers |
| **v0.3.124** | 2026-04-25 | Real `v[i] = X` indexed-LHS lowering (replaces v0.3.81 diag-only
stub) |
| **v0.3.123** | 2026-04-25 | Chained inherent-method calls (`Counter::new().add(10).add(5)`) |
| **v0.3.122** | 2026-04-25 | NUC-FEEDBACK-004 closed — stdlib import resolution from outside
the OSS repo |
| **v0.3.121** | 2026-04-25 | NUC-FEEDBACK-001 closed — `nuc test` harness now passes panic!/
assert! family macros through correctly |
| **v0.3.120** | 2026-04-25 | JSONL evidence-emit rod (NUC-IMPROVE-003) |
| **v0.3.119** | 2026-04-25 | ML_Suite feedback batch — closes NUC-FEEDBACK-002 (narrow-float
vec silent miscompute) and NUC-FEEDBACK-003 (clang toolchain
detection on Windows) |
| **v0.3.118** | 2026-04-25 | Range patterns in stmt-position match — closes a HIGH-blast
silent-miscompute → segfault |
| **v0.3.117** | 2026-04-25 | `with_capacity(N)` for the collection family |
| **v0.3.116** | 2026-04-25 | `Vec::with_capacity(N)` constructor |
| **v0.3.115** | 2026-04-25 | Turbofish syntax `name::<TypeArgs>(args)` |
| **v0.3.114** | 2026-04-25 | Pure-shorthand struct init in expression position |
| **v0.3.113** | 2026-04-25 | Capture propagation across nested closure literals |
| **v0.3.112** | 2026-04-25 | `extern "C" fn name(...)` declaration syntax |
| **v0.3.111** | 2026-04-25 | User-defined associated fn dispatch + `Self` return-type
substitution |
| **v0.3.110** | 2026-04-25 | Supertrait syntax `trait Sub: Super` / `trait Foo: Bar + Baz` |
| **v0.3.109** | 2026-04-25 | Iterator combinators `.any()` / `.all()` / `.take(n)` / `.skip(n)` |
| **v0.3.108** | 2026-04-25 | `.iter().count()` iterator-length terminator |
| **v0.3.107** | 2026-04-25 | Nested closures get distinct LLVM function names |
| **v0.3.106** | 2026-04-25 | `&self` / `&mut self` in trait declarations + tuple-pattern
match diagnostic |
| **v0.3.105** | 2026-04-25 | Bitwise/shift compound assigns + char literal escapes — closes
two more silent-miscompute classes |
| **v0.3.104** | 2026-04-25 | Shift operators `<<` and `>>` — eliminates the second silent-
miscompute hazard in the same family as v0.3.103 bitwise |
| **v0.3.103** | 2026-04-25 | Real bitwise op codegen + `str_from_int` alias |
| **v0.3.102** | 2026-04-25 | `.collect()` iterator-chain terminator |
| **v0.3.101** | 2026-04-25 | Closure literal `-> RetType` annotation parsing |
| **v0.3.100** | 2026-04-25 | Receiver-type-aware method dispatch for the collection family |
| **v0.3.99** | 2026-04-25 | `vec![…]` macro — Rust-idiom container literal |
| **v0.3.98** | 2026-04-25 | Runtime helper: `env_get_or(name, default)` — Rust ergonomic
parity for the canonical `env::var(name).unwrap_or("default")`
idiom |
| **v0.3.97** | 2026-04-25 | Diagnostic: bitwise operators (`&`, `\|`, `^`) — silent
miscompute eliminated |
| **v0.3.96** | 2026-04-25 | Diagnostic: closures cannot mutate captured variables (FnMut
silent miscompute eliminated, HIGH-BLAST-RADIUS) |
| **v0.3.95** | 2026-04-25 | Feature: extended macro set — `assert_eq!`, `assert_ne!`, `todo!`,
`unimplemented!`, `unreachable!` |
| **v0.3.94** | 2026-04-25 | Feature: `panic!`, `assert!`, and `dbg!` macro forms |
| **v0.3.93** | 2026-04-25 | Diagnostic: `&mut T` reference parameters surface a clear error
instead of silently producing wrong output (HIGH-BLAST-RADIUS) |
| **v0.3.92** | 2026-04-25 | Parser fix: `dyn Trait` syntax acceptance |
| **v0.3.91** | 2026-04-25 | HIGH-BLAST-RADIUS FIX: compiler-builtin `Ok(x)`/`Err(x)`
constructors used different tag values than stdlib
`result_ok(x)`/`result_err(x)`, breaking the `?` operator
for compiler-builtin Result |
| **v0.3.90** | 2026-04-25 | Feature: shorthand field initialization in struct literals |
| **v0.3.89** | 2026-04-25 | Feature: generic params on trait methods (`fn count<T>(self)`) |
| **v0.3.88** | 2026-04-25 | Feature: `vec.iter().X()` chain — Rust idiom support |
| **v0.3.87** | 2026-04-25 | Feature: struct-like enum variant construction
`EnumName::Variant { field: value, ... }` |
| **v0.3.86** | 2026-04-25 | Feature: multi-capture enum patterns `Variant(a, b, c)` |
| **v0.3.85** | 2026-04-25 | Diagnostic: associated constants in trait/impl bodies |
| **v0.3.84** | 2026-04-25 | High-blast-radius fix: match-arm assignment body silently dropped
the assignment |
| **v0.3.83** | 2026-04-25 | Feature: fn-pointer type syntax `fn(T1, T2) -> R` in parameter
positions |
| **v0.3.82** | 2026-04-25 | Feature: trait default methods |
| **v0.3.81** | 2026-04-25 | Two more compiler-segfault patterns eliminated |
| **v0.3.80** | 2026-04-25 | Robustness fix: nested struct field assignment was crashing the
compiler with ACCESS_VIOLATION (exit -1073741819) |
| **v0.3.79** | 2026-04-25 | LOCKED PARTIAL CLOSED — T1.2 match-on-enum-with-data + stmt-style
arm bodies |
| **v0.3.78** | 2026-04-25 | LOCKED PARTIAL CLOSED — T2.1/2/3 inline closure with capture |
| **v0.3.77** | 2026-04-25 | Lexer |
| **v0.3.76** | 2026-04-25 | Compiler fix #19: shadowing semantics — `let x = expr_using_x;`
silently produced garbage |
| **v0.3.75** | 2026-04-25 | Parser fix #2: clear diagnostic for statement-level keywords at
module scope |
| **v0.3.74** | 2026-04-25 | Compiler fix #18: trait-method-call (kind 8) indexed operand
resolution |
| **v0.3.73** | 2026-04-25 | Parser fix #1: clear diagnostic for `let` at module scope (was
silently dropped + emitted broken IR) |
| **v0.3.72** | 2026-04-25 | Compiler fix #17: closure capture link-time correctness — runtime
helpers `__nucleor_capture_set` / `__nucleor_capture_get` were declared
in the IR header but never DEFINED in the runtime |
| **v0.3.71** | 2026-04-25 | Compiler fix #16: Rust-style associated-fn aliases for collections
(`HashMap::new()` etc.) + clear diagnostic for unsupported assoc-fn
calls |
| **v0.3.70** | 2026-04-25 | Production-coverage lock for the v0.3.65-69 nested-composition
arc |
| **v0.3.69** | 2026-04-25 | Compiler fix #15: trait method results returning structs +
immediate field access (`v.rotated().x`) |
| **v0.3.68** | 2026-04-25 | Compiler fix #14: nested indexing (`grid[i][j]`) — closes
the composition matrix |
| **v0.3.67** | 2026-04-25 | Compiler fix #13: indexing on fn-call result
(`make_vec()[i]`) |
| **v0.3.66** | 2026-04-25 | Compiler fix #12: method calls on indexed struct field
receivers (`p.rects[0].area()`) |
| **v0.3.65** | 2026-04-25 | Compiler fix #11: nested-operand indexing — `self.samples[i]`
in trait method bodies |
| **v0.3.64** | 2026-04-25 | Production-coverage synthesis: `examples/23_rt_sensor_fusion.nr`
combines all v0.3.51 → v0.3.63 codegen-fix shapes inline |
| **v0.3.63** | 2026-04-25 | Compiler fix #10: fixed-array-of-struct field access |
| **v0.3.62** | 2026-04-25 | Compiler fix #9: fixed-size array `[T; N]` indexing in
inline f64 binops |
| **v0.3.61** | 2026-04-25 | Compiler fix #8: as-cast results in inline f64 binops |
| **v0.3.60** | 2026-04-25 | Compiler fix #7: trait method results in inline f64
binops |
| **v0.3.59** | 2026-04-25 | Compiler fix #6: Vec-of-struct field access |
| **v0.3.58** | 2026-04-25 | Compiler fix #5: chained field access on fn-call result |
| **v0.3.57** | 2026-04-25 | Compiler fix #4: f64 unary minus now compiles correctly |
| **v0.3.56** | 2026-04-25 | Production-coverage lock for f64 inline binops with mixed
operand kinds |
| **v0.3.55** | 2026-04-25 | Compiler fix #3: f64 inline binary ops on Vec indexing now
compile correctly |
| **v0.3.54** | 2026-04-25 | Compiler fix #2: f64 inline binary ops on fn-call results
now compile correctly |
| **v0.3.53** | 2026-04-25 | Compiler fix: f64 inline binary ops on struct-field operands
now compile correctly |
| **v0.3.52** | 2026-04-25 | T3.27 strict regression test for the v0.3.51 codegen
workaround |
| **v0.3.51** | 2026-04-25 | `examples/22_rt_export.nr` — runnable demo of `#[export]` —
also surfaces a real codegen bug |
| **v0.3.50** | 2026-04-25 | `examples/21_rt_state_machine.nr` — runnable demo of
`#[max_depth = N]` |
| **v0.3.49** | 2026-04-25 | Robotics guide picks up `examples/20_rt_motor_ffi.nr` |
| **v0.3.48** | 2026-04-25 | `examples/20_rt_motor_ffi.nr` — runnable demo of the
v0.3.24 `#[ffi_no_alloc]` / `#[ffi_no_panic]` markers |
| **v0.3.47** | 2026-04-25 | T3.21 strict assertion: build exit code + error-tier
absence |
| **v0.3.46** | 2026-04-25 | T3.20 strict shape-prefix assertions |
| **v0.3.45** | 2026-04-25 | Docs ship: robotics guide picks up the drift-gate
ecosystem |
| **v0.3.44** | 2026-04-25 | T3.26 cli-help cmds drift gate |
| **v0.3.43** | 2026-04-25 | T3.25 examples-list drift gate |
| **v0.3.42** | 2026-04-25 | T3.24 spec-doc drift gate — caught the SAME NUM-006..020
gap in `docs/spec/Nucleor_Error_Codes.md` |
| **v0.3.41** | 2026-04-25 | `cli_explain_full_smoke` tightened from synopsis-only to
full three-entry check |
| **v0.3.40** | 2026-04-25 | T3.23 extended to three-way drift coverage (verify.sh ⇄
verify.ps1 ⇄ s1) |
| **v0.3.39** | 2026-04-25 | T3.23 drift gate — caught a real 15-code synchronization
gap on its first run |
| **v0.3.38** | 2026-04-25 | T3.22: DIAG-001 enumerated check — within-series typos now
caught |
| **v0.3.37** | 2026-04-25 | T3.21 fixture proves `#[allow(DIAG-001)]` suppresses
DIAG-001 itself |
| **v0.3.36** | 2026-04-25 | T3.20: DIAG-001 — first user-facing code in the RFC-0020
DIAG namespace |
| **v0.3.35** | 2026-04-25 | Docs ship: robotics guide cookbook §4 grows a semantics
summary + known-limitation note |
| **v0.3.34** | 2026-04-25 | T3.19 strict assertion: `#[allow_fn]` cannot demote error
tier |
| **v0.3.33** | 2026-04-25 | T3.18 strict assertion: `#[deny_fn(RT-007)]` actually
promotes warning to error |
| **v0.3.32** | 2026-04-25 | T3.17 fixture proves `#[allow_fn(RT-004)]` per-fn opt-out is
real, not vapor |
| **v0.3.31** | 2026-04-25 | RT-004 warning text now mentions per-fn `#[allow_fn]` |
| **v0.3.30** | 2026-04-25 | `nuc explain RT-005` and `nuc explain RT-007` hints updated |
| **v0.3.29** | 2026-04-25 | RT-007 warning text now mentions the v0.3.20 per-fn allow |
| **v0.3.28** | 2026-04-25 | RT-005 warning text now mentions the v0.3.24 markers |
| **v0.3.27** | 2026-04-25 | Cache-hit hardening pass for the diagnostic-dependent verify
steps flagged in v0.3.26 |
| **v0.3.26** | 2026-04-25 | **T3.16 negative test — `#[deadline]` intersection rule for the |
| **v0.3.25** | 2026-04-25 | Docs ship: `docs/v0.3-robotics-guide.md` documents the v0.3.24
`#[ffi_no_alloc]` / `#[ffi_no_panic]` extern markers |
| **v0.3.24** | 2026-04-25 | T3.15 `#[ffi_no_alloc]` and `#[ffi_no_panic]` extern markers
— closes RT-005 v1's deferred opt-out surface |
| **v0.3.23** | 2026-04-25 | T3.14 negative test — `#[allow_fn]` does not suppress
error-tier diagnostics |
| **v0.3.22** | 2026-04-25 | `docs/v0.3-robotics-guide.md` — per-fn allow/deny attribute
documentation |
| **v0.3.21** | 2026-04-25 | T3.13 per-fn `#[deny_fn(CODE)]` attribute |
| **v0.3.20** | 2026-04-25 | T3.12 per-fn `#[allow_fn(CODE)]` attribute |
| **v0.3.19** | 2026-04-25 | `docs/v0.3-robotics-guide.md` Cookbook section |
| **v0.3.18** | 2026-04-25 | `docs/v0.3-robotics-guide.md` — single-page summary of the
v0.3 robotics arc |
| **v0.3.17** | 2026-04-25 | RFC-0028 phase 3 — `format3_isf` combo |
| **v0.3.16** | 2026-04-25 | RFC-0028 phase 3 — `format3_sif` combo |
| **v0.3.15** | 2026-04-25 | RFC-0028 phase 3 — `format3_ssf` combo |
| **v0.3.14** | 2026-04-25 | RFC-0028 phase 3 — float-mixing format3 combos |
| **v0.3.13** | 2026-04-25 | RFC-0028 phase 3 — `format3_iis` + `format3_isi` combos |
| **v0.3.12** | 2026-04-25 | RFC-0028 phase 3 — `format3_ssi` + `format3_sis` combos |
| **v0.3.11** | 2026-04-25 | **Test-framework coverage for the bare `arena_*` builtin path.** |
| **v0.3.10** | 2026-04-25 | `examples/19_rt_pid.nr` — fully RT-annotated PID step |
| **v0.3.9** | 2026-04-25 | T3.10 RT-008 — direct self-recursion in `#[deadline]` fn
warns; `#[max_depth = N]` opts out. Closes RFC-0001 §3.5 |
| **v0.3.8** | 2026-04-25 | T3.9 RT-005 — FFI call from inside an RT-marked fn warns |
| **v0.3.7** | 2026-04-25 | T3.8 RT-006 — RT attribute on `async fn` is rejected |
| **v0.3.6** | 2026-04-25 | T3.7 polish — RT-001/002/003 v1 checkers strip strings and
line comments before scanning |
| **v0.3.5** | 2026-04-25 | T3.6 `#[no_dyn]` enforcement (RT-003) — completes the
RFC-0001 RT attribute family |
| **v0.3.4** | 2026-04-25 | T3.4 extern-C shim generator — `#[export]` attribute makes
Nucleor fns callable from C |
| **v0.3.3** | 2026-04-25 | T3.5 RT-007 cross-check — `#[deadline]` without
`#[no_alloc]` or `#[no_panic]` now warns |
| **v0.3.2** | 2026-04-25 | T3.3 static WCET v1 estimator (RT-004) + critical
`expand_deadline` leak fix |
| **v0.3.1** | 2026-04-24 | T3.2 `#[no_panic]` enforcement (RT-002) — source-level v1
check matching the existing `#[no_alloc]` shape |
| **v0.3.0** | 2026-04-24 | v0.3 robotics foundation lands — `#[deadline = N]` runtime
checks now actually fire at runtime |

## v0.2.x

| Version | Date | Summary |
|---|---|---|
| **v0.2.353** | 2026-04-24 | T2.8 async runtime decision — threads-only commitment
shipped as working syntax + runtime |
| **v0.2.352** | 2026-04-24 | T2.7 `nuc doc` HTML mode — `--html` flag (with .html /
.htm extension auto-detect on `--out`) emits a styled,
standalone, self-contained HTML doc page from the same
two-pass walk that drives the Markdown renderer |
| **v0.2.351** | 2026-04-24 | T2.5 lifetime parameters (parse-only) — `'a` annotations
recognized in generic param lists, reference types, and
generic type instantiations |
| **v0.2.350** | 2026-04-24 | T2.4 trait objects (a) — `Box<dyn Trait>` 2-cell handle
runtime helpers + manual dispatch pattern |
| **v0.2.349** | 2026-04-24 | Source-level rewriting in the resolver lifts each
closure into a synthesized top-level fn and replaces the closure
expression with the synth fn's name. The T2.2 iterator dispatch
then receives a function-pointer value through the existing
i64-cell calling convention — no runtime change required.

### Scope and bug-discovered limitation

T2.3 fires ONLY when `\|` is preceded (after whitespace) by `(`
or `,` — i.e., |
| **v0.2.348** | 2026-04-24 | **T2.2 iterator trait + adapters — Vec method-call dispatch |
| **v0.2.347** | 2026-04-24 | T2.1 pattern matching beyond enum tags — range patterns
(`1..=9` inclusive, `1..10` exclusive) wired in both
compilers + the long-existing __range / __range_bad lower
handlers + tools-suite `..=` lex token + tools-suite lower
parity for both stmt and expr match forms |
| **v0.2.346** | 2026-04-24 | T2.6 format strings — `println!()`, `print!()`, `format!()`
macros via source-level expansion in the resolver |
| **v0.2.345** | 2026-04-24 | T1.6 C ABI rest — `#[repr(C)]` struct-by-value support
in `nuc gen-headers` + a fix for the long-standing
`struct_repr` lookback false-positive |
| **v0.2.344** | 2026-04-24 | T1.4 packager — `nuc registry export-static` (GitHub-Pages
publishable static-site shape) + RFC-0019 §5 schema +
fs_copy_file return-code bug fix |
| **v0.2.343** | 2026-04-24 | T1.5d modules — friendly compile-time MOD-003 diagnostic
replaces the link-time clang error |
| **v0.2.342** | 2026-04-24 | T1.5c modules — resolver-layer name privatization (real
cross-module visibility enforcement, opt-in semantics) |
| **v0.2.341** | 2026-04-24 | T1.5b modules — `pub` parsed + tagged + surfaced via `nuc
summary` |
| **v0.2.340** | 2026-04-24 | T1.5a modules — `mod foo { ... }` inline block-form +
tools-suite resolver parity for `mod foo;` |
| **v0.2.339** | 2026-04-24 | T1.7 Linux build target — cross-platform link command +
checked-in IR seed + Linux/macOS bootstrap script + verify
gate parity |
| **v0.2.338** | 2026-04-24 | T1.3 HashMap + String — verify-gate test promoted + harness
`#cfile` import bridge fixed |
| **v0.2.337** | 2026-04-24 | T1.9 test framework smoke + T1.1 narrow-wrap parity fix
in tools_suite |
| **v0.2.336** | 2026-04-24 | T1.2 Result/Option/match payloads + `?` operator — end-to-end
functional |
| **v0.2.335** | 2026-04-24 | T1.8 Diagnostics — Rust-style snippet + caret rendering |
| **v0.2.334** | 2026-04-24 | Robotics: 4-wheel skid-steer kinematics + odometry
(`skid_steer`). Forward + inverse kinematics + Euler odometry
step. Companion to `diff_drive` (2-wheel) and `mecanum`
(omnidirectional). Used for Husky, Jackal, big agricultural /
mining vehicles, and tracked platforms |
| **v0.2.333** | 2026-04-24 | Robotics: mecanum-wheel (4-wheel omnidirectional) kinematics
+ odometry (`mecanum`). Forward + inverse kinematics for the
standard X-pattern roller geometry, plus Euler odometry step.
Supports strafing, rotation, and combined motion. Used in
indoor warehouse robots, soccer/competition platforms |
| **v0.2.332** | 2026-04-24 | Robotics: differential-drive (2-wheel) kinematics + odometry
(`diff_drive`). Forward + inverse kinematics, Euler odometry
step, exact constant-curvature arc integration. Foundational
for TurtleBot / iRobot Create / custom 2-wheel platforms |
| **v0.2.331** | 2026-04-24 | Robotics: Canny edge detector (`canny`). Full 4-stage edge
pipeline: Sobel gradients → non-maximum suppression along
gradient direction → double threshold → 8-connected hysteresis.
Output is a binary edge map (0 / 255) |
| **v0.2.330** | 2026-04-24 | Robotics: Sobel edge gradient (`sobel`). Computes per-pixel
x-gradient, y-gradient, and gradient magnitude using the
standard 3×3 Sobel stencils. Foundation for Canny / Hough /
edge-strength salience maps |
| **v0.2.329** | 2026-04-24 | Robotics: FAST corner detector (`fast_corner`). "Features
from Accelerated Segment Test" (Rosten & Drummond 2006).
Faster than Harris (no gradient computation, just pixel
comparisons); standard front-end for ORB. Implements FAST-9
(9-pixel consecutive segment) |
| **v0.2.328** | 2026-04-24 | Robotics: Harris corner detector (`harris`). For each pixel,
computes `R = det(M) - k·trace(M)²` where M is the 3×3-windowed
structure tensor of image gradients. Pixels above threshold AND
3×3 local maxima are reported as corners, sorted by response |
| **v0.2.327** | 2026-04-24 | Robotics: unit-quaternion utilities for 3-D rotation
(`quat`). Foundational primitives — IMU sensor fusion,
robot arm joint orientations, smooth orientation
interpolation (slerp), SLAM rotation parameterization |
| **v0.2.326** | 2026-04-24 | Robotics: Catmull-Rom spline evaluation in 2-D and 3-D
(`catmullrom`). Interpolating spline that passes through every
control point. C¹ continuous, locally controlled. Centripetal
parameterization (alpha = 0.5) avoids loops/cusps from non-
uniform control point spacing |
| **v0.2.325** | 2026-04-24 | Robotics: Bezier curve evaluation in 2-D and 3-D
(`bezier`). Numerically-stable de Casteljau recurrence for
arbitrary degree. Companion to cubic spline for path
authoring with explicit handle control |
| **v0.2.324** | 2026-04-24 | Robotics: natural cubic spline interpolation through N
waypoints (`cubicspline`). C² piecewise-cubic curve fitting
for path smoothing, sensor calibration tables, and visualization.
Two-call API: `fit` precomputes second-derivative table,
`sample` queries the curve at any x |
| **v0.2.323** | 2026-04-24 | Robotics: trapezoidal velocity profile for point-to-point
motion (`trapvel`). Foundational profile underneath every
robot servo drive, CNC controller, and pick-and-place
sequencer that needs smooth bounded motion. First rod ship
after the T1.1 numerics refactor closeout |
| **v0.2.322** | 2026-04-24 | 🏁 T1.1 Phase 13 — final RFC rollup. T1.1 maximalist
narrow-numerics refactor COMPLETE |
| **v0.2.321** | 2026-04-24 | T1.1 Phase 12 — rod audit + audit document |
| **v0.2.320** | 2026-04-24 | T1.1 Phase 11 — width-correct formatting (`print_<T>` for
every integer / float width) |
| **v0.2.319** | 2026-04-24 | T1.1 Phase 10 — diagnostics namespace expansion (NUM-001..020)
+ negative-test for literal-overflow |
| **v0.2.318** | 2026-04-24 | T1.1 Phase 9 — FFI ABI + `nuc gen-headers` subcommand |
| **v0.2.317** | 2026-04-24 | 🎉 T1.1 Phase 8 — `Vec<T>` byte-packed runtime + matrix
fully green (60/60) |
| **v0.2.316** | 2026-04-24 | T1.1 Phase 7 — overflow modes (wrapping / saturating /
checked) at all widths |
| **v0.2.315** | 2026-04-24 | T1.1 Phase 6 — bitwise + shift ops at narrow widths |
| **v0.2.314** | 2026-04-24 | T1.1 Phase 4 — full `as` cast operator matrix |
| **v0.2.313** | 2026-04-24 | T1.1 Phase 3b — `sizeof_struct(<Name>)` compile-time builtin
+ struct-layout machinery foundation |
| **v0.2.312** | 2026-04-24 | Closes the matrix
fail surface for floating-point binops; pairs with the Phase 1
`let`-narrow hook so f32 literals get correctly narrowed at the
assignment boundary.

### What landed

#### A. Runtime helpers (`stdlib/runtime/nucleor_llvm_rt.c`)
- 10 new f64 helpers: `__nucleor_f64_{add,sub,mul,div,lt,gt,le,ge,eq,ne}`.
- 4 new f32 comparison helpers: `__nucleor_f32_{ne,le,ge}`
  (`{lt,gt,eq}` already existed).
- |
| **v0.2.311** | 2026-04-24 | T1.1 Phase 3a — `sizeof_<T>()` primitive byte-size builtins |
| **v0.2.310** | 2026-04-24 | T1.1 Phase 3c — stdlib audit + i32/u32 narrowing re-enabled |
| **v0.2.309** | 2026-04-24 | T1.1 Phase 2 — f32 literals + bootstrap-stability gate +
defensive narrow scope |
| **v0.2.308** | 2026-04-24 | T1.1 Phase 1 — width-correct integer wrap on `let` binding |
| **v0.2.307** | 2026-04-24 | T1.1 Phase 0 — maximalist narrow-numerics test matrix
scaffolding |
| **v0.2.306** | 2026-04-24 | Robotics: 2-D Voronoi diagram from Delaunay triangulation
(`voronoi`). Lifts a Delaunay triangulation (`delaunay.nr`)
into its dual Voronoi diagram. Each Voronoi vertex is a
Delaunay-triangle circumcenter; each Voronoi edge connects
circumcenters of triangles sharing a Delaunay edge. Hull-
boundary edges have one endpoint at infinity (encoded as
`-1`) |
| **v0.2.305** | 2026-04-24 | Robotics: 2-D Delaunay triangulation + circumcenter helper
(`delaunay`). Bowyer-Watson incremental insertion (Bowyer 1981
/ Watson 1981). Foundation for Voronoi diagrams (lift each
output triangle through `circumcenter_2d` to get the Voronoi
vertex), triangle-mesh generation, and natural-neighbor
interpolation |
| **v0.2.304** | 2026-04-24 | Robotics: Hough transform for 2-D circle detection
(`hough_circle`). Companion to `hough.nr` (lines). Each input
edge point votes for every (cx, cy) on every candidate ring
of radius R; peaks in the 3-D `(cx × cy × R)` accumulator
reveal circles. 3×3×3 NMS with lex-order tie-breaking gives
deterministic single-peak per neighborhood |
| **v0.2.303** | 2026-04-24 | Robotics: Gaussian image pyramid reduce/expand
(`image_pyramid`). Burt & Adelson 1983 multi-resolution
representation with the canonical 5-tap separable Gaussian
`[1 4 6 4 1] / 16`. Foundation for coarse-to-fine Lucas-Kanade
and multi-scale feature detection |
| **v0.2.302** | 2026-04-24 | Robotics: Reeds-Shepp shortest paths (`reeds_shepp`). Car-like
robot with minimum turning radius that can MOVE IN REVERSE
(Reeds & Shepp 1990). Companion to `dubins.nr` which is
forward-only. For a U-turn-in-place query, RS gives π/2
(back-up + pull-forward) vs Dubins' 2π — 4× shorter |
| **v0.2.301** | 2026-04-24 | Robotics: point-to-plane ICP (`icp_p2p`). Chen & Medioni 1991
variant that minimises point-to-tangent-plane distance rather
than point-to-point. Converges 3–10× faster than point-to-point
ICP on planar scenes (room walls, floor, CAD models) |
| **v0.2.300** | 2026-04-24 | Robotics: Vector Field Histogram (VFH) local obstacle
avoidance (`vfh`). Polar histogram of obstacle density around
the robot, valley detection, picks the valley nearest the goal
bearing. Borenstein & Koren 1991 (simplified VFH+) |
| **v0.2.299** | 2026-04-24 | Robotics: kinematic bicycle model forward integration
(`bicycle`). Closed-form Euler and RK4 single-step integrators
for the standard car-like kinematic bicycle model. Companion
to `purepursuit` and `stanley` — the controllers output a
steering angle, this rod rolls the model forward |
| **v0.2.298** | 2026-04-24 | Robotics: Stanley path tracker for Ackermann robots (`stanley`).
Front-axle controller from the Stanford DARPA Grand Challenge
vehicle. Steering combines heading error (path tangent − vehicle
heading) and signed cross-track error of the front axle relative
to the path |
| **v0.2.297** | 2026-04-24 | Robotics: pure-pursuit geometric path tracker for Ackermann
robots (`purepursuit`). Given current pose, a 2-D waypoint path,
and a lookahead distance, computes the steering angle that drives
the vehicle along an arc to the lookahead point |
| **v0.2.296** | 2026-04-24 | Robotics: 2D Hough line detection (`hough`). Each input point
votes for the family of lines through it parameterized by `(ρ, θ)`;
peaks in the accumulator → detected lines. Foundation for laser-
scan landmark extraction and structured-environment SLAM |
| **v0.2.295** | 2026-04-24 | Robotics: Dubins shortest paths for car-like robots with minimum
turning radius (Dubins 1957; Shkel & Lumelsky 2001 closed-form
classification). All 6 candidate types (LSL, LSR, RSL, RSR, RLR,
LRL); returns the shortest length plus type and per-segment
parameters |
| **v0.2.294** | 2026-04-24 | Collinear |
| **v0.2.293** | 2026-04-24 | Robotics: 2D point-cloud / laser-scan ICP alignment (`scanmatch`).
2D specialization of ICP with closed-form 2D Procrustes per
iteration. Standard SLAM front-end scan-matcher primitive.
Complement to `icp.nr` (3D point-to-point ICP) |
| **v0.2.292** | 2026-04-24 | Robotics: full LiDAR-scan update for `occgrid.nr`
(`occgrid_update_scan`). Single-call wrapper that applies every
beam of a scan as an inverse-sensor-model raycast — the typical
"plug a 2D LiDAR into the mapper" call |
| **v0.2.291** | 2026-04-24 | Robotics: 2D BFS / Dijkstra-lite wavefront distance transform
(`wavefront`). Given a traversability grid and one or more start
cells, compute per-cell distance to the nearest start, propagated
along traversable cells. 4- or 8-connectivity |
| **v0.2.290** | 2026-04-24 | Robotics: frontier detection for `occgrid.nr` — finds "free"
cells that have at least one "unknown" 4-neighbor. Standard
autonomous-exploration primitive: the boundary between known free
space and the unknown is where the robot should go next |
| **v0.2.289** | 2026-04-24 | Robotics: outlier-robust point-cloud rigid alignment
(`pcalign_horn_ransac`). Self-contained RANSAC over
3-correspondence samples + Horn fit + reprojection-error inlier
scoring + final refit on the inlier set. One-call replacement for
the manual RANSAC + pcalign + refit triple needed for outlier-
contaminated correspondences |
| **v0.2.288** | 2026-04-24 | Robotics: closed-form 3-D rigid alignment of two point clouds
with known correspondences via Horn's quaternion method (`pcalign`).
Foundation for ICP back-end, fragment registration, AR pose
estimation from world-anchor correspondences, and RANSAC-outlier-
robust registration |
| **v0.2.287** | 2026-04-24 | Robotics: 2D log-odds probabilistic occupancy grid (`occgrid`)
with Bresenham-traced raycast updates. Foundational mapping
primitive for 2D mobile-robot SLAM (Hector / gmapping style),
free-space planning, and frontier exploration |
| **v0.2.286** | 2026-04-24 | Robotics: hierarchical coordinate-frame transform tree (`tf`).
ROS-style "tf" — each frame has an integer ID, a parent ID, and a
pose `(t, q)` in the parent frame. Lookup composes transforms
along the path through the tree |
| **v0.2.285** | 2026-04-24 | Robotics: pointer-based SE(3) operations (`se3`). Compose,
inverse, apply, log/exp, interpolation, distance — all on raw
`(t ∈ ℝ³, q ∈ S³)` buffers. Counterpart to `kinematics.nr`'s
allocated-handle Pose, parallel to how `qutil.nr` complements the
allocated-handle Quat |
| **v0.2.284** | 2026-04-24 | Robotics: Huber-robust kernel for `pgs3.nr` (3D pose-graph SLAM).
Mirror of v0.2.277's 2D `pgs_optimize_huber` for SE(3) loop
closures |
| **v0.2.283** | 2026-04-24 | Robotics: differential-drive and Ackermann mobile-robot
kinematics + arc-integrated pose update (`mobile`) |
| **v0.2.282** | 2026-04-24 | Robotics: Lucas-Kanade single-feature tracker (`klt`). Given two
grayscale images and a feature point in image 1, find the
displacement to match in image 2 via Newton-LK iteration.
Foundation for visual-odometry feature flow and template tracking |
| **v0.2.281** | 2026-04-24 | Robotics: generic RANSAC orchestrator (`ransac_run`) added to
`ransac.nr` alongside the existing specialized `ransac_plane_3d`.
User-supplied `fit` and `score` callbacks let RANSAC handle any
geometric model (2-D line / circle, 3-D sphere, fundamental /
essential matrix, similarity transform between point clouds, etc.)
without one-off rod-per-shape duplication |
| **v0.2.280** | 2026-04-24 | Robotics: basic image processing primitives (`imgproc`) on
grayscale images. Sobel gradient, gradient magnitude, box filter,
separable Gaussian blur, bilinear resize. Foundation for vision
pipelines and image pyramids |
| **v0.2.279** | 2026-04-24 | Robotics: iterative inverse-distortion for `cam.nr`. Adds two
functions to invert the Brown-Conrady distortion model so that
distorted pixels can be undistorted (then unprojected) without
caller pre-rectification |
| **v0.2.278** | 2026-04-24 | Robotics: Cauchy/Lorentzian redescending kernel for `pgs.nr`
(`pgs_optimize_cauchy`). Stronger outlier rejection than Huber —
the weight `w = 1 / (1 + r²/c²)` is < 1 for ALL non-zero
residuals (truly redescending), so outliers are rejected even if
their residual happens to drop below the Huber threshold during
IRLS convergence |
| **v0.2.277** | 2026-04-24 | Robotics: Huber robust-cost kernel for `pgs.nr` (`pgs_optimize_huber`).
Down-weights outlier loop-closure edges via iteratively-reweighted
least squares so a single bad measurement can't dominate the
optimization. Standard back-end-robust SLAM technique |
| **v0.2.276** | 2026-04-24 | Robotics: Monte Carlo reachability mapper (`reach`) for arm
robots. Samples N joint configurations uniformly within per-joint
bounds, runs a user-supplied forward-kinematics callback on each,
stores the resulting end-effector positions as a workspace cloud |
| **v0.2.275** | 2026-04-24 | Robotics: 2D Visibility Graph planner (`vgraph`). Builds a graph
whose vertices are the start, the goal, and every vertex of every
convex polygonal obstacle; connects pairs whose direct segment is
collision-free; runs Dijkstra. Optimal-path baseline against
sampling planners (RRT/PRM) for known polygonal worlds |
| **v0.2.274** | 2026-04-24 | Robotics: pinhole camera with Brown-Conrady distortion (`cam`).
Project world points → pixels and unproject pixels + depth →
world points. Standalone utility complementing `ba.nr`'s built-in
projection (which is fixed at pure pinhole and tied to the BA
solver state) |
| **v0.2.273** | 2026-04-24 | Robotics: Model Predictive Path Integral control (`mppi`) —
Williams, Aldrich & Theodorou 2016/2017. Sample-based MPC variant
that requires no gradient and handles arbitrary non-smooth
dynamics + costs (binary obstacle indicators, friction stick-slip,
hybrid systems) where iLQR / DDP would fail |
| **v0.2.272** | 2026-04-24 | Robotics: Dynamic Time Warping (`dtw`) sequence-similarity
distance for n-dimensional time series (Sakoe & Chiba 1978).
Tolerant of time-axis stretches that confuse Euclidean point-to-
point comparison — useful for trajectory tracking error, gesture
matching, and learning-from-demonstration matching |
| **v0.2.271** | 2026-04-24 | Robotics: natural cubic spline (`cspline`) — smooth interpolation
through waypoints with continuous first and second derivatives at
every internal waypoint and `y''(x_0) = y''(x_N) = 0` boundary
conditions |
| **v0.2.270** | 2026-04-24 | Robotics: per-DOF admittance controller (`admit`). Maps measured
force to a position command via a virtual mass-spring-damper —
foundational for compliant manipulation on position-controlled
robots |
| **v0.2.269** | 2026-04-24 | Robotics: ZMP tracking via cart-table inverse dynamics + finite-
horizon LQR (`zmp`) — Kajita's bipedal locomotion CoM trajectory
generator. Given a desired ZMP reference (typically inside the
support polygon), inverts the cart-table dynamics to produce the
CoM trajectory whose induced ZMP best tracks the reference |
| **v0.2.268** | 2026-04-24 | Robotics: 2-D Signed Distance Field (`sdf`) on a regular grid
with bilinear interpolation + gradient. Foundational primitive
for collision queries, repulsive potential fields, cost-aware
planning, and distance-aware MPC stage costs |
| **v0.2.267** | 2026-04-24 | Robotics: box-constrained iLQR (`cilqr`). Same iterative-LQR
algorithm as `ilqr.nr` with one addition — each control update
during the forward pass is clamped to a per-component box
`[u_min, u_max]`. Standard pragmatic way to enforce hard actuator
limits |
| **v0.2.266** | 2026-04-24 | Robotics: jerk-limited 7-phase s-curve trajectory (`scurve`).
Production-grade motion-control profile that bounds velocity,
acceleration, AND jerk — eliminating the δ-spike acceleration
jumps of `topp.nr`'s trapezoidal profile that cause robot
lurching, vibration, and reduced joint lifetime |
| **v0.2.265** | 2026-04-24 | Robotics: quaternion utilities (`qutil`) on raw `double[4]`
buffers. Complement to `kinematics.nr`'s allocated-handle Quat;
this rod takes pointers directly so inner loops (controllers,
trajectory interpolation) can avoid per-call allocation |
| **v0.2.264** | 2026-04-24 | Robotics: hierarchical (strict-priority) whole-body controller
(`hwbc`) via Siciliano-Slotine null-space projection (Siciliano &
Slotine 1991). Strict counterpart to `wbc.nr` — lower-priority
tasks live entirely in the null space of higher-priority Jacobians
and can never undo a higher-priority objective |
| **v0.2.263** | 2026-04-24 | Robotics: trapezoidal time-parameterization (`topp`) for an arc-
length path. Given path length `L`, max velocity `v_max`, and max
acceleration `a_max`, computes the minimum-time `s(t)` profile
with `\|ṡ\| ≤ v_max` and `\|s̈\| ≤ a_max`. Falls back to triangular
profile (peak `< v_max`) when the path is too short for cruise |
| **v0.2.262** | 2026-04-24 | ### Algorithm

```
g(s)   — current best known cost-to-goal
rhs(s) — one-step look-ahead from neighbors
key(s) = (min(g, rhs) + h(s, s_start) + km,  min(g, rhs))

ComputeShortestPath:
  while top of U has key < key(s_start) OR s_start inconsistent:
    pop u
    if u stale (popped key < current key): re-push with current key
    elif g(u) == rhs(u): skip (lazy-heap stale entry, cell is consistent)
    elif g(u) > rhs(u):  g(u) = rhs(u);  update predecessors
    else (g(u) < rhs(u)): g(u) = ∞;  update u + predecessors
```

Edge cost: `max(c[s], c[s']) · (√2 if diagonal else 1)`. Heuristic:
octile distance (admissible AND consistent for 8-connected
uniform grids).

Implementation uses a lazy binary min-heap (no decrease-key /
delete operations) — duplicates are pushed and the pop side
detects stale entries.

### Surface

```nucleor
import "stdlib/rods/dstar.nr"

let h = dstar_new(W, H);
// ... set per-cell costs (negative = obstacle / ∞) ...
dstar_set_start(h, sx, sy);
dstar_set_goal(h, gx, gy);
dstar_plan(h);
// path:
for i in 0..dstar_path_len(h) {
    let x = dstar_path_x(h, i);
    let y = dstar_path_y(h, i);
}

// World changes — incremental replan:
dstar_update_cost(h, x, y, new_c_b);    // call repeatedly
dstar_replan(h);                         // O(perturbed cells)
```

### Verification

20×20 grid, three direct C tests:

1 |
| **v0.2.261** | 2026-04-24 | Robotics: AHRS via Mahony quaternion complementary filter
(`ahrs`). IMU orientation estimation — fuses gyro angular rates
with accelerometer gravity vector to maintain a body→world
quaternion. Mahony, Hamel & Pflimlin 2008 |
| **v0.2.260** | 2026-04-24 | Robotics: receding-horizon MPC wrapper (`mpc`) around iLQR.
Standard MPC pattern — observe state, plan over horizon T (warm-
started from previous tick's solution), execute u_0, shift the
warm-start sequence, tick again. Thin persistent layer over the
existing iLQR runtime |
| **v0.2.259** | 2026-04-24 | The new rod's user-facing surface is `pidc_new`, `pidc_set_gains`,
`pidc_set_integral_clamp`, `pidc_set_output_clamp`, `pidc_reset`,
`pidc_step`, `pidc_integral`, `pidc_last_error`, `pidc_free`. The
"c" suffix denotes the |
| **v0.2.258** | 2026-04-24 | Robotics: discrete infinite-horizon LQR (`lqr`) via Riccati
iteration. Linear-time-invariant systems with quadratic cost get a
static optimal feedback gain `u = −K · x`. Complement to `ilqr.nr`
and `ddp.nr` (both finite-horizon nonlinear) |
| **v0.2.257** | 2026-04-24 | Robotics: classic PID controller (`pid`) with anti-windup
integral clamp + output clamp. Foundational SISO controller — the
single missing primitive in the controls set (between bang-bang
and full model-based control like iLQR/WBC) |
| **v0.2.256** | 2026-04-24 | Robotics: whole-body / multi-task controller (`wbc`) — weighted
QP that simultaneously realizes a stack of task-space objectives
(end-effector velocities, CoM motion, posture regularization) by
solving for joint velocities at each control tick |
| **v0.2.255** | 2026-04-24 | Robotics: box LCP solver via Projected Gauss-Seidel (`lcp`). The
iteration that underlies MuJoCo, Bullet, ODE, and most other rigid-
body contact solvers. Solves |
| **v0.2.254** | 2026-04-24 | Open terrain |
| **v0.2.253** | 2026-04-24 | Robotics: minimum-snap polynomial trajectory generator (qtraj)
for quadrotors and other differentially-flat systems (Mellinger &
Kumar 2011). Quadrotor dynamics are differentially flat in
(x, y, z, yaw); a smooth trajectory in those four outputs uniquely
determines the full 12-state + 4-input sequence |
| **v0.2.252** | 2026-04-24 | Robotics: Bundle adjustment (visual SLAM back-end). Jointly
refines a set of camera SE(3) poses and a set of 3D world points
to minimize the sum of squared 2-D reprojection errors over a list
of (camera, point, pixel) observations. Pinhole camera model with
shared focal length + principal point |
| **v0.2.251** | 2026-04-24 | Robotics: 3D KD-tree (`kdt`) for fast nearest-neighbor queries
that adapt to non-uniform point distributions. Median-split on
alternating axes + bounding-box pruning. O(log n) average-case NN
with proper splitting-plane pruning + heap-based k-NN |
| **v0.2.250** | 2026-04-24 | Robotics: 3D pose-graph SLAM (SE(3) Gauss-Newton). Three-dimensional
generalization of `pgs.nr`. Each pose is an SE(3) element — translation
in ℝ³ + rotation in SO(3) stored as a unit quaternion. Edges encode
relative-pose measurements with diagonal information weights (3
translational + 3 rotational per edge) |
| **v0.2.249** | 2026-04-24 | Robotics: Differential Dynamic Programming (Mayne 1966; Jacobson
& Mayne 1970). Second-order extension of iLQR — includes the full
dynamics-Hessian contractions in the Q-function update for
quadratic local convergence (vs iLQR's superlinear) |
| **v0.2.248** | 2026-04-24 | **Robotics: Informed RRT* (Gammell, Srinivasa & Barfoot 2014) |
| **v0.2.247** | 2026-04-23 | Robotics: Lazy PRM (Bohlin & Kavraki 2000). Build the roadmap
WITHOUT collision-checking edges; validate edges on-demand during
the query. For dense roadmaps with sparse queries, much faster
than eager `prm_build` because most edges are never traversed |
| **v0.2.246** | 2026-04-23 | Robotics: CCD capsule vs static AABB. Closes the CCD pair
matrix alongside sphere-sphere (v0.2.196), capsule-capsule
(v0.2.201), and sphere-AABB (v0.2.201). Common scenario: arm-
link sweep past an environment box |
| **v0.2.245** | 2026-04-23 | Robotics: spatial hash grid for fast 3D nearest-neighbor
queries. Bins points into cubic cells of side `cell_size`; NN
queries scan only the query cell + its 26 neighbors instead of
all points. Roughly O(1) average-case lookup on uniformly-
distributed clouds — order-of-magnitude speedup over the brute-
force O(N) NN in `icp.nr` and `prm.nr` for large workloads |
| **v0.2.244** | 2026-04-23 | Robotics: behavior trees — THE standard pattern for modern
robot behavior orchestration (Colledanchise & Ögren 2018). Tree
of composite + decorator + leaf nodes ticked from the root each
control cycle. Foundation for pick-and-place sequencing, reactive
obstacle avoidance, task scheduling, error recovery, and any
structured multi-step behavior with long-running actions |
| **v0.2.243** | 2026-04-23 | Robotics: 2D polygon-polygon collision via Separating Axis
Theorem. Foundation for 2D mobile-robot footprint collision (AGV
shape vs static obstacle polygons), planar gripper-jaw checks,
board-game piece overlap. Complements the 3D GJK + EPA shipped
earlier with a lower-dimensional, faster primitive for inherently-
2D scenarios |
| **v0.2.242** | 2026-04-23 | Robotics: cubic Bezier curves in the trajectory rod. Pairs with
Catmull-Rom (v0.2.237): Catmull-Rom passes through every waypoint
exactly (good for path-following); Bezier offers explicit tangent
control at endpoints via the inner control points (good for
smooth merges with existing trajectories, specified-velocity
approach paths) |
| **v0.2.241** | 2026-04-23 | Robotics: artificial potential field (Khatib 1986). Simplest
reactive controller — attractive force to goal + repulsive force
from obstacles. Common baseline alongside DWA / pure pursuit.
Closes the reactive controller suite |
| **v0.2.240** | 2026-04-23 | Robotics: Dynamic Window Approach (Fox, Burgard & Thrun 1997)
local planner. Standard ROS-style local navigation controller for
differential-drive robots. Selects the best (v, ω) command at
each control step by sampling within the dynamic window of
velocities reachable in one step given the robot's acceleration
limits, rolling out each candidate trajectory, scoring by
(obstacle clearance + heading + velocity), and picking the best |
| **v0.2.239** | 2026-04-23 | Robotics: dense 3D voxel grid for occupancy. Complements
`octree.nr` (sparse): dense storage is faster than the octree
for cluttered scenes (no tree traversal per query) but memory-
intensive for sparse scenes. Foundation for inflated obstacle
costmaps, voxel-based collision detection in tightly-packed
cells, signed-distance-field caching |
| **v0.2.238** | 2026-04-23 | Robotics: 2D convex hull (Andrew's monotone chain). Standard
geometry primitive: O(N log N) sort-then-scan. Foundation for
grasp wrench space construction (convex hull of contact-force
generators), object bounding-shape extraction from depth scans,
collision broad-phase precomputation (replace mesh with hull for
cheaper queries) |
| **v0.2.237** | 2026-04-23 | Robotics: Catmull-Rom spline interpolation in the trajectory rod.
Standard C¹ smoothing primitive that passes through every
waypoint exactly with continuous tangent. Common workflow: run
RRT/PRM to get a discrete path → smooth with Catmull-Rom →
time-parameterize with TOPP |
| **v0.2.236** | 2026-04-23 | Robotics: stereo triangulation — recover a world-frame 3D point
from two camera views of it. Closes the vision rod: pinhole
projection (v0.2.224) + IBVS (v0.2.225) + PnP (v0.2.233) + now
the inverse direction (3D from 2D × 2). Foundation for stereo
SLAM landmark reconstruction, 3D measurement from stereo rigs,
and structure-from-motion |
| **v0.2.235** | 2026-04-23 | Robotics: particle filter (sequential Monte Carlo) for nonlinear,
non-Gaussian state estimation. Represents the posterior by a swarm
of weighted particles; foundation for kidnapped-robot localization
(multi-modal posterior over the entire map until enough
observations narrow it down), strongly nonlinear tracking where
EKF/UKF fail, and Bayesian filtering on non-vector state spaces |
| **v0.2.234** | 2026-04-23 | Robotics: Unscented Kalman Filter — sigma-point Bayesian
estimator for nonlinear systems where the EKF's Jacobian
linearization doesn't capture the local geometry well. Standard
alternative to EKF in robotics state estimation. Same callback
contract as `ekf.nr` (no API changes; user can swap EKF ↔ UKF
based on which works better for their problem) |
| **v0.2.233** | 2026-04-23 | Robotics: Perspective-n-Point camera pose estimation. Given N
known 3D points and their observed 2D pixel projections plus the
camera intrinsics, find the camera pose (R, t) that minimizes
reprojection error. Foundation for object pose estimation
(against a known CAD-model point cloud), visual odometry
(track 3D landmarks across frames), AR markers / fiducials
(estimate camera pose from corner pixels), and hand-eye
calibration target tracking |
| **v0.2.232** | 2026-04-23 | Robotics: hand-eye calibration — solve `AX = XB` for the unknown
end-effector → camera transform. Foundation for any vision-in-the-
loop robot control where the camera is mounted on the end-effector
(eye-in-hand visual servoing, object pose estimation, eye-in-hand
tracking). The result `X` is what relates the camera observation
frame to the robot's kinematic chain |
| **v0.2.231** | 2026-04-23 | Robotics: RANSAC outlier-robust 3D plane fitting (Fischler &
Bolles 1981 + PCA refit). Foundational perception primitive used
throughout robotics: LiDAR ground-plane detection (segment ground
from obstacles), depth-scan surface fitting (table tops, walls),
point-cloud preprocessing for ICP (filter outliers before
registration) |
| **v0.2.230** | 2026-04-23 | Robotics: Stanley path-following controller (Hoffmann et al.
2007). Classic alternative to pure pursuit, originally from
Stanford's DARPA Grand Challenge entry "Stanley". Tracks the
path itself rather than a lookahead point — snappier on straights,
makes pure pursuit + Stanley a natural pair to switch between
based on path curvature |
| **v0.2.229** | 2026-04-23 | Robotics: pure pursuit path-following controller. Classical
geometric controller for differential-drive (AGVs, AMRs,
two-wheeled balancing robots) and car-like (Ackermann-steered)
ground vehicles. Foundation for autonomous mobile robot control,
outdoor delivery robots, autonomous vehicles, and warehouse
transports |
| **v0.2.228** | 2026-04-23 | Robotics: 2D pose graph SLAM optimizer (Gauss-Newton). Standard
SLAM back-end: minimize the sum of squared edge errors over a
graph of relative-pose constraints. Foundation for SLAM systems
where the front-end (scan matching with `icp.nr`, visual odometry,
IMU dead-reckoning) produces relative-pose measurements between
poses, and loop closures fix accumulated drift via additional
edges between non-adjacent poses |
| **v0.2.227** | 2026-04-23 | Robotics: Extended Kalman Filter for nonlinear state
estimation. Foundation for sensor fusion (IMU + odometry + GPS),
SLAM front-ends, model-based observers, and any time-varying
state estimation. User supplies dynamics + measurement callbacks;
EKF handles all the linearization and Bayesian update math |
| **v0.2.226** | 2026-04-23 | Robotics: sparse octree for 3D occupancy grids and broad-phase
collision pruning. Foundation for occupancy-grid SLAM (octomap-
style probabilistic mapping from depth-camera / LiDAR streams),
voxel-based collision detection in cluttered scenes, and broad-
phase collision pruning against a precomputed octree of the
static environment |
| **v0.2.225** | 2026-04-23 | Robotics: image-based visual servoing (IBVS) — Chaumette &
Hutchinson 2006. Standard IBVS control law that maps observed
image-feature errors to a camera-frame Cartesian velocity command.
Closes the perception-to-control loop: vision rod (v0.2.224)
projects → IBVS computes camera velocity → robot Jacobian
pseudoinverse maps to joint commands |
| **v0.2.224** | 2026-04-23 | Robotics: pinhole camera projection rod. Standard
intrinsics + world-to-pixel projection + image (interaction)
Jacobian for visual servoing. Foundation for hand-eye calibration,
object pose estimation, and IBVS (which lands as v0.2.225 next) |
| **v0.2.223** | 2026-04-23 | Robotics: ICP — Iterative Closest Point for 3D point cloud
alignment. Foundation for robotics perception (LiDAR / depth
camera / RGBD scan matching, object pose estimation against CAD
models, SLAM scan-matching front-end). Uses Horn 1987's quaternion-
based closed-form solution for the per-iteration optimal rotation
step + brute-force nearest-neighbor matching |
| **v0.2.222** | 2026-04-23 | Robotics: iLQR — iterative Linear Quadratic Regulator (Tassa
et al. 2012). Locally-optimal nonlinear trajectory optimizer
that's the workhorse inner loop of model predictive control
(MPC) and direct shooting trajectory optimization. Closes the
optimal-control gap in Nucleor's robotics rod stack |
| **v0.2.221** | 2026-04-23 | 0.310000 |
| **v0.2.220** | 2026-04-23 | See the bundled v0.2.221 entry below — PRM A* query (v0.2.220) |
| **v0.2.219** | 2026-04-23 | Robotics: per-joint friction model in dynamics rod (v0.2.218) +
operational-space inverse dynamics (Khatib-style, v0.2.219). Two
related dynamics extensions that close the gap between the model
and real hardware behavior |
| **v0.2.218** | 2026-04-23 | See the bundled v0.2.219 entry below — joint friction (v0.2.218) |
| **v0.2.217** | 2026-04-23 | Robotics: CHOMP — gradient-based trajectory optimizer (Ratliff
et al. 2009). Smooths an initial discretized path while pushing
it away from obstacles via a user-supplied cost function. The
standard "post-process the planner's output before sending to the
controller" tool — turns RRT / PRM's discretely-collision-free
but jerky paths into smooth executable trajectories |
| **v0.2.216** | 2026-04-23 | Robotics: grasp quality metrics for a 2-finger parallel-jaw
gripper. New `grasp` rod with three foundational metrics:
antipodal score, force closure under Coulomb friction, and
approach-vector alignment. Foundation for grasp synthesis (sample
candidate grasps, score them, pick the best) and for control-time
validation (reject planned grasps that the friction model says
will slip) |
| **v0.2.215** | 2026-04-23 | Robotics: two model-based controllers — joint-space computed-
torque (v0.2.214) for trajectory tracking, and 6-DOF Cartesian
impedance (v0.2.215) for full-pose contact-rich manipulation.
Together with v0.2.212's 3-DOF impedance + tip wrench, the
dynamics rod now ships every standard model-based controller a
robotics user would reach for |
| **v0.2.214** | 2026-04-23 | See the bundled v0.2.215 entry below — joint-space computed- |
| **v0.2.213** | 2026-04-23 | Robotics: extend the end-to-end showcase to 10 stages, adding
RNEA gravity-compensation and Cartesian impedance demonstrations.
Verify gate updated to expect "10 stages" in the run output |
| **v0.2.212** | 2026-04-23 | Robotics: Cartesian impedance / operational-space PD controller
+ inverse dynamics with applied tip wrench. Two related dynamics
extensions that round out the model-based control surface for
contact-rich manipulation |
| **v0.2.211** | 2026-04-23 | Robotics: mass matrix `M(q)` extraction and forward dynamics —
the second half of the dynamics rod started in v0.2.207. Inverse
dynamics ("given motion, compute torques") was the foundation;
this ship adds the inverse direction ("given torques, compute
motion") plus the explicit `M(q)` matrix that's foundational for
impedance/admittance control, operational-space control, and
kinetic-energy analysis |
| **v0.2.210** | 2026-04-23 | ```
q̇ = J⁺·e_pos + (I − J⁺·J)·w·(q_pref − q)
```

The secondary task here is joint-space posture preference (drive q
toward a desired `q_pref`). Useful for joint-limit avoidance
(`q_pref` = midpoint of each joint's range), solution-branch
selection on a 7-DOF arm, and continuity across waypoints
(`q_pref` = previous solve's output).

### Surface

```nucleor
import "stdlib/rods/ik_dls.nr"

ik_dls_solve_nullspace(
    chain, vars,
    tx_b, ty_b, tz_b,
    q_pref_ptr, secondary_weight_b,    // 0 disables secondary task
    100, tol_b, damping_b);
```

`q_pref_ptr` is a `double[n_joints]` handle. `secondary_weight_b`
scales the secondary task; 0 disables it (degenerates to the base
DLS solver), 0.1-0.5 is typical.

### Verification

4-DOF planar arm (3 revolute + 1 fixed tip) with target (2, 1, 0)
from a perturbed initial guess `q = (1, 0.5, -0.5, 0.3)` and
`q_pref = (0, 0, 0, 0)`:

\| Solver \| EE position \| Σ\\|q\\| \|
\|---\|---\|---\|
\| Base DLS  \| (2.000, 1.000) — exact \| |
| **v0.2.209** | 2026-04-23 | Robotics: workspace sampling primitives. `fk_workspace_sample`
takes a serial chain + per-joint `[lo, hi]` bounds and writes N
end-effector world positions to a caller-allocated buffer by
running FK on uniformly-random configurations. `fk_workspace_aabb`
computes the axis-aligned bounding box; `fk_workspace_aabb_volume`
the box volume. Useful for "what can this arm reach?" estimation,
reachable-set visualization, and seeding workspace-coordinate
IK / RRT initialization |
| **v0.2.208** | 2026-04-23 | **Robotics: Yoshikawa manipulability metric `√det(J·Jᵀ)` at an |
| **v0.2.207** | 2026-04-23 | Robotics: robot inverse dynamics via the Recursive Newton-Euler
Algorithm (RNEA, Luh-Walker-Paul 1980). Given joint positions q,
velocities qd, and accelerations qdd, computes the joint torques
required to produce that motion against gravity: |
| **v0.2.206** | 2026-04-23 | Robotics: end-to-end showcase exercising the full v0.2.174-205
stack. `examples/showcase/robotic_arm.nr` now runs an 8-stage
integration that touches every robotics rod the OSS distribution
ships, in the order a real motion-planning pipeline would: build
chain → FK → IK (with limits + singularity readout) → TOPP
→ collision (sphere/AABB/sphere-sphere) → BVH workspace |
| **v0.2.205** | 2026-04-23 | **Robotics: convex-mesh GJK + EPA. Convenience entry points for |
| **v0.2.204** | 2026-04-23 | Robotics: URDF (Unified Robot Description Format) parser. Reads
URDF source as a NUL-terminated text buffer, extracts joint
attributes (type, origin xyz/rpy, axis, limits), and constructs an
FK chain via `urdf_to_fk_chain` for direct use with the IK solver
(v0.2.176, .193, .194, .199), motion planners (v0.2.179-200), and
trajectory parameterization (v0.2.181-203). Closes the v0.5
robotics roadmap — the rod stack now spans URDF input → FK →
collision → motion planning → IK → trajectory parameterization |
| **v0.2.203** | 2026-04-23 | Robotics: TOPP — time-optimal path parameterization. Given a
piecewise-linear path through joint space and per-joint velocity +
acceleration bounds, computes the minimum-time time-parameterization
(s ↦ t) that respects the bounds. Standard forward + backward pass
on the squared path velocity b(s) = (ds/dt)², per Pham 2014's
TOPP-RA algorithm — simplified for piecewise-linear paths |
| **v0.2.202** | 2026-04-23 | Robotics: GJK EPA — penetration depth + contact normal for two
overlapping convex shapes. Once GJK reports overlap (v0.2.184),
the Expanding Polytope Algorithm (Van den Bergen 2001) iteratively
expands the GJK terminating tetrahedron until it finds the face
nearest the origin on the Minkowski-difference polytope. The
distance from the origin to that face is the penetration depth;
the face normal is the minimum-translation direction to separate
the shapes |
| **v0.2.201** | 2026-04-23 | Robotics: two more CCD (continuous collision detection) pairs.
`coll_ccd_capsule_capsule` and `coll_ccd_sphere_aabb` extend the
swept-sphere-sphere primitive (v0.2.196) to the two most-asked-for
moving-body cases: two moving capsules vs each other, and a moving
sphere vs a static AABB. Same surface as v0.2.196 — return earliest
collision time t ∈ [0, 1] as bit-cast f64; -1.0 if clear |
| **v0.2.200** | 2026-04-23 | Robotics: PRM Dijkstra query. The probabilistic roadmap rod
finally ships its query side — `prm_query` connects start and goal
configurations to the precomputed roadmap and runs Dijkstra from
start to goal. Multiple queries can run against the same roadmap
without rebuilding, which is the entire reason to use PRM over
RRT. Read the resulting path back via `prm_path_len` /
`prm_path_node` / `prm_path_at` |
| **v0.2.199** | 2026-04-23 | Robotics: singularity detection in IK solver. The damped least
squares solver tracks the smallest `\|det(J·Jᵀ + λ²I)\|` observed
during a solve and exposes it via `ik_get_last_singularity_metric`.
A small value (~1e-9 or below) flags that the chain approached a
singular configuration where the Jacobian is rank-deficient — the
caller can use this to back off, try a different goal, or switch
to a more aggressive damped strategy |
| **v0.2.198** | 2026-04-24 | Robotics: goal-region planning in RRT. Sample uniformly inside
a per-dimension `[lo, hi]` acceptable region instead of converging
on a single goal point. Useful when the target pose is approximate
or has a tolerance bubble |
| **v0.2.197** | 2026-04-24 | Robotics: sphere-OBB (Oriented Bounding Box) cross-pair.
Closes the v0.5 collision-pair matrix |
| **v0.2.196** | 2026-04-24 | Robotics: Continuous Collision Detection (CCD) for swept
sphere-sphere. Reports the earliest collision time t ∈ [0, 1]
of two moving spheres over a unit time interval — so fast-
moving objects don't tunnel through each other in discrete-
time simulation |
| **v0.2.195** | 2026-04-24 | Robotics: collision cross-pairs — sphere-AABB and capsule-
AABB. Fills out the collision matrix so every primitive pair
in the v0.5 list has a narrow-phase test |
| **v0.2.194** | 2026-04-24 | Robotics: 6-DOF orientation IK. New `ik_dls_solve_6d`
extends the v0.2.176 position-only solver to a position +
orientation target. Closes the IK roadmap item from the v0.5
list |
| **v0.2.193** | 2026-04-24 | Robotics: per-joint min/max limits in IK solver. Set once
per chain, applied as clamping during every solve iteration |
| **v0.2.192** | 2026-04-24 | Robotics: Dynamic Movement Primitives (DMPs) added to
`trajectory.nr`. Learnable trajectories that generalize across
goal positions — foundation for imitation learning and skill
transfer |
| **v0.2.191** | 2026-04-24 | Robotics: S-curve (bounded-jerk) trajectory profile added to
`trajectory.nr`. Respects velocity, acceleration, AND jerk
limits simultaneously — smoother than trapezoidal, less
residual vibration |
| **v0.2.190** | 2026-04-24 | Best parent selection |
| **v0.2.189** | 2026-04-24 | Documentation: `docs/release-notes-v0.2.x-robotics.md` adds a
single-page summary of the v0.2.174-v0.2.188 robotics stack arc
suitable for blog posts, release announcements, and reviewer
overview |
| **v0.2.188** | 2026-04-24 | Robotics: RRT-Connect (Kuffner & LaValle 2000) — bidirectional
RRT, typically 5-10× faster than vanilla RRT on hard problems |
| **v0.2.187** | 2026-04-24 | Robotics integration showcase: `examples/showcase/robotic_arm.nr`
composes 5 of the v0.2.174-186 robotics rods (kinematics +
fk_chain + trajectory + collision + bvh) into a single working
program. Proves the rods compose end-to-end |
| **v0.2.186** | 2026-04-24 | 259 / 259 PASS, 0 SKIP |
| **v0.2.185** | 2026-04-24 | Robotics: PRM (Probabilistic Roadmap) multi-query motion
planner. Complement to v0.2.179 RRT (single-query) |
| **v0.2.184** | 2026-04-24 | Robotics: BVH (Bounding Volume Hierarchy) for broad-phase
collision pruning. Companion to v0.2.183 GJK narrow-phase |
| **v0.2.183** | 2026-04-24 | Robotics: GJK convex-convex collision. Generic shape support
via user-supplied support functions. Industry-standard convex
collision algorithm |
| **v0.2.182** | 2026-04-24 | Robotics: two more v0.5-list items shipped — RRT path
shortcutting + DH-parameter constructor for fk_chain |
| **v0.2.181** | 2026-04-24 | Robotics: trapezoidal velocity profile added to `trajectory.nr`.
First v0.5-list item shipped |
| **v0.2.180** | 2026-04-24 | v0.5.0, v0.6.0, v0.7.0, v0.8.0 milestone trackers drafted.
Roadmap is now concrete through 2028-Q1 |
| **v0.2.179** | 2026-04-24 | Robotics: motion planner. New `rrt.nr` rod ships LaValle's
RRT (Rapidly-exploring Random Tree). Closes the kinematics +
collision + planning loop — the full motion-planner stack is
now in the standard library |
| **v0.2.178** | 2026-04-24 | Robotics: collision primitives. New `collision.nr` rod ships
sphere-sphere, sphere-capsule, capsule-capsule, and AABB-AABB
overlap tests. Foundation for collision-aware motion planning
(v0.5) |
| **v0.2.177** | 2026-04-24 | Robotics: time-parameterized trajectories. New `trajectory.nr`
rod ships quintic (5th-order polynomial) joint trajectories with
C² boundary conditions. Third robotics ship |
| **v0.2.176** | 2026-04-24 | Robotics: inverse kinematics. New `ik_dls.nr` rod solves for
joint variables that put the end-effector at a target position.
Damped Least Squares (Wampler 1986, Buss 2009) |
| **v0.2.175** | 2026-04-24 | Robotics: forward kinematics for serial joint chains. New
`fk_chain.nr` rod composes per-joint poses to compute every
link's world-frame pose. Compute backend for RFC-0013 (URDF
static frame chain verification, v0.5) |
| **v0.2.174** | 2026-04-24 | Robotics math foundation: new `kinematics.nr` rod with
Vec3, quaternion, and Pose primitives. RFC-0003 (typed
coordinate frames) compute path now ready; the type-system
overlay lands in v0.4 once Nucleor has generics |
| **v0.2.173** | 2026-04-24 | `sb_new_with_cap(initial_cap)` builtin: pre-size a string
builder when the final size is known. IR builder now starts
at 2 MB instead of 256 B (saves ~14 reallocs per self-host
compile) |
| **v0.2.172** | 2026-04-24 | `sb_append_char` builtin: append a single byte to a string
builder without allocating a temp string. Cuts ~73K
transient allocations from the s1 self-host (escape_llvm_str
hot path) |
| **v0.2.171** | 2026-04-24 | Memory-fix Ship 7: tools-suite gets its own 200 MB
allocation budget (was un-gated). Both compilers now
regression-protected |
| **v0.2.170** | 2026-04-24 | Documentation: `docs/release-notes-v0.2.x-memory.md` adds a
single-page summary of the v0.2.158-v0.2.169 memory-fix arc
suitable for blog posts, release announcements, and reviewer
overview |
| **v0.2.169** | 2026-04-24 | RFC-0030 phase 4: `str_free` exposed as a builtin.
Foundation for explicit-free patterns in user code and for
the Ship 4 type-checker arena migration |
| **v0.2.168** | 2026-04-23 | Documentation: `docs/memory-architecture.md` adds a complete
case-study writeup of the v0.2.158-v0.2.167 memory work for new
contributors. Linked from README |
| **v0.2.167** | 2026-04-23 | Vec initial capacity tuned: 16 → 4 elements drops vec_new
from 119 MB to 49 MB (-70 MB). Total peak: 137 → 67 MB.
Cumulative reduction from v0.2.157 baseline: 19 GB → 67 MB
= 283× reduction. Budget tightened to 100 MB |
| **v0.2.166** | 2026-04-23 | SB initial capacity tuned: 4 KB → 256 B drops sb_new from
60 MB to 11 MB on the s1 self-host (49 MB saved). Total
peak: 185 → 137 MB. Budget tightened to 250 MB |
| **v0.2.165** | 2026-04-23 | RFC-0030 phase 1: per-compile string arena. Five new builtins
(`str_arena_new`, `str_arena_free`, `str_arena_bytes`,
`str_arena_concat`, `str_arena_substring`) for allocate-then-
free-all-at-once string lifetimes |
| **v0.2.164** | 2026-04-23 | RFC-0029 phase 1: identifier interner. New `str_intern(s)`
builtin returns a stable canonical pointer per unique input
string. Architectural building block for the v0.4 TypeId
interner and for any pointer-equality fast paths |
| **v0.2.163** | 2026-04-23 | Memory-fix Ship 6: env-snapshot UAF audit (item E4 from
MEMORY_FIX_PUNCHLIST.md). Type-pass env snapshots now free
properly; check-pass snapshots remain deferred with a
documented reason |
| **v0.2.162** | 2026-04-23 | Release-readiness polish: SECURITY.md added; README updated
with current rod count (132), helper count (686), test counts,
and the 4.5 s / 185 MB self-host metrics from v0.2.159 |
| **v0.2.161** | 2026-04-23 | Memory-fix Ship 5: gate enforces a 400 MB peak-allocation
budget on the s1 self-host compile. Locks in v0.2.159's 52×
memory reduction so future ships can't silently regress |
| **v0.2.160** | 2026-04-23 | Memory-fix Ship 2 part 2: convert remaining 13 cold-path
`str_eq(str_substring(...))` sites to `str_starts_with` /
`str_eq_at`. Eliminates the anti-pattern from the codebase
entirely |
| **v0.2.159** | 2026-04-23 | Memory-fix Ship 2 (part 1): non-allocating `str_eq_at` helper
replaces the dominant `str_eq(str_substring(source, i, i+tlen),
target)` source-scan anti-pattern. Self-host compile drops from
9.7 GB / 19 GB peak RSS / 25 s to 185 MB / 4.5 s — a 52× memory
reduction and 5.5× speedup |
| **v0.2.158** | 2026-04-23 | Memory-fix Ship 1: vec_free builtin, env-snapshot leak fix,
non-allocating string-prefix probes, allocation tracing
infrastructure. Plus RFC-0028 phase 4 (3 new format builtins).
Bundled with `MEMORY_FIX_PUNCHLIST.md` documenting the road
to a < 500 MB self-host compile |
| **v0.2.157** | 2026-04-23 | RFC-0001 phase 1: `#[no_alloc]` v1 — first RT attribute that
actually enforces. File-wide source-level static check. Fires
new diag code RT-001 on violation |
| **v0.2.156** | 2026-04-23 | Gate ergonomics: `KEEP_CACHE=1 bash tools/verify.sh` skips
the post-run wipe and reuses the module-graph cache on the
next run |
| **v0.2.155** | 2026-04-23 | v0.2.155 |
| **v0.2.154** | 2026-04-23 | RFC-0002 phase 1: bare arena builtins now actually link.
Closes the v0.2.150 footgun where `arena_new` etc. were
pre-declared by the s1 but had no runtime impl |
| **v0.2.153** | 2026-04-23 | v0.2.153 |
| **v0.2.152** | 2026-04-23 | `#[deny(CODE)]` promotes warnings to errors — sibling of v0.2.151
`#[allow]`. Item 4 fully closed at the v0.2 scope |
| **v0.2.151** | 2026-04-23 | Closes punchlist item 4: `#[allow(CODE)]` actually suppresses
warnings now (file-wide; per-fn scoping deferred to v0.4) |
| **v0.2.150** | 2026-04-23 | Closes v0.2.123 finding: 11 orphan runtime C files now have rod
wrappers + smokes. Rod count 121 → 132, gate 226 → 237 |
| **v0.2.149** | 2026-04-23 | Closes v0.2.124 finding: 22 orphan rod wrappers now have smoke
tests under the verify gate. Gate grew 204 → 226 |
| **v0.2.148** | 2026-04-23 | One more lockfile-name straggler missed by v0.2.147 — fixed |
| **v0.2.147** | 2026-04-23 | Filename drift: 8 docs + 1 tools-suite entry referenced
`nuc.toml` / `nuc.lock`, but the shipped compiler reads
`Nucleor.toml` / `Nucleor.lock` |
| **v0.2.146** | 2026-04-23 | Milestone tracker sub-chain header was undercounting post-RC
releases — 71 → 96 |
| **v0.2.145** | 2026-04-23 | Spec doc "Suppression" section described `#[allow]` / `#[deny]`
as a working feature — it isn't. Documented the current behavior +
v0.4 plan |
| **v0.2.144** | 2026-04-23 | Tab-completion scripts (all 4 shells) also missing 3 flags that
shipped CLI surfaces use |
| **v0.2.143** | 2026-04-23 | Tab-completion scripts missing 5 user-facing CLI commands across
all 4 shells; added |
| **v0.2.142** | 2026-04-23 | `examples/showcase/README.md` claimed "Not part of the verify
gate" — but `showcase_build_smoke` has been gated since v0.2.90 |
| **v0.2.141** | 2026-04-23 | Spec doc "Adding a new error code" recipe was 4 steps; we
actually use 6 — recipe brought into agreement with practice |
| **v0.2.140** | 2026-04-23 | Architecture doc "where to look in the source" table named 7
fns that don't exist — replaced with the actual names |
| **v0.2.139** | 2026-04-23 | Bootstrap contract clarifies the "748 vs 676 symbols" gotcha |
| **v0.2.138** | 2026-04-23 | Two test-suite README drifts: features/ dir missing from
top-level layout table, and quarantined-features count off by one |
| **v0.2.137** | 2026-04-23 | Four RFC "Target release" rows lagged behind the phased
implementation reality — brought into step with their own
Status rows + the index |
| **v0.2.136** | 2026-04-23 | v0.3.0 milestone tracker + cross-platform readiness doc — stale
gate-step counts and "as of vN" stamps brought current |
| **v0.2.135** | 2026-04-23 | RFC README footer paragraph undercounted the implemented RFCs
and carried a stale v0.2.124 stamp |
| **v0.2.134** | 2026-04-23 | Two RFCs reference diagnostic codes that don't exist in the spec
— renamed to match the shipped prefix |
| **v0.2.133** | 2026-04-23 | Three more docs caught with stale or mistaken roadmap claims —
brought into agreement with current RFC index |
| **v0.2.132** | 2026-04-23 | Mirror v0.2.131's 161-code spec catalog into milestone tracker,
status doc, and bootstrap contract |
| **v0.2.131** | 2026-04-23 | 3 more diagnostic codes were fired by the tools-suite type-check
pass but missing from spec + explain registry — wired in |
| **v0.2.130** | 2026-04-23 | Three more "as-of" markers in process docs + architecture brought
current |
| **v0.2.129** | 2026-04-23 | Migration guide + benchmarks doc — stale "as of v0.2.91 / v0.2.97"
markers brought current |
| **v0.2.128** | 2026-04-23 | Three stale numeric markers in user-facing docs (README + getting-
started) brought current |
| **v0.2.127** | 2026-04-23 | RFC-0030 (async decision) status synchronized + new "Decision"
status added to the legend |
| **v0.2.126** | 2026-04-23 | 11 RFC headers had `Status: Draft` while the index + milestone
tracker showed them as Implemented — drift class fixed |
| **v0.2.125** | 2026-04-23 | docs/rfcs/README.md cross-cutting-contracts table refresh — three
stale numbers from the v0.2.73 era brought current |
| **v0.2.124** | 2026-04-23 | Audit finding: 22 orphan rod wrappers document themselves as
v0.4 example-coverage targets |
| **v0.2.123** | 2026-04-23 | Audit finding: 11 orphan runtime C files document themselves as
v0.4 wrap targets |
| **v0.2.122** | 2026-04-23 | Mirror v0.2.121 to status doc + bootstrap contract |
| **v0.2.121** | 2026-04-23 | Milestone tracker refresh — capture the v0.2.99–120
diagnostic-code coverage closure as a sixth sub-chain bucket |
| **v0.2.120** | 2026-04-23 | One more code: TNT-001 (taint analysis) was the last
undocumented diagnostic |
| **v0.2.119** | 2026-04-23 | After v0.2.117 brought EXPECT-header coverage to 33/33 err
tests, audit of the codes referenced in those headers found
that |
| **v0.2.118** | 2026-04-23 | Lock down v0.2.117: gate step enforces every err test has an
EXPECT header |
| **v0.2.117** | 2026-04-23 | After v0.2.116 fixed `err_args` and `err_bool_arith`, 28 of
the 33 negative tests still had no `// EXPECT:` header. Added
the canonical 2-line header to each:

```
// EXPECT: <CODE> <text>
// <one-line description of what the test exercises>
```

Headers were captured by running each file through the
compiler and recording the first `error[CODE]:` /
`warning[CODE]:` line, then writing the EXPECT line + a
short prose description.

### Distribution of diagnostic codes added

- |
| **v0.2.116** | 2026-04-23 | zero header comments |
| **v0.2.115** | 2026-04-23 | Same v0.2.41/v0.2.42 off-by-one in `check_compiler_drift.sh`
header (which I introduced in v0.2.112) |
| **v0.2.114** | 2026-04-23 | Fix off-by-one ship attribution: drift-gate enforcement of
helper manifest shipped v0.2.42, not v0.2.41 |
| **v0.2.113** | 2026-04-23 | Mirror v0.2.112 to `tools/verify.ps1` header |
| **v0.2.112** | 2026-04-23 | Two tool-script headers stale at v0.1-era state |
| **v0.2.111** | 2026-04-23 | `gen_releases_index.py` docstring said "124 entries" — actual
is 179 |
| **v0.2.110** | 2026-04-23 | Bug fix: `gen_helper_manifest.py` docstring said "14 v0.2.39
taxonomy classes" — actual is 13 since v0.2.41 |
| **v0.2.109** | 2026-04-23 | Bug fix: `nuc.toml` `[features]` showcase comment listed
wrong example name |
| **v0.2.108** | 2026-04-23 | Bug fix: `docs/architecture.md` "around line 1700" stale —
actual line is 2004 |
| **v0.2.107** | 2026-04-23 | Audit found two POSIX-stub gaps in the runtime not flagged
in the v0.3 readiness doc |
| **v0.2.106** | 2026-04-23 | v0.2.0 milestone tracker — Helper Schema status block + 1
success criterion still at v0.2.76 era |
| **v0.2.105** | 2026-04-23 | v0.2.0 milestone tracker — two more stale claims inside
per-RFC checklist rows |
| **v0.2.104** | 2026-04-23 | Bug fix: spec doc claimed TST-001..003 are "proposed for v0.4"
— they were wired in v0.2.79 |
| **v0.2.103** | 2026-04-23 | v0.3.0 milestone tracker + v0.3 readiness status doc had four
stale claims (broken cross-tracker reference + 186-step gate) |
| **v0.2.102** | 2026-04-23 | `docs/milestones/v0.4.0.md` Helper Schema section was at v0.2.40
state — said Phase 2 was "DONE in v0.2.40" with 144 REVIEW
REQUIRED rows |
| **v0.2.101** | 2026-04-23 | `NUCLEOR_BOOTSTRAP_CONTRACT.md` refresh — was at v0.2.81 era;
the "tooling-only" claim was wrong by v0.2.84 |
| **v0.2.100** | 2026-04-23 | Status doc refresh — `v0.2-shipped-and-deferred.md` was 22
ships stale. v0.2.100 milestone crossed |
| **v0.2.99** | 2026-04-23 | Milestone tracker refresh — was 22 ships stale |
| **v0.2.98** | 2026-04-23 | `docs/benchmarks.md` self-host stats + `docs/rfcs/README.md`
RFC status both stale |
| **v0.2.97** | 2026-04-23 | `docs/rods-and-runtime.md` — same stale rod count drift as
README v0.2.88 |
| **v0.2.96** | 2026-04-23 | `docs/language-reference.md` v0.1 → v0.2 promotion + v0.1.5
audit corrigendum updated for the v0.2.x chain |
| **v0.2.95** | 2026-04-23 | Bug fix: docs claimed hex/binary literals don't work — they do |
| **v0.2.94** | 2026-04-23 | Top-level `CONTRIBUTING.md` "scope" section was 90+ releases
stale |
| **v0.2.93** | 2026-04-23 | Process docs refresh — `semver-and-release.md` and
`process/contributing.md` were stale |
| **v0.2.92** | 2026-04-23 | Migration guide refresh — `docs/migrations/v0.1-to-v0.2.md`
was 50+ releases stale |
| **v0.2.91** | 2026-04-23 | Mojibake sweep + gate enforcement. Self-defeating mojibake
in v0.2.90 / v0.2.58 CHANGELOG entries fixed |
| **v0.2.90** | 2026-04-23 | Bug fix: `examples/showcase/README.md` was referenced but
missing. Plus showcase build smoke + UTF-8 mojibake fix |
| **v0.2.89** | 2026-04-23 | Continue README freshness pass — fix two more `v1.1` stale
roadmap claims plus document `for-in` already shipped |
| **v0.2.88** | 2026-04-23 | README freshness pass — fix two stale claims |
| **v0.2.87** | 2026-04-23 | `nuc -V` and `nuc version` now work as aliases for
`--version`. Plus negative-test audit findings |
| **v0.2.86** | 2026-04-23 | Gate `--json` output across 11 CLI commands. 200-step gate
milestone crossed |
| **v0.2.85** | 2026-04-23 | Bug fix: `nuc registry list` leaked `cmd.exe` stderr |
| **v0.2.84** | 2026-04-23 | Bug fix: `nuc doc` and `nuc fix` were dispatched but missing
from `nuc help` |
| **v0.2.83** | 2026-04-23 | CHANGELOG ↔ git tag parity restored; drift gate now enforces |
| **v0.2.82** | 2026-04-23 | Bug fix: create the missing `NUCLEOR_BOOTSTRAP_CONTRACT.md`
file referenced by `nuc bootstrap` since v0.2.70 |
| **v0.2.81** | 2026-04-23 | Refresh `HELPER-CONTRACT.md` and `helper_manifest_schema.md`
for v0.2.41–78 Phase 2 reality |
| **v0.2.80** | 2026-04-23 | Wire 44 forward-looking error codes into the explain registry;
gate now exercises the full 130-code spec catalog |
| **v0.2.79** | 2026-04-23 | Bug fix: 4 v0.2 error codes spec'd but not wired into explain
registry. Plus a new gate step that enforces full v0.2 code-set
coverage going forward |
| **v0.2.78** | 2026-04-23 | Helper manifest Phase 2 — populate 15 stable `TensorOps`
helpers |
| **v0.2.77** | 2026-04-23 | Milestone tracker + status doc + RFC index refresh for the
v0.2.50–76 sub-chain |
| **v0.2.76** | 2026-04-23 | Helper manifest Phase 2 — populate `IO` (70), `DataCodec` (14
remaining), and 4 ToolingMeta legacy bridge entries |
| **v0.2.75** | 2026-04-23 | Helper manifest Phase 2 — populate `StringFormat` (81) and
`Collection` (54) with read-vs-mutate split |
| **v0.2.74** | 2026-04-23 | Helper manifest Phase 2 — per-name override mechanism + populate
PanickingArith (full) + 10 ToolingMeta/DataCodec entries |
| **v0.2.73** | 2026-04-23 | Helper manifest Phase 2 — populate effects/taint/proof for four
more taxonomy classes |
| **v0.2.72** | 2026-04-23 | Verify gate: diagnostic smoke step (policy/certify/translate/evidence/graph/perf/bench) |
| **v0.2.71** | 2026-04-23 | Verify gate: inspector smoke step (summary/audit/query/impact) |
| **v0.2.70** | 2026-04-23 | Verify gate: `nuc bootstrap`, `nuc check`, `nuc abi` smoke steps |
| **v0.2.69** | 2026-04-23 | `nuc test` bug fix — target/ now created before harness write |
| **v0.2.68** | 2026-04-23 | Verify gate: `nuc lock` smoke step (both bash + PowerShell) |
| **v0.2.67** | 2026-04-23 | Verify gate: `nuc doc` smoke step (both bash + PowerShell) |
| **v0.2.66** | 2026-04-23 | Verify gate: `nuc init` smoke step (both bash + PowerShell) |
| **v0.2.65** | 2026-04-23 | `tools/verify.ps1` mirrors v0.2.64 explain smoke step |
| **v0.2.64** | 2026-04-23 | Verify gate: `nuc explain` smoke step closes a real coverage gap |
| **v0.2.63** | 2026-04-22 | `bin/` housekeeping — README + explicit .gitignore for scratch binaries |
| **v0.2.62** | 2026-04-22 | `tools/verify.ps1` mirrors v0.2.61 non-empty stdout check |
| **v0.2.61** | 2026-04-22 | Verify gate: examples now checked for non-empty stdout |
| **v0.2.60** | 2026-04-22 | `tools/examples.list` — single source of truth for the verify-gate example list |
| **v0.2.59** | 2026-04-22 | `tools/verify.ps1` parity with `verify.sh` — Windows CI now actually gates the v0.2.x demos |
| **v0.2.58** | 2026-04-22 | Manifest generators: fix UTF-8 mojibake in rod descriptions |
| **v0.2.57** | 2026-04-22 | `RELEASES.md` — tag-only navigable index of all 124 releases |
| **v0.2.56** | 2026-04-22 | Language reference: stale "POSIX port planned for v0.2" reference fixed |
| **v0.2.55** | 2026-04-22 | CI workflow stale-warning fix + v0.3 phase 3 scaffolding documented |
| **v0.2.54** | 2026-04-22 | v0.3 milestone tracker: Phase 2 launcher work checked off |
| **v0.2.53** | 2026-04-22 | POSIX `nuc` launcher: full clang resolution chain + fail-fast errors (v0.3 phase 2 prep) |
| **v0.2.52** | 2026-04-22 | `docs/milestones/v0.3.0.md` — formal v0.3 milestone tracker |
| **v0.2.51** | 2026-04-22 | v0.3 cross-platform readiness audit (`docs/status/v0.3-cross-platform-readiness.md`) |
| **v0.2.50** | 2026-04-22 | v0.2.0 milestone tracker status line current — v0.2.x chain officially closed |
| **v0.2.49** | 2026-04-22 | Error codes spec: TST + DIAG namespaces documented as reserved-but-empty |
| **v0.2.48** | 2026-04-22 | Status snapshot refreshed — covers full v0.1.46 → v0.2.47 chain |
| **v0.2.47** | 2026-04-22 | Drift gate enforces rod manifest freshness — generalized helper checker |
| **v0.2.46** | 2026-04-22 | Rod manifest companion to the helper manifest (121 rods cataloged) |
| **v0.2.45** | 2026-04-22 | Verify gate: 186 / 186 — first all-PASS run, no skips |
| **v0.2.44** | 2026-04-22 | Top-level README size-claim sentence — replaced loose talk with verified counts |
| **v0.2.43** | 2026-04-22 | Helper manifest `since` field accurately tagged (132 rows updated) |
| **v0.2.42** | 2026-04-22 | Drift gate enforces helper manifest freshness (Helpers.md going-forward constraint) |
| **v0.2.41** | 2026-04-22 | Helper manifest audit — REVIEW REQUIRED count: 144 → 0 |
| **v0.2.40** | 2026-04-22 | Helpers.md contract Phase 2 — manifest + schema doc shipped |
| **v0.2.39** | 2026-04-22 | Migration guide refreshed for the v0.2.x stdlib enrichment chain |
| **v0.2.38** | 2026-04-22 | Top-level `README.md` reflects the v0.2.x stdlib + new examples |
| **v0.2.37** | 2026-04-22 | `examples/README.md` — discoverability index for the 18 examples |
| **v0.2.36** | 2026-04-22 | Micro-benchmark harness (`examples/18_benchmark.nr`) |
| **v0.2.35** | 2026-04-22 | Multi-file `wc`-style counter (`examples/17_linecount.nr`) |
| **v0.2.34** | 2026-04-22 | ASCII histogram example (`examples/16_histogram.nr`) |
| **v0.2.33** | 2026-04-22 | Helper schema contract ported into the plan as a tracked deliverable |
| **v0.2.32** | 2026-04-22 | Word-frequency counter example (`examples/15_word_count.nr`) |
| **v0.2.31** | 2026-04-22 | End-to-end CSV summary example (`examples/14_csv_summary.nr`) |
| **v0.2.30** | 2026-04-22 | Vec statistics helpers (6 helpers) |
| **v0.2.29** | 2026-04-22 | Random helpers + Vec shuffle / sample (6 helpers) |
| **v0.2.28** | 2026-04-22 | Checked / wrapping / saturating div-rem-neg (7 helpers) |
| **v0.2.27** | 2026-04-22 | HashMap accessor + bulk-op extras (4 helpers) |
| **v0.2.26** | 2026-04-22 | String padding + join + explode (6 helpers) |
| **v0.2.25** | 2026-04-22 | Base-conversion helpers (6 helpers) |
| **v0.2.24** | 2026-04-22 | Parse + stringify primitives (6 helpers) |
| **v0.2.23** | 2026-04-22 | Path utilities (6 helpers) |
| **v0.2.22** | 2026-04-22 | Vec mutation + accessor extras (7 helpers) |
| **v0.2.21** | 2026-04-22 | Time decomposition + elapsed (9 helpers) |
| **v0.2.20** | 2026-04-22 | Env extras + string utility round-out (7 helpers) |
| **v0.2.19** | 2026-04-22 | Filesystem extras (5 helpers) |
| **v0.2.18** | 2026-04-22 | Float math + bit population helpers (7 helpers) |
| **v0.2.17** | 2026-04-22 | Hash helpers + print/eprint without trailing newline (5 helpers) |
| **v0.2.16** | 2026-04-22 | HashMap iteration + ISO 8601 time formatting (4 helpers) |
| **v0.2.15** | 2026-04-22 | RNG primitives wired + latent linker gap closed |
| **v0.2.14** | 2026-04-22 | Char predicates + transformations (12 helpers) |
| **v0.2.13** | 2026-04-22 | Vec arithmetic + format extensions (8 helpers) |
| **v0.2.12** | 2026-04-22 | `nuc install --git <url> [--rev <ref>]` stub (RFC-0019 phase 3 partial) |
| **v0.2.11** | 2026-04-22 | Stdlib string utilities (10 helpers) |
| **v0.2.10** | 2026-04-22 | Vec utility expansion: contains, index_of, sort, reverse, clone,
clear, plus f64 reductions |
| **v0.2.9** | 2026-04-22 | RFC-0024 phase 1: Vec<i64> functional helpers via function pointers |
| **v0.2.8** | 2026-04-22 | `format_f64` builtin — RFC-0028 phase 1 completion |
| **v0.2.7** | 2026-04-22 | RFC-0015 phase 6 closed: NUM-002 + NUM-005 fired by typecker |
| **v0.2.6** | 2026-04-22 | RFC-0028 phase 1: format string builtins |
| **v0.2.5** | 2026-04-22 | `nuc install sysroot` stub + `nuc doc` parameter rendering |
| **v0.2.4** | 2026-04-22 | RFC-0019 phase 4: `nuc add` / `nuc remove` / `nuc update` aliases |
| **v0.2.3** | 2026-04-22 | RFC-0023 range patterns — IR scaffolding (parser deferred) |
| **v0.2.2** | 2026-04-22 | `docs/status/v0.2-shipped-and-deferred.md` — public snapshot |
| **v0.2.1** | 2026-04-22 | `docs/migrations/v0.1-to-v0.2.md` — upgrade guide |
| **v0.2.0** | 2026-04-22 | Foundation milestone — numerics, Result/Option/match, modules,
packages, diagnostics, tests, cross-platform |

## v0.1.x

| Version | Date | Summary |
|---|---|---|
| **v0.1.67** | 2026-04-22 | `docs/milestones/v0.4.0.md` — capture v0.2-deferred work |
| **v0.1.66** | 2026-04-22 | v0.2.0 milestone closed: 0 TODO rows, 6/6 success criteria green |
| **v0.1.65** | 2026-04-22 | `nuc doc` skeleton + 11 examples migrated to `use std::` syntax |
| **v0.1.64** | 2026-04-22 | NUM-003 lossy-cast warning + warnings-don't-halt-build |
| **v0.1.63** | 2026-04-22 | `nuc fix --numeric` migration linter + RFC-0022 phase 3 deferrals |
| **v0.1.62** | 2026-04-22 | RFC-0015 phase 1 numeric-type lattice classifiers landed |
| **v0.1.61** | 2026-04-22 | RFC-0016 phase 5 partial: MATCH-001 + MATCH-002 fired by typecker |
| **v0.1.60** | 2026-04-22 | `nuc fix --imports` migration tool + `From`/`Into` deferral |
| **v0.1.59** | 2026-04-22 | RFC-0020 phase 1 LineMap + RFC-0022 phase 2 `_WIN32` audit closed |
| **v0.1.58** | 2026-04-22 | Stdin read primitives + RFC-0015 phase 4 runtime helpers complete |
| **v0.1.57** | 2026-04-22 | Compiler ABI drift detector wired into the gate |
| **v0.1.56** | 2026-04-22 | Tools-binary IR-gen tables fully synced — eliminates compile-time
drift between `nucleor` and `nucleor_tools` |
| **v0.1.55** | 2026-04-22 | RFC-0021 phase 2: `nuc test --isolation=process` + RFC-0019 lockfile/workspace tracker reconciliation |
| **v0.1.54** | 2026-04-22 | RFC-0015 phase 4: per-narrow-width overflow primitives |
| **v0.1.53** | 2026-04-22 | RFC-0018 phase 1 partial: `mod foo;` directive |
| **v0.1.52** | 2026-04-22 | RFC-0018 phase 1 partial: Rust-style `use std::<rod>` paths |
| **v0.1.51** | 2026-04-22 | Spec doc + tracker reconciliation: MOD/PKG/TGT diagnostic tables |
| **v0.1.50** | 2026-04-22 | RFC-0016 phase 1: `?` postfix operator |
| **v0.1.49** | 2026-04-22 | MATCH-005…010 explain entries + milestone tracker accuracy |
| **v0.1.48** | 2026-04-22 | Process spawn primitives + POSIX `nuc` wrapper recognized |
| **v0.1.47** | 2026-04-22 | HTTP client wrapper + COLL-004/005 diagnostics + socket smoke test |
| **v0.1.46** | 2026-04-22 | Comprehensive math primitive library |
| **v0.1.45** | 2026-04-22 | Stdlib polish: atomic + bits rod wrappers |
| **v0.1.44** | 2026-04-22 | RFC-0007 partial: AtomicI64 + bit-twiddling primitives |
| **v0.1.43** | 2026-04-22 | Stdlib polish: binary + digest rod wrappers |
| **v0.1.42** | 2026-04-22 | Crypto / hash / id helpers: CRC32, SHA-256, Base64, UUID v4 |
| **v0.1.41** | 2026-04-22 | Decisions §B5: byte-buffer + endian + MessagePack subset |
| **v0.1.40** | 2026-04-22 | RFC-0023 partial: `..=` inclusive range in `for` loops |
| **v0.1.39** | 2026-04-22 | RFC-0019 phase 1 ★ canonical `nuc.toml` + manifest validator |
| **v0.1.38** | 2026-04-22 | Stdlib polish: fs_extras + simd + asserts rod wrappers |
| **v0.1.37** | 2026-04-22 | Stdlib polish: typed-time + OS-info rod wrappers |
| **v0.1.36** | 2026-04-22 | RFC-0017 stdlib polish: rod-level wrappers for all collection
types |
| **v0.1.35** | 2026-04-22 | RFC-0019 phase 1: `stdlib/rods/toml.nr` rod-level wrapper |
| **v0.1.34** | 2026-04-22 | RFC-0020 phase 2 complete: JSON diagnostic renderer + RFC-0017/
0018/0019/0022 explain coverage |
| **v0.1.33** | 2026-04-22 | RFC-0019 phase 1 start: minimal TOML parser for `nuc.toml` |
| **v0.1.32** | 2026-04-22 | RFC-0018 / RFC-0019 prerequisite: file system primitives |
| **v0.1.31** | 2026-04-22 | RFC-0017 phase 3: BTreeMap + BTreeSet — ordered collections |
| **v0.1.30** | 2026-04-22 | RFC-0017 phase 4: VecDeque + HashSet. RFC-0022 phase 2: POSIX `nuc` wrapper |
| **v0.1.29** | 2026-04-22 | Robotics-RFC §5.1 + RFC-0022: typed time, env vars, OS info |
| **v0.1.28** | 2026-04-22 | RFC-0017 partial: HashMap<str, i64> with full open-addressed
implementation |
| **v0.1.27** | 2026-04-22 | RFC-0017 partial: heap-allocated `String` type |
| **v0.1.26** | 2026-04-22 | RFC-0033 (preview): SIMD vector types — `f32x4`, `i32x4` |
| **v0.1.25** | 2026-04-22 | returns the
  value untouched |
| **v0.1.24** | 2026-04-22 | RFC-0015: `stdlib/rods/numeric.nr` — unified numeric API |
| **v0.1.23** | 2026-04-22 | RFC-0015 phase 6: f32 distinct compute + bf16/f16/f8 software
emulation |
| **v0.1.22** | 2026-04-22 | RFC-0015 phase 5b: hex/oct/bin literals + typed-storage Vecs |
| **v0.1.21** | 2026-04-22 | RFC-0015 phase 5: per-width print helpers + bin/hex |
| **v0.1.20** | 2026-04-22 | RFC-0015 phase 4: explicit-overflow arithmetic |
| **v0.1.19** | 2026-04-22 | RFC-0015 phase 2: `as` cast operator + numeric type predicates |
| **v0.1.18** | 2026-04-22 | `nuc explain` entries for ASSUME/UNIT/CONTRACT/ATOMIC/ISR/WCET/ |
| **v0.1.17** | 2026-04-22 | `loop { BODY }` |
| **v0.1.16** | 2026-04-22 | `while let PATTERN = EXPR { BODY }` |
| **v0.1.15** | 2026-04-22 | `panic!` builtin + `nuc explain` documentation for 24 new error |
| **v0.1.14** | 2026-04-22 | RFC-0021 step: `assert!`, `assert_eq!`, `assert_ne!` builtins + |
| **v0.1.13** | 2026-04-22 | `if let PATTERN = EXPR { THEN } [else { ELSE }]` |
| **v0.1.12** | 2026-04-22 | RFC-0016 partial: native enum `match` with payload extraction |
| **v0.1.11** | 2026-04-22 | RFC-0020 phase 1: Rust-style diagnostic rendering |
| **v0.1.10** | 2026-04-22 | Lexer recognizes `#[...]` |
| **v0.1.9** | 2026-04-22 | `tools/verify.sh` |
| **v0.1.8** | 2026-04-22 | 67 to 101 steps |
| **v0.1.7** | 2026-04-22 | 38 to 67 steps |
| **v0.1.6** | 2026-04-22 | `json_from_f64(val)` / `json_f64(j)` |
| **v0.1.5** | 2026-04-22 | `bin/nucleor_tools.exe` shipped |
| **v0.1.4** | 2026-04-22 | `vqe_h2_data.csv` |
| **v0.1.3** | 2026-04-22 | `vqe_h2.nr` |
| **v0.1.2** | 2026-04-21 | `nuc zen` |
| **v0.1.1** | 2026-04-21 | 29 new `.nr` rod wrappers |
| **v0.1.0** | 2026-04-21 | Self-hosted compiler |
