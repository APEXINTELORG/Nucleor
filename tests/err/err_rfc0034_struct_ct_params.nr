// EXPECT: nucleor: struct CT-params not yet supported
// v0.6.65 — struct CT-params halt cleanly at parse (was wrong-class NR020)
struct Buf[N: usize] { data: Vec<i64> }
fn main() -> i32 { return 0; }
