/// Doubles its argument.
/// T2.7 (v0.2.352) HTML doc fixture.
fn dbl(x: i64) -> i64 {
    return x * 2;
}

/// Adds two i64 values.
fn add(a: i64, b: i64) -> i64 {
    return a + b;
}

fn helper_no_doc() -> i64 { return 0; }
