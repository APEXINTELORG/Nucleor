# Partner Queue 9A — QM-7 Citation-Backed Enumerator Row v0845

Date: 2026-05-07
Agent: partner pair (Claude controller)
Worktree: `C:\Users\JoeWe\Desktop\Nucleor_AGENT_partner_lane10_v0845`
Branch: `fix/partner-qm7-enumerator-citation-row-v0845` based on `origin/main` @ `5890c846`

## Scope accepted

- Add a documentation/evidence row tying the in-tree bounded weight-enumerator counts in `tests/features/qm7_clifford_weight_enumerator_smoke.nr` to a citation already present in `Nucleor_OSS` (Tomita and Svore, Phys. Rev. A 90, 062320 (2014), Table II).
- Document the citation chain so the relationship between cited stabilizer generators and asserted enumerator counts is explicit, without changing runtime math.

## Scope explicitly not taken

- Did not change runtime math in `stdlib/rods/clifford.nr` or `stdlib/rods/quantum_rt.c` — the asserted counts are unchanged.
- Did not add a separate third-party enumerator-value cross-validation row (asserting the same numbers against a published enumerator computed in another framework). That remains optional future work; the punchlist now records this distinction.
- Did not edit RFC-0042 / RFC-0001 / other RFCs — only the PUNCHLIST entry and the fixture comment header.
- Did not run the full quantum fixture suite or `tools/verify.sh`. The change is comment-only on the test source plus a docs-row append, so the existing fixture's runtime behavior is unchanged.

## Base / HEAD / merge-base

- Base: `origin/main` @ `5890c846` (`docs: add Cloud_Control1 source-of-truth for cloud Linux agents`).
- HEAD: TBD — committed locally on this branch.
- Merge-base with `origin/main`: `5890c846`.

## Host OS and shell

- Windows 11 Home 10.0.26200; Bash (Git Bash) used.

## Commands run (verbatim)

```
git checkout -B fix/partner-qm7-enumerator-citation-row-v0845 origin/main
grep -nE "Tomita|Svore" tests/features/qm7_clifford_surface_d3_smoke.nr
read tests/features/qm7_clifford_weight_enumerator_smoke.nr
read docs/rfcs/v1_PUNCHLIST.md (Phase 2c entry, lines 307–312)
edit tests/features/qm7_clifford_weight_enumerator_smoke.nr (header citation chain)
edit docs/rfcs/v1_PUNCHLIST.md (append "Phase 2c citation row" entry under Phase 2c)
```

## Files changed

- `tests/features/qm7_clifford_weight_enumerator_smoke.nr` — header comment expanded with explicit citation chain pointing at `qm7_clifford_surface_d3_smoke.nr`'s Tomita/Svore PRA 90, 062320 (2014), Table II reference. No source-code change.
- `docs/rfcs/v1_PUNCHLIST.md` — appended a "Phase 2c citation row: DONE 2026-05-07 (v0845)" entry under the existing Phase 2c entry, explaining the citation chain and clarifying that separate external enumerator-VALUE parity remains optional.

## Validation

- `git diff --check`: clean (comment-only + docs-only edits; no whitespace / tab / line-ending issues).
- Did NOT rebuild `bin/nucleor.exe`, did NOT re-run `tests/features/qm7_clifford_weight_enumerator_smoke.nr`, did NOT run `tools/verify.sh`. Justification: the asserted runtime counts and the executable behavior of the fixture are unchanged; this queue's scope is documentation-only. If the integration owner requires a fresh fixture transcript before merging, it will produce the same `rc=0` it produced when the fixture last passed (per punchlist Phase 2c "DONE 2026-05-06").
- Compiler/runtime/perf gates not run (no compiler/tools/cache/hot-path change).

## Citation chain

```
v1_PUNCHLIST.md Phase 2c citation row
  -> tests/features/qm7_clifford_weight_enumerator_smoke.nr  (header citation chain)
       -> tests/features/qm7_clifford_surface_d3_smoke.nr     (cites Tomita/Svore Table II)
            -> Tomita and Svore, "Low-distance surface codes under realistic quantum noise,"
               Phys. Rev. A 90, 062320 (2014), Table II.
  -> runtime: cliff_stabilizer_weight_count / cliff_logical_weight_count
       compute the asserted counts deterministically as exhaustive
       consequences of the cited generator set.
```

## Honest residuals

1. The citation row asserts that the in-tree counts are exhaustive consequences of the cited stabilizer generators. It does NOT independently verify those counts against an external published enumerator value computed in another framework; if a future launch artifact requires that level of cross-validation, the punchlist now records this explicitly as optional future work rather than as a closed gap.
2. Tomita/Svore Table II provides the stabilizer set and the logical operators; the published paper does not (to the best of this audit's knowledge) tabulate the full stabilizer-group weight enumerator or the full logical-coset weight enumerator. The honest claim therefore is "generator set is cited; the counts are deterministic consequences." This is exactly the documentation gap the queue was supposed to close.
3. Did not search for a more aggressive third-party citation (Bravyi/Suchara/Vargo, Fowler et al, Aguado/Vidal) because the queue's scope is "if a reliable source is already in docs or can be cleanly cited." The Tomita/Svore source is already in tree at the d=3 fixture and is the correct primary citation for the generator set; no external research was conducted.
4. The fixture's runtime behavior is unchanged. The change is exclusively docs/comment-level; the test still asserts the same return codes and the same weight counts.

## Conclusion

Queue 9A's documentation gap is closed: the in-tree bounded weight-enumerator counts are now explicitly tied to the Tomita/Svore Phys. Rev. A 90, 062320 (2014) Table II generator set already cited at the d=3 fixture, and the punchlist explicitly distinguishes between "cited generator set + deterministic enumerator computation" (DONE) and "separate external enumerator-VALUE parity row" (optional future work).
