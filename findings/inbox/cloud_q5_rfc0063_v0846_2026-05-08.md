# Q5 — RFC-0063 wave 12-16 parser-unification (30 IDENTICAL safe-delete)

**Branch:** `claude/v1-finish-cloud-Q5-rfc0063`
**Base:** `origin/main @ dc9d1c1` (post Q1+Q2 land; Q3+Q4 still on their own branches awaiting fast-forward)
**Host:** `Linux vm 6.18.5 #2 SMP PREEMPT_DYNAMIC Wed Jan 14 17:56:08 UTC 2026 x86_64`
**Tools:** `clang Ubuntu-18.1.3`
**Mandate:** `CLOUD_AGENT_V1_FINISH_BRIEF_2026-05-07.md` Q5 row.

## Bug shape (the gap this closes)

`tools/audit_dup_fns_report.csv` enumerated 180 duplicate fns between
`compiler/nucleor_s1_compiler.nr` and `compiler/nucleor_tools_suite.nr`:
- 30 IDENTICAL (sigs match AND bodies byte-identical) — the brief's
  "mechanical safe-delete" target.
- 131 SIG_MATCH_BODY_DIFFERS — triage required.
- 19 SIG_DIFFERS — case-by-case reconciliation required.

Wave 1 (the Phase 1 RFC-0063 ship) created `nucleor_rfc0063_shared_wave1.nr`
with 264 fns. Tools_suite imports that module; s1 has its own canonical
local copies of those 264 fns. The 30 IDENTICAL leftovers are fns that
neither wave 1 captured AND that exist as byte-for-byte duplicates in
both files.

## Patch shape (this Q ships only the IDENTICAL batch)

For each of the 30 IDENTICAL fns:
1. Extract from `compiler/nucleor_tools_suite.nr` (string-literal-aware
   brace tracker — see /tmp/{extract_fn,delete_fns}.awk built for this
   batch).
2. Append to `compiler/nucleor_rfc0063_shared_wave1.nr` under a header
   block documenting wave 12-16 provenance.
3. Delete from `compiler/nucleor_tools_suite.nr`.

`compiler/nucleor_s1_compiler.nr` is **unchanged**. s1 keeps its
canonical local copies; tools_suite gets the same fns via its existing
`import "nucleor_rfc0063_shared_wave1.nr"`. Bootstrap fixed-point is
unaffected (s1 source identical → seed identical).

## The 30 IDENTICAL fns

```
str_from_int                          parse_args
skip_bracket_list                     skip_compile_time_param_default
ct_param_type_is_unsigned             ct_param_default_is_negative_literal
skip_angle_group                      skip_where_clause
parse_match_binding_block             mk_passthrough_block_expr
parse_passthrough_block_expr          parse_wrapped_block_expr
parse_mul                             parse_add
parse_eq                              parse_and_expr
parse_or_expr                         parse_pipe_expr
parse_expr                            parse_type_alias_decl
parse_return_stmt                     parse_stmts
has_main_decl                         remove_main_decl
char_guard_key                        tenv_set_char_guard
tenv_has_char_guard                   char_guard_merge_flags
lower_if_expr_branch                  lower_stmts
```

## Validation

### File deltas
```
compiler/nucleor_tools_suite.nr:        18958 → 18667  (-291 lines, 30 fns)
compiler/nucleor_rfc0063_shared_wave1.nr: 2889 →  3221  (+332 lines: 30 fns + header block)
```

### Bootstrap fixed-point
```
seed pre-Q5:    65c6088542350f6800bc60f2f791f026 (md5)
                448336f49fe17db762591f6a57637f7d886ef727ff54e45385e4661805cb72e2 (sha256)
seed post-Q5:   IDENTICAL — s1 source was not modified.
```
Stage-1 + stage-2 IR converges at the unchanged seed.

### `bash tools/verify.sh`

**PASS=1486 / SKIP=8 / FAIL=0 across 1494 steps.** Full log:
`findings/inbox/cloud_q5_rfc0063_v0846_2026-05-08_default.log`.

### `bash tools/verify_strict.sh`

**PASS=1486 / SKIP=8 / FAIL=0 across 1494 steps.** Cache-cold +
`NUC_VERIFY_STRICT=1`. Full log:
`findings/inbox/cloud_q5_rfc0063_v0846_2026-05-08_strict.log`.

### Audit-report regen
```
Pre-Q5:  IDENTICAL=30  SIG_MATCH_BODY_DIFFERS=131  SIG_DIFFERS=19  (180 total)
Post-Q5: IDENTICAL=0   SIG_MATCH_BODY_DIFFERS=131  SIG_DIFFERS=19  (150 total)
```

### Tools-suite build
`bin/nucleor build compiler/nucleor_tools_suite.nr -o nucleor_tools` succeeds
post-deletion. The 30 fns are now resolved via the existing
`import "nucleor_rfc0063_shared_wave1.nr"` directive at line 15.

## Honesty

- **131 SIG_MATCH_BODY_DIFFERS not addressed in this Q.** Per the
  brief: "triage and unify. For divergences that are real (one impl
  is correct, the other stale), keep correct + delete stale. For
  genuine fork (both used, different callers), refactor to shared
  helper." That's case-by-case reconciliation, not mechanical
  safe-delete. The integrator's refined mandate in Cloud_Control1.md
  ("Phase A target: Q1+Q2+Q3+Q5 through Phase 2b sound") permits
  shipping Phase 2b sound = the IDENTICAL batch is sound (zero
  false positives, deterministic merge into shared module). Phase B
  follow-up: triage 131 SIG_MATCH_BODY_DIFFERS.
- **19 SIG_DIFFERS not addressed.** Same reason — these require
  reconciliation between two divergent surfaces (different arg
  counts mean call-sites need updates). Filing as Phase B work.
- **s1's 264 wave-1 fns still locally duplicated.** s1 doesn't import
  shared_wave1 — only tools_suite does. The truly-clean answer ("one
  parser surface") would also have s1 import shared_wave1 and delete
  its 264 wave-1 local copies. That requires bootstrap-affecting
  source changes (the seed needs to include shared_wave1's IR via
  the import path). Out of scope for the IDENTICAL safe-delete; would
  be its own structural Q. Filing as Phase B follow-up.

## Done

- Brief's mechanical "30 IDENTICAL safe-delete in one batched commit"
  shipped. Both verify modes GREEN. Bootstrap fixed-point unaffected.
  Audit-report confirms IDENTICAL=0.
- 131 SIG_MATCH_BODY_DIFFERS + 19 SIG_DIFFERS remain as Phase B
  triage / reconciliation work per the integrator's refined mandate.

Cumulative cloud queue progress: Q1+Q2 landed, Q3+Q4+Q5 on their own
branches awaiting fast-forward.
