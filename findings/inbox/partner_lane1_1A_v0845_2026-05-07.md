# Partner Lane 1 Queue 1A - RFC-0063 Wave 10 IDENTICAL Duplicate Retirement

## Scope accepted

Accepted Queue 1A from `docs/rfcs/v1_REMAINING_PUNCHLIST_AGENT_HANDOFF_v0845_2026-05-07.md`: retire a safe mechanical batch of `IDENTICAL` duplicate functions from `compiler/nucleor_tools_suite.nr` by moving canonical copies into `compiler/nucleor_rfc0063_shared_wave1.nr`, refresh `tools/audit_dup_fns_report.csv`, update stale RFC-0063 count claims, and run the lane gates.

The retired batch was intentionally conservative: 18 byte-identical low-level accessor/non-core helpers:

`str_is_digits`, `tok_type`, `tok_val`, `is_digit`, `tok_at`, `tok_type_at`, `tok_val_at`, `node_add`, `node_kind`, `node_field`, `list_len`, `list_get`, `pr_pos`, `pr_val`, `pk`, `pkv`, `resolve_source`, `close_synthesize_fn`.

## Scope explicitly not taken

Did not touch the larger parser expression/statement bodies, any `SIG_MATCH_BODY_DIFFERS` rows, or any `SIG_DIFFERS` rows. Did not modify `findings/heartbeat.json`. Did not push the branch. Did not run full `bash tools/verify.sh`; this queue ran the requested focused gates plus the perf gate.

## Base SHA

`origin/main`: `5890c84603bd46fc6d86b9500b2ef7cd4ae4d63c`

## HEAD SHA

Report written before the final local commit. Final post-commit SHA is reported in `C:\Users\JoeWe\Desktop\actual_bridge\.crosslink\result.md` and the append-only `C:\Users\JoeWe\Desktop\Control1.md` completion entry, because embedding the final commit SHA inside the committed report would change that SHA.

## Merge-base with origin/main

`5890c84603bd46fc6d86b9500b2ef7cd4ae4d63c`

## Host OS and shell

- OS: Microsoft Windows NT 10.0.26200.0
- Shell: PowerShell 7.5.5
- Worktree: `C:\Users\JoeWe\Desktop\Nucleor_AGENT_partner_lane1_v0845`
- Branch: `fix/partner-rfc0063-tools-suite-wave10-v0845`

## Commands run (verbatim)

```powershell
git status --short --branch
git rev-parse HEAD
git merge-base HEAD origin/main
.\bin\nucleor.exe build tools\audit_dup_fns.nr -o audit_dup_fns --no-cache
.\target\audit_dup_fns.exe
```

```powershell
$names = @('str_is_digits','tok_type','tok_val','is_digit','tok_at','tok_type_at','tok_val_at','node_add','node_kind','node_field','list_len','list_get','pr_pos','pr_val','pk','pkv','resolve_source','close_synthesize_fn')
```

Used an inline PowerShell mechanical rewrite to extract those exact `fn <name>(...)` blocks from `compiler\nucleor_tools_suite.nr`, append them under a Wave 10 section in `compiler\nucleor_rfc0063_shared_wave1.nr`, and remove the raw tools-suite copies. The script used a string/comment-aware brace scanner and UTF-8 no-BOM writes.

```powershell
git diff --stat
.\bin\nucleor.exe build compiler\nucleor_tools_suite.nr -o nucleor_tools --no-cache
.\bin\nucleor.exe build tools\audit_dup_fns.nr -o audit_dup_fns --no-cache
.\target\audit_dup_fns.exe
rg -n "204|44|IDENTICAL|SIG_MATCH_BODY_DIFFERS|SIG_DIFFERS|duplicate function|duplicate" docs\rfcs\RFC-0063-production-readiness-roadmap.md docs\rfcs\v1_PUNCHLIST.md
bash tools/check_compiler_drift.sh
bash tools/check_rod_void_abi.sh
git diff --check
pwsh -NoProfile -File tools\check_perf_regression.ps1
git status --short --branch
git diff --name-only
git diff --stat
git rev-parse HEAD
git rev-parse origin/main
git merge-base HEAD origin/main
```

## Validation transcript summary

Baseline audit:

```text
s1 fns: 845
tools fns: 484
Duplicate fns by name: 204
  IDENTICAL: 44
  SIG_MATCH_BODY_DIFFERS: 144
  SIG_DIFFERS: 16
```

Tools-suite rebuild:

```text
source: compiler\nucleor_tools_suite.nr (1128181 bytes)
mode: fast (ownership + type)
mode: llvm-only (--no-link)
functions: 716
DCE: 39 of 716 fns elided as unreachable
compiled: target\nucleor_tools.exe
```

Post-change audit:

```text
s1 fns: 845
tools fns: 466
Duplicate fns by name: 186
  IDENTICAL: 26
  SIG_MATCH_BODY_DIFFERS: 144
  SIG_DIFFERS: 16
```

Focused gates:

```text
bash tools/check_compiler_drift.sh: PASS
  Expected RFC-0063 parser-drift warnings remain for parse_match_stmt, parse_stmt, and parse_expr.
  OK: tools-suite ABI tables match nucleor_s1_compiler.nr
  OK: audit_dup_fns_report.csv is up to date
bash tools/check_rod_void_abi.sh: PASS
  OK: rod void ABI clean (355 C void nuc_* definitions, 1275 non-void rod externs checked)
git diff --check: PASS
  Warning only: tools/audit_dup_fns_report.csv LF will be replaced by CRLF the next time Git touches it.
pwsh -NoProfile -File tools\check_perf_regression.ps1: PASS
  OK perf: cold=3.55s (max 4s) | hot=0.4s (max 1s) | mem cold_tree=347/400MB cold_compiler=333/350MB hot_tree=70/128MB hot_compiler=55/64MB
```

## Files changed

- `compiler/nucleor_rfc0063_shared_wave1.nr`
- `compiler/nucleor_tools_suite.nr`
- `tools/audit_dup_fns_report.csv`
- `docs/rfcs/RFC-0063-production-readiness-roadmap.md`
- `docs/rfcs/v1_PUNCHLIST.md`
- `findings/inbox/partner_lane1_1A_v0845_2026-05-07.md`

## Honest residuals

The queue reduced `IDENTICAL` candidates from 44 to 26. The remaining 26 include parser/core-adjacent rows and should be retired in another focused batch only after the same audit/build gates stay green. `SIG_MATCH_BODY_DIFFERS` remains 144 and `SIG_DIFFERS` remains 16; neither category was attempted in this queue. Full `tools/verify.sh` was not run.
