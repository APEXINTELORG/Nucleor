# Partner Queue 9B — Quantum Future-Work Fence Audit v0845

Date: 2026-05-07
Agent: partner pair (Claude controller)
Worktree: `C:\Users\JoeWe\Desktop\Nucleor_AGENT_partner_lane10_v0845`
Branch: `probe/partner-quantum-future-work-fence-audit-v0845` based on `origin/main` @ `5890c846`

## Scope accepted

- Audit current Nucleor_OSS quantum docs and fixtures for overclaims.
- Confirm explicit future-work fences for: foreign OpenQASM 2.0 import, density-matrix/Kraus backend, backend calibration/resource scheduler, hardware target lowering, and per-graph handle state.
- Read-only probe. Single findings file. No source modifications.

## Scope explicitly not taken

- Did not run quantum fixtures (`tests/features/qm7_*.nr`, `tests/features/quantum_*.nr`) — read-only audit.
- Did not edit any docs or rod source (no overclaims found that require redaction; flagged residuals are upstream-style nits, not OSS-side hazards).
- Did not enumerate every quantum function in `stdlib/rods/quantum.nr` — only audited the surface that is publicly claimed.

## Base / HEAD / merge-base

- Base: `origin/main` @ `5890c846`
- HEAD: same as base (probe branch — only this report will land on it).
- Merge-base with `origin/main`: `5890c846`.

## Host OS and shell

- Windows 11 Home 10.0.26200; Bash (Git Bash) used.

## Commands run (verbatim)

```
glob: stdlib/rods/quantum*.nr
glob: tests/features/qm*.nr
ls docs/rfcs | grep -iE "quantum|qm|qasm"
grep -niE "QM-7|QM-6|...|hardware.target|kraus|density.matrix|backend.scheduler|per.graph|external.sink" docs/rfcs/v1_PUNCHLIST.md
grep -niE "future|gap|tbd|not.yet|stub" docs/rfcs/gap-analyses/Nucleor_Quantum_Subsystem_Gap_Analysis_and_RFC_2026-05-04.md
grep -niE "quantum|qm-|qsim|qasm|kraus|density|mps|stabilizer|surface" CHANGELOG.md (latest 25 lines)
grep -niE "quantum|QM-|qsim|qasm|kraus|...hardware" README.md
read stdlib/rods/quantum.nr (header + qasm2_limitations + parser disclosure)
read docs/rfcs/v1_PUNCHLIST.md (lines 280–360, 680–760)
```

## Audit Matrix — Targeted Future-Work Items

| Future-work item (per handoff §Lane 9 / Queue 9B) | Fenced in `v1_PUNCHLIST.md`? | Fenced in `gap-analyses/Nucleor_Quantum_Subsystem_Gap_Analysis_and_RFC_2026-05-04.md`? | Fenced in rod source? | README/CHANGELOG overclaim? |
|---|---|---|---|---|
| Foreign OpenQASM 2.0 import | YES — line 348-350 ("General OpenQASM 2.0 importing of foreign sources … remains future work") | YES — line 178 ("No emit, no ingest, no stub") narrative + Section narrative below | YES — `qasm2_limitations()` (line 792-793 of `stdlib/rods/quantum.nr`) explicitly states "Real importing of foreign OpenQASM2 sources remains future work" plus enumerates the exact deterministic emit-dialect-only acceptance | None found. README does not mention OpenQASM. CHANGELOG entries describe only the emit + minimal parser scope. |
| Density-matrix / Kraus backend (amplitude damping, phase damping, mixed-state composition, exact channel composition) | YES — line 691-694 ("this is not a full density-matrix/Kraus backend; amplitude damping, phase damping, mixed-state composition, and exact channel composition remain future work") | YES — line 190-192 ("this is not a full density-matrix/Kraus backend; amplitude damping … remain future work") | NOT explicitly fenced in rod docstring (qsim_noise_* functions exist, but no per-function caveat that they are trajectory-only and not full mixed-state evolution). Counter-balanced by the punchlist + gap-analysis fences. | None. README mentions only "full state-vector quantum simulator" which is accurate (statevector ≠ density matrix). |
| Backend calibration / resource scheduler | YES — line 676-677 ("Remaining gap: no backend calibration/resource scheduler or hardware target lowering") | Implied via section narrative — no proactive scheduler claim found anywhere. | N/A — no scheduler is exposed in `stdlib/rods/quantum.nr`. | None. |
| Hardware target lowering | YES — lines 676-677 and 701-702 ("hardware timing and target-lowering semantics remain RFC-0054 Phase B") | YES — line 203 (gap-analysis: "hardware timing and target-lowering semantics remain RFC-0054 Phase B") | N/A — no hardware-target codegen claim in rod. | None. README does not claim hardware execution. |
| Per-graph handle state (qsim_graph) | YES — repeated four times across lines 627, 635, 642, 651-652 ("process-local graph state … rather than per-graph handles" / "per-graph handle state for scalable independent graph ownership") | YES — line 120 ("per-graph handle state for independent graph ownership rather than one [process-local store]") | NOT explicitly fenced in rod docstring; the public `qsim_graph_thread_safety_disclosure_smoke.nr` and `qsim_graph_is_thread_safe()` runtime call disclose thread-safety but do not advertise per-graph isolation. Counter-balanced by the punchlist + gap-analysis fences. | None. |

## Additional surface checks

- **README.md, line 3:** "...quantum simulation, and 60+ more rods, all built in." — accurate.
- **README.md, line 26:** "Full state-vector quantum simulator." — accurate (statevector, not density-matrix).
- **README.md, line 66:** "**Quantum:** `quantum` (full state-vector simulator: H, X, Y, Z, CNOT, measure)" — UNDER-enumerated (the rod also exposes Y, S, T, Sdg, Tdg, RX, RY, RZ, SWAP, CZ, CCX, CRK, gate1, cgate1) but NOT an overclaim. Optional follow-up: extend the gate enumeration in README, but that is style, not a 9B blocker.
- **README.md, line 89:** `examples/05_quantum.nr` — Bell state — verified file exists.
- **README.md, line 121:** `examples/showcase/vqe_h2.nr` — VQE H2 — verified file exists; the showcase entry honestly characterizes it as "parameter-shift gradient descent" on a "2-qubit Hamiltonian", not a hardware claim.
- **CHANGELOG.md historical entries** (lines 77295, 77297): "Full state-vector simulation up to 16+ qubits" — accurate; the qsim_state capacity contract (`qsim_state_capacity_status_smoke.nr` per punchlist line 600) explicitly enforces this.
- **`stdlib/rods/quantum.nr`** runtime self-disclosure surfaces:
  - `qasm2_limitations()` returns a long string that fences the emit/parser dialect explicitly.
  - `qsim_graph_is_thread_safe()` returns 1 with a serialized C11 spinlock; per-graph handle absence is documented in the punchlist.
  - `qsim_state_capacity_status` enforces upper qubit counts.

## Validation transcript summary

- No source files modified on this branch (probe-only).
- `git status --short --branch` clean except for this report.
- `git diff --check` clean.
- No compiler/runtime/perf gates triggered (no source/runtime/cache/hot-path change).

## Files changed

- `findings/inbox/partner_lane9_9B_v0845_2026-05-07.md` (this file).

## Honest residuals

1. Two of the targeted future-work fences (density-matrix/Kraus, per-graph handles) are clearly disclosed in `v1_PUNCHLIST.md` and the gap-analysis but are not cited in the rod's runtime self-disclosure surface (`qasm2_limitations()`-style). Optional small follow-up (NOT in 9B's write scope): add `qsim_noise_limitations()` and `qsim_graph_limitations()` runtime helpers that return the same disclosure string. Recommended as a future Phase-1 polish queue, not a v1.0 blocker.
2. README enumerates the gate subset incompletely (line 66) — under-claim. Polish-grade, not a 9B fence issue.
3. Did not exhaust every fixture under `tests/features/qm7_*` or `tests/features/quantum_*` — sampled the public-surface entries listed in `v1_PUNCHLIST.md`. The punchlist's named fixtures all matched live files in `tests/features/`.
4. Did not audit RFC-0054 (logical qubit type), RFC-0053 (neuromorphic types), or RFC-0056 (deterministic replay) line-by-line — they each appeared in the initial grep set but their substantive future-work claims are reflected through the punchlist fences already audited.

## Conclusion

All five targeted future-work items called out by the handoff are explicitly fenced in `docs/rfcs/v1_PUNCHLIST.md` and `docs/rfcs/gap-analyses/Nucleor_Quantum_Subsystem_Gap_Analysis_and_RFC_2026-05-04.md`. The README and CHANGELOG do not overclaim into hardware execution, density-matrix evolution, or foreign QASM import territory. The rod's runtime self-disclosure surface (`qasm2_limitations()`) provides excellent caller-visible fencing for the OpenQASM 2.0 dialect.

No `Nucleor_OSS` doc/rod redaction is required by 9B. The audit recommends NO blocker and NO punchlist edit.
