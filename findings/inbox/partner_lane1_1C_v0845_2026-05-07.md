# Partner Lane 1 Queue 1C v0845 - RFC-0063 SIG_DIFFERS Decision Table

## Status

DONE. This is a probe-only decision table for the 16 `SIG_DIFFERS` rows in `tools/audit_dup_fns_report.csv`.

## Scope Accepted

- Re-ran the duplicate-function audit from fresh `origin/main`.
- Confirmed the current baseline is `SIG_DIFFERS = 16`.
- Read the s1 and tools-suite signatures and bodies for all 16 rows.
- Classified every row into the requested categories.
- Wrote this report only; no compiler or tools-suite source was changed.

## Scope Explicitly Not Taken

- Did not implement any retirement, adapter, rename, or source migration.
- Did not modify `compiler/nucleor_s1_compiler.nr`.
- Did not modify `compiler/nucleor_tools_suite.nr`.
- Did not modify `findings/heartbeat.json`.
- Did not push this branch.
- Did not run compiler/tools-suite/perf gates; this was a read-only probe except for this report.

## Git State

- Worktree: `C:\Users\JoeWe\Desktop\Nucleor_AGENT_partner_lane1_v0845`
- Branch: `probe/partner-rfc0063-sig-differs-decision-table-v0845`
- Base SHA: `5890c84603bd46fc6d86b9500b2ef7cd4ae4d63c`
- HEAD SHA at report write: `5890c84603bd46fc6d86b9500b2ef7cd4ae4d63c`
- Merge-base with `origin/main`: `5890c84603bd46fc6d86b9500b2ef7cd4ae4d63c`
- Final commit SHA: reported in `.crosslink/result.md` and `C:\Users\JoeWe\Desktop\Control1.md` after commit to avoid a self-referential SHA in this file.

## Host

- OS: `Microsoft Windows NT 10.0.26200.0`
- Shell: PowerShell `7.5.5`

## Commands Run

```powershell
git fetch origin
git checkout -B probe/partner-rfc0063-sig-differs-decision-table-v0845 origin/main
git rev-parse HEAD
git merge-base HEAD origin/main
.\bin\nucleor.exe build tools\audit_dup_fns.nr -o audit_dup_fns --no-cache
.\target\audit_dup_fns.exe
Import-Csv tools\audit_dup_fns_report.csv | Where-Object { $_.category -eq 'SIG_DIFFERS' } | Format-Table -AutoSize
rg -n "fn <name>\(" compiler\nucleor_s1_compiler.nr compiler\nucleor_tools_suite.nr
rg -n "<function-name>\(" compiler\nucleor_s1_compiler.nr compiler\nucleor_tools_suite.nr
Get-Content compiler\nucleor_s1_compiler.nr | Select-Object -Skip <line> -First <count>
Get-Content compiler\nucleor_tools_suite.nr | Select-Object -Skip <line> -First <count>
git restore -- tools\audit_dup_fns_report.csv
```

## Audit Baseline

```text
s1 fns: 845
tools fns: 484
Wrote tools/audit_dup_fns_report.csv
Duplicate fns by name: 204
  IDENTICAL: 44
  SIG_MATCH_BODY_DIFFERS: 144
  SIG_DIFFERS: 16
```

The audit command rewrote `tools/audit_dup_fns_report.csv` as part of the required baseline run. That side effect was restored before commit so this probe remains report-only.

## Decision Table

| Function | s1 signature | tools-suite signature | Body delta | Classification | Justification | Smallest patch sketch |
| --- | --- | --- | --- | --- | --- | --- |
| `parse_match_stmt` | `fn parse_match_stmt(tokens: Vec<i32>, pos: i64, pool: Vec<i32>, match_span: i64) -> Vec<i32>` | `fn parse_match_stmt(tokens: Vec<i32>, pos: i64, pool: Vec<i32>) -> Vec<i32>` | s1 factors arm parsing through `parse_match_one_pattern`, includes newer defensive pattern diagnostics, expands or-patterns through the helper, and records the match node span via `node_set_span`. tools-suite has an older inlined parser body and returns a spanless match node. | s1 canonical | The extra `match_span` is the canonical parser metadata input; tools call sites can source it from the `match` token position. The tools body is not just formatting drift, but the newer s1 factoring appears to subsume the inlined tools parsing while preserving the AST shape. | Call-site migration: `parse_match_stmt(tokens, pos + 1, pool, tok_pos_at(tokens, pos))`; delete the tools body after parser smokes cover match literals, ranges, enum payloads, guards, and or-patterns. |
| `emit_module_ext` | `fn emit_module_ext(fns: Vec<i32>, strtab: Vec<i32>, pool: Vec<i32>, externs: Vec<i32>, src: str) -> str` | `fn emit_module_ext(fns: Vec<i32>, strtab: Vec<i32>, pool: Vec<i32>, externs: Vec<i32>) -> str` | s1 is the production emitter: pre-sized string builder, user externs, string constants, provenance, public-name collision detection, DCE accounting, ISRs, and full function emission. tools-suite is a minimal loop over `emit_fn` with no source text. | s1 canonical | The missing `src` parameter enables source-aware module emission and the s1 body carries release-path behavior. tools-suite should retire its reduced emitter and pass the source string from each compile path. | Call-site migration: `let ll: str = emit_module_ext(fns, strtab, pool, externs, source);`. |
| `diag_emit_json` | `fn diag_emit_json(diags: Vec<i32>) -> i64` | `fn diag_emit_json(diags: Vec<i32>, source: str, src_path: str) -> i64` | s1 emits a compact JSON array of diagnostics from the shared diagnostic records. tools-suite emits JSONL-style diagnostic objects plus a summary, source snippets, file path, checker buckets, and compilation result fields. | do not merge | These are two different output contracts, not a missing-argument variant of the same function. Collapsing either side under the same name would break consumers that expect array output or tools JSONL/summary output. | Rename rather than adapt: keep s1 `diag_emit_json(diags)` and move the tools contract to a unique name such as `diag_emit_jsonl_with_source(diags, source, src_path)`. |
| `check_fn` | `fn check_fn(pool: Vec<i32>, fn_nid: i64, structs: Vec<i32>, enums: Vec<i32>, diags: Vec<i32>, source: str) -> i64` | `fn check_fn(pool: Vec<i32>, fn_nid: i64, structs: Vec<i32>, enums: Vec<i32>, diags: Vec<i32>, source: str, review_path: str, review_filter: str) -> i64` | The core ownership walk is shared in shape, but tools-suite wraps it with review-surface events, review filtering, event accumulation, and flush-on-finding behavior. | needs adapter | The review arguments are a tools reporting concern layered around ownership checking. The low-risk shape is shared core logic plus a tools wrapper, not choosing one body and losing either review events or s1's smaller core surface. | `fn check_fn_review(..., review_path, review_filter) { review_on_checker_enter(...); let w = check_fn(...); review_on_checker_exit(...); return w; }` |
| `sig_entry_new` | `fn sig_entry_new(plist: i64, rtype: str, is_extern: i64, gparams: i64) -> Vec<i32>` | `fn sig_entry_new(plist: i64, rtype: str, is_extern: i64) -> Vec<i32>` | s1 stores generic-parameter metadata as a fourth entry. tools-suite stores only params, return type, and extern flag. | s1 canonical | Generic signature metadata belongs in the canonical signature table. tools-suite call sites can pass an empty generic-parameter list/default when no generics are present. | Call-site migration: `sig_entry_new(plist, rtype, is_extern, mk_list(pool, Vec::new()))` or equivalent shared empty-gparams value. |
| `sig_add` | `fn sig_add(sigs: Vec<i32>, name: str, plist: i64, rtype: str, is_extern: i64, gparams: i64) -> i64` | `fn sig_add(sigs: Vec<i32>, name: str, plist: i64, rtype: str, is_extern: i64) -> i64` | s1 threads the generic-parameter metadata through `sig_entry_new`. tools-suite calls the older 3-field entry constructor. | s1 canonical | Same reason as `sig_entry_new`: the s1 shape is the metadata-complete form, and non-generic tools-suite functions can pass an empty/default `gparams`. | Call-site migration: `sig_add(sigs, name, plist, rtype, is_extern, empty_gparams)`. |
| `type_last_stmt` | `fn type_last_stmt(pool: Vec<i32>, prog: i64, lid: i64, env: Vec<i32>, sigs: Vec<i32>, structs: Vec<i32>, fn_name: str, diags: Vec<i32>, source: str, tprof: Vec<i32>) -> str` | `fn type_last_stmt(pool: Vec<i32>, lid: i64, env: Vec<i32>, sigs: Vec<i32>, structs: Vec<i32>, fn_name: str, diags: Vec<i32>, source: str, tprof: Vec<i32>) -> str` | Bodies are structurally the same helper, but every recursive type-expression call in s1 carries `prog`. tools-suite calls the older `type_expr` without program context. | s1 canonical | This helper is part of the type-checker context propagation chain. The `prog` argument is needed by the canonical `type_expr`/compatibility machinery; leaving it out keeps tools-suite on the stale type-checker path. | Propagate `prog` through tools `type_check_program` helper calls and pass it into `type_last_stmt`. |
| `type_expr` | `fn type_expr(pool: Vec<i32>, prog: i64, nid: i64, env: Vec<i32>, sigs: Vec<i32>, structs: Vec<i32>, fn_name: str, diags: Vec<i32>, source: str, tprof: Vec<i32>) -> str` | `fn type_expr(pool: Vec<i32>, nid: i64, env: Vec<i32>, sigs: Vec<i32>, structs: Vec<i32>, fn_name: str, diags: Vec<i32>, source: str, tprof: Vec<i32>) -> str` | s1 is much larger and carries program-context-aware checks for newer typing surfaces, compatibility, generics, trait/object logic, unit/taint/result cases, match expression checking, and richer diagnostics. tools-suite is a shorter stale copy with a subset of those cases. | s1 canonical | The missing `prog` is not decorative; s1 uses it for context-sensitive compatibility and trait/type checks. tools-suite should adopt the s1 call graph rather than preserve a partial type-expression implementation. | Propagate `prog` from `type_check_program`; update recursive calls to `type_expr(pool, prog, ...)`; then delete tools copy after type fixture gates. |
| `type_check_stmt` | `fn type_check_stmt(pool: Vec<i32>, prog: i64, nid: i64, env: Vec<i32>, sigs: Vec<i32>, structs: Vec<i32>, fn_name: str, diags: Vec<i32>, source: str, rtype: str, tprof: Vec<i32>) -> i64` | `fn type_check_stmt(pool: Vec<i32>, nid: i64, env: Vec<i32>, sigs: Vec<i32>, structs: Vec<i32>, fn_name: str, diags: Vec<i32>, source: str, rtype: str, tprof: Vec<i32>) -> i64` | s1 carries the newer statement checker with program-context compatibility checks, expanded let/assign/return/match/loop cases, trait/dyn handling, tuple destructure, and related diagnostics. tools-suite is a shorter older checker. | s1 canonical | The statement checker must stay aligned with the canonical `type_expr` and `types_compatible_context` behavior. The safe direction is to thread `prog` through tools-suite and retire the stale checker. | Propagate `prog` through `type_check_stmts` and recursive statement paths; update calls to `type_check_stmt(pool, prog, ...)`. |
| `type_check_stmts` | `fn type_check_stmts(pool: Vec<i32>, prog: i64, lid: i64, env: Vec<i32>, sigs: Vec<i32>, structs: Vec<i32>, fn_name: str, diags: Vec<i32>, source: str, rtype: str, tprof: Vec<i32>) -> i64` | `fn type_check_stmts(pool: Vec<i32>, lid: i64, env: Vec<i32>, sigs: Vec<i32>, structs: Vec<i32>, fn_name: str, diags: Vec<i32>, source: str, rtype: str, tprof: Vec<i32>) -> i64` | Bodies are otherwise the same loop over statement nodes and type-profiler marks; the signature and recursive call differ only by `prog`. | s1 canonical | This is the thin propagation wrapper for the canonical statement checker. Once `type_check_stmt` is s1 canonical, this helper must also carry `prog`. | Call-site migration: `type_check_stmts(pool, prog, lid, env, sigs, structs, fn_name, diags, source, rtype, tprof)`. |
| `match_bind_payloads` | `fn match_bind_payloads(binding: str, blk: Vec<i32>, regs: Vec<i32>, sym: Vec<i32>, mslot: i64, fir: Vec<i32>) -> Vec<i32>` | `fn match_bind_payloads(binding: str, blk: Vec<i32>, regs: Vec<i32>, sym: Vec<i32>, mslot: i64) -> Vec<i32>` | s1 allocates payload locals in the function entry block via `ir_fn_add_entry_alloca(fir, bar)` and records default payload types. tools-suite's same-name function is a back-compat shim that calls `match_bind_payloads_typed(..., "", "")`; tools current real match lowering mostly calls the typed variant directly. | needs adapter | This is not a pure call-site arg default. The lowering path needs a current function IR handle (`fir`) to allocate correctly, while tools also has a legacy untyped shim. Keep the shim under a distinct legacy name or add a typed wrapper around the s1 helper. | `fn match_bind_payloads_legacy(...){ return match_bind_payloads_typed(..., "", ""); }`; update real lowering paths to pass `fir` into the canonical helper. |
| `lower_fn` | `fn lower_fn(pool: Vec<i32>, fn_nid: i64, strtab: Vec<i32>, strtab_aux: Vec<i32>, structs: Vec<i32>, enums: Vec<i32>, globals: Vec<i32>, closures: Vec<i32>, trait_impls: Vec<i32>, fn_decls: Vec<i32>, fn_return_map: i64, require_exprs: Vec<i32>, ensure_exprs: Vec<i32>, invariant_exprs: Vec<i32>, old_snapshots: Vec<i32>, auto_drop_fns: Vec<i32>, manual_drop_fns: Vec<i32>, suppress_auto_drop: i64) -> Vec<i32>` | `fn lower_fn(pool: Vec<i32>, fn_nid: i64, strtab: Vec<i32>, structs: Vec<i32>, enums: Vec<i32>, globals: Vec<i32>, closures: Vec<i32>, trait_impls: Vec<i32>) -> Vec<i32>` | s1 is the production lowerer with auxiliary string tables, function declaration/return maps, contract lists, old snapshots, auto/manual drop metadata, and suppression controls. tools-suite is a shorter older lowerer that lacks those metadata inputs. | needs design blocker | The missing arguments are not thin defaults in isolation; they come from compile-file analysis stages that tools-suite may not currently build in every command path. Integration owner should decide whether tools-suite first adopts the s1 compile-file metadata pipeline or whether a constrained adapter may pass empty structures for specific probe commands. | Smallest safe question: "Can every tools-suite lower path construct `strtab_aux`, `fn_decls`, `fn_return_map`, contract lists, snapshots, and drop lists before lowering?" If yes, migrate the pipeline; if no, add a named limited adapter with empty structures only for commands proven not to need those features. |
| `cache_v2_store` | `fn cache_v2_store(key: str, input_filename: str, flags: str, ll: str, ll_artifact_path: str) -> i64` | `fn cache_v2_store(key: str, input_filename: str, flags: str, ll: str) -> i64` | s1 can copy an already-written LLVM artifact into the cache when `ll_artifact_path` exists, otherwise it falls back to writing the `ll` string. tools-suite always writes the string directly. | s1 canonical | The s1 signature is strictly more capable and backwards-compatible when called with an empty artifact path. It preserves the tools behavior through the fallback branch. | Call-site migration: `cache_v2_store(key, input_filename, flags, ll, "")` unless a real artifact path is available. |
| `run_build_shared_command` | `fn run_build_shared_command(argc: i64) -> i64 { return run_external_tool(argc); }` | `fn run_build_shared_command(src_path: str, out_name: str, use_cache: i64, time_passes: i64, json_mode: i64) -> i64` | s1 is a one-line external-tool delegator. tools-suite contains the real shared-library build flow: read/preflight source, parse program, validate exports, compile, link shared module, emit manifests/header/Rust metadata, and print JSON or text output. | tools-suite canonical | The tools-suite body is the actual command implementation. The s1 body is a proxy for an external tool split, not a canonical implementation of the command. | Adopt/lift tools implementation if command ownership moves into s1; otherwise rename the s1 proxy to avoid duplicate-name import collision. |
| `run_fix_command` | `fn run_fix_command(argc: i64) -> i64 { return run_external_tool(argc); }` | `fn run_fix_command(src_path: str, subcmd: str) -> i64` | s1 delegates externally. tools-suite implements `nuc fix --imports` and `nuc fix --numeric`, including input validation, file reads/writes, import rewrite, and numeric finding reporting. | tools-suite canonical | The tools-suite body owns the command behavior. s1's `argc` proxy is useful only while tools are external; it should not win over the real implementation. | Lift/adopt tools implementation or rename s1 proxy; CLI dispatch must parse `src_path` and `subcmd` before calling the canonical body. |
| `run_doc_command` | `fn run_doc_command(argc: i64) -> i64 { return run_external_tool(argc); }` | `fn run_doc_command(src_path: str, out_path: str) -> i64` | s1 delegates externally. tools-suite is the real doc command entry point and forwards parsed source/output paths to `run_doc_command_mode`. | tools-suite canonical | The tools-suite signature is the implementation-facing command API. The s1 `argc` stub is not semantically equivalent to the doc command. | Lift/adopt tools implementation or rename s1 proxy; dispatch should pass parsed `src_path` and `out_path`. |

## Aggregate Counts By Category

| Classification | Count | Rows |
| --- | ---: | --- |
| s1 canonical | 9 | `parse_match_stmt`, `emit_module_ext`, `sig_entry_new`, `sig_add`, `type_last_stmt`, `type_expr`, `type_check_stmt`, `type_check_stmts`, `cache_v2_store` |
| tools-suite canonical | 3 | `run_build_shared_command`, `run_fix_command`, `run_doc_command` |
| needs adapter | 2 | `check_fn`, `match_bind_payloads` |
| do not merge | 1 | `diag_emit_json` |
| needs design blocker | 1 | `lower_fn` |

## Honest Residuals

- This probe does not retire any duplicate function.
- `SIG_DIFFERS` remains 16 until a follow-up implementation branch lands.
- The riskiest row is `lower_fn`, because the signature delta represents a broader compile-pipeline metadata question, not a local helper mismatch.
- `diag_emit_json` should be renamed/split rather than merged under a single contract.
- The type-checker rows are likely one coherent implementation wave: `type_last_stmt`, `type_expr`, `type_check_stmt`, and `type_check_stmts` should move together with `prog` propagation and focused type fixtures.
- The command rows are likely a separate implementation wave: decide whether s1 absorbs tools commands or keeps external-tool proxies under non-colliding names.
- Full `tools/verify.sh`, compiler drift, tools-suite build, and perf gates were intentionally not run because this queue was probe-only and report-only.
